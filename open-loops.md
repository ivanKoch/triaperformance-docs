# Open Loops — the single source of truth for what's in flight

**Last updated: September 10, 2026** *(storefront branch, Phase 2 — All-Access, then the plan page twice)*. — 🔸 **Two pages stopped explaining and started showing.** ✅ **All-Access reordered in all three languages** — product before maths, the first picture of the SKU at y≈1,300, the sixteen-card library grid cut to three rows. ✅ **A revenue argument that pays for itself:** *TrainingPeaks Virtual ships with Premium, so a cyclist already paying Zwift ($19.99) and Premium ($19.95) is spending* **$39.94** *— the ES/EN price of All-Access — with no plans in it.* ✅ **The plan page was rebuilt, then rebuilt again the same day.** *The morning gave it a dark All-Access card; the afternoon moved that card out of the first screen, because* **two prices ask a reader to pick a commercial model before they have decided the plan is theirs** *— and `monthly-close/2026-08.md` says the product being argued for is 3 → 1 subscribers at 27.3% NRR with the survivor never logged in.* **One price, one primary, All-Access as a sentence; the week is now an object instead of a table row; Heat appears on a plan page for the first time, on the longest session.** 🚨 ***The audit said to reuse the banner PNGs from the TrainingPeaks listings — they carry a retired logo and an uncorrectable "+1000 planes vendidos", and they exist only because that CMS allows four tags and 4,000 characters.*** 🚨 ***And the copy had called sessions "days" for a month*** *— impossible on 66 of 301 plans, and invisible until the week block put the rest-day count directly above the sentence.* ⚠️ **The plan-page half is NOT LIVE — 7 files uncommitted, 1 deleted.** *Which is also why the second audit read as a list of things not done: it was reading the deployed page.* **Iván commits, pushes and deploys.**

*Previously: **September 8, 2026*** *(routine-engine feedback session — the third entry today; the pace-converter session and the weekly hygiene pass are the first two `Previously` blocks below)*. — ✅ **both routine engines changed from one athlete's feedback, 25 paid pages. CLOSED — tested and confirmed live by Iván**, on the second deploy; the first one shipped new markup running August's JavaScript, which is the finding that outlived the four fixes and is in `open-loops-archive.md`. Full record: `ai-infrastructure-documentation.md` §48. **(1) Every block now opens PAUSED with its cue on screen**, with hands-free chaining behind a remembered toggle. 🔑 ***The diagnostic worth carrying: the tool's default behaviour required a workaround on its own first screen, and the workaround was a control the tool provides*** — *when the obvious first action a user takes is to defeat what just happened, the default is wrong, not the user.* **(2) A duration prescription now gets a countdown the athlete starts.** 🚨 ***`strength-tool.js`'s founding principle — "a countdown tells someone to keep going when the prescription is eight good reps" — is right and was applied one step too widely: true of reps, false of a hold.*** *So the one exercise on the knee tool prescribed for its analgesic effect printed "45s" and left the athlete counting. The fix keeps the principle — the hold never completes the set.* **(3) The running activation stopped asking for a towel** it never offered in its equipment question. ✅ **`tests/routine-engines.test.js`, 15 assertions with a faked clock, wired into `npm test`** — *which closes §40's own warning, written four days ago, that `strength-tool.js` had never been checked this way.* ⚠️ ***Two things need Iván and are logged in NOW below:*** *the auto-chain toggle is new CSS that no headless check can judge, and the time estimate said something nobody asked —* **the knee routine is ≈52 minutes, dosed twice a week.** *That is a coaching decision, not an engine one.* 🔓 **Delete permission on the repo folder was granted this session, so `npm run build` and `npm test` both ran clean** *(one pre-existing failure, `/admin/ideas/`, untouched and unrelated)*; **Iván still commits, pushes and deploys.**

*Previously: **September 8, 2026 (pace-converter session, then the weekly hygiene pass)** — **the pace converter shipped and CLOSED: six pages, three languages, public plus a dark members copy, 16 tests.** *Closing note and the five decisions are in `open-loops-archive.md`.* 🔑 ***The decision worth carrying forward is the one that was declined: no email capture on the tool.*** *The zones calculator trades a PDF for an email and that works because the guide continues the job the tool started; a pace converter is a fifteen-second lookup with no such artifact, and a bare email box on it converts near zero while taxing the only property that makes the page worth having.* **The capture belongs on `/recursos/`, which is now the fix for two separate items** — the nav has six Recursos rows behind a label that leads nowhere, and cold traffic still has no public page that starts a sequence. 🚨 ***And a fourth consecutive inventory miss: `/members/garmin/`, live in three languages, in neither inventory of record.*** *Three dated notes already diagnose that pattern; a fourth is not the fix, so it is logged as its own item — generate those tables from `library.json` or delete them.* ⚠️ **Not deployed: the build cannot unlink `_site/robots.txt` from the session sandbox (same blocker as the design-refresh item), so Iván commits, pushes and deploys.**

*Previously: **September 8, 2026 (weekly hygiene pass — first scheduled run)** — **nine closed items moved to the archive, three orphaned NEXT items recovered from `_to_delete/`, and two live-facing claims corrected.** 🚨 ***The finding worth carrying: `NEXT #12`, `#13` and `#14` were written on September 4 into `_to_delete/.next_items_tmp.md` — a dotfile inside a folder named for deletion — and never appended here, so the NEXT section has ended at #11 for four days.*** *#13 and #14 turned out to be closed already; #12 is real and is now queued.* 🔑 **An item drafted outside this file is not queued, it is lost.** *The other two: `members-area-announcement-2026-08.md` excluded its one English athlete because "`/members/en/` — its library is empty", a condition that stopped being true when the English zone calculator shipped* **August 13** *(26 days, on a send due Sept 12); and `artifact-publish-runbook.md` carried the pace converter as* **"not started"** *in one table and* **"shipped Sept 8, all three languages"** *in another — both in the same file, on the same day.* ⚠️ **Eight of the nine archived items were already ticked `[x]` and had simply never been moved — six days after the split whose whole finding was that ticking is not closing.** *Full record below in `open-loops-archive.md`, September 8.*

*Previously: **September 5, 2026** — **two items built, deployed, verified live and CLOSED in one session: per-athlete usage logging and the `/w/` TrainingPeaks link scheme.** *Both are in `open-loops-archive.md` with their closing notes; what stays open in NOW is the pasting pass, which is the half that produces value and has not started.* *Both were opened for the members-area announcement next week, and the announcement is what made them urgent — it is the first deliberate attempt to move "of ~35 athletes with tokens, two have ever opened it", and nothing in place could read the result at tool grain.* 🚨 ***Two findings worth more than the build.*** *(1)* **The parked "deeper analytics" build was one `INSERT` away for a month** — *`/members/check` already ran on every gated request, already held the token, and Caddy already forwarded the path.* *(2)* **This file's own measurement plan for the TrainingPeaks links would have measured nothing**, *and the contradiction was already written down in `ai-infrastructure-documentation.md` three days earlier — a UTM on a gated URL is nested inside `next=` and never parsed.* 🔑 ***The rule both point at: before scoping a build, check whether the data is already passing through something that runs.*** *Detail: `ai-infrastructure-documentation.md` §43.*

*Previously: **September 5, 2026** — **six items closed, and `data/revenue_history.csv` added: the first all-time revenue series this repo has held.** *It sourced the 670% LinkedIn claim (it is +673%, and year three passed year two's full-year total in five months rather than seven) and produced an unlooked-for finding — coaching revenue −22.4% from its February peak, which `tenure-analysis.md` reaches independently from athlete counts.* 🚨 ***Two of the six closed items had been executed days earlier and were still on this list; the rule that came out of it is the first one below.***

*Previously: **September 2, 2026** — **hygiene pass plus four decisions Iván took the same day.***

**Shipped:** *the strength guide in EN and PT (`/members/en/strength/`, `/members/pt/forca/`), closing the last case of `library.json` selling a tool that existed only in Spanish.*
🚨 ***The lesson is the inventories, not the pages: neither `triaperformance-business-overview.md` §Interactive tools nor `artifact-publish-runbook.md`'s Published-artifacts table listed `/members/fuerza/` at all,*** *so the two documents a session would open to test that claim could not contradict it.* **An inventory is only a control if the thing it is meant to catch would appear in it.** *Both corrected.*

**Decided:** *(1)* **`monthly-close/YYYY-MM.md` owns every point-in-time figure from close #1** *— ending a three-way trigger ambiguity that meant nobody owned the handover.* *(2)* 🛑 **Review count and plan count are cosmetic, exempt from the one-home rule, and must never be reconciled or logged again** *— they had generated ~25 KB of notes and items across nine files.* *(3)* **The "+1 small slot" WIP rule is removed; that section is `Backlog`.** *(4)* **This file was split, 252 KB → 138 KB.**

⚠️ ***The one thing still open from the pass:*** *~1.7 KB of inline build narrative per open item. Mechanical moves are done; this one needs a call per item.*

✅ ***Re-measured September 8, 2026 (hygiene pass), and the duplication half of that concern is closed — by the split itself, not by any later work.*** ~~*this file ↔ `ai-infrastructure-documentation.md`, the repo's hottest duplication pair (55 near-duplicate passages)*~~ **is now 4 passages. The pair at 39 is `open-loops-archive.md` ↔ `ai-infrastructure-documentation.md`** — *the same duplication, relocated: the September 2 split moved 44% of this file into the archive and took the duplicated build narrative with it, where it costs nothing because the archive is opened on demand rather than at the start of every session.* 🔑 ***The lesson is about measurement, not duplication: the 55 was measured on the pre-split 252 KB file, the split fixed it the same day, and the note describing the problem outlived the fix by six days because nobody re-ran the count.*** *A finding and its own remedy landing in one session is exactly when a stale note gets written.*

*Prior sessions: the twelve chained "Prior:" entries that used to live in this header —* **43.8 KB of session narrative** *— moved to* `open-loops-archive.md` *§Session log on September 2, 2026, together with* **41 items that had already been ticked closed and never moved (61.2 KB)**, **four items that each described themselves in their own text as duplicates of an item above them**, *and* **the entire August 14 status-claim-audit section**, *which its own opening paragraph had declared fully closed nineteen days earlier.* ***This file is the list of what is open. It is not the record of how anything got that way.*** *That was the Aug 8 split's rule and it had stopped being enforced: the file reached* **252 KB** *— five times the 50 KB that triggered the split — of which* **44% was history**. **Now 142 KB and 67 open items, zero closed.** ⚠️ ***The remaining 142 KB is not clean, and the number to watch is this one: 67 open items across 142 KB is ~2 KB of prose per open item.*** *Each item carries its own build narrative inline, which is why `open-loops.md` ↔ `ai-infrastructure-documentation.md` is the repo's hottest duplication pair at 55 near-duplicate passages. **Moving closed things out was mechanical and is done; trimming open items down to a line plus a pointer needs a judgement call per item and has not been started.***

## Rules

- 🔢 **EVERY item in this file is a `- [ ]` checkbox — NOW, Backlog, NEXT and LATER alike.** *(Standardised September 3, 2026, Iván. NEXT and LATER used numbered and bulleted items, so every count anyone ran — including a whole session's worth of "64 → 45" reports — silently described the checkbox half and read as though it described the file.* ***Arithmetically right, and it told you something false: the real total that day was 83.***) **NEXT keeps its numbers inside the title (`**#1 · …**`) because 27 references to `NEXT #N` exist across 15 files.** *Do not renumber; append.*
- **WIP limit: 1 big branch. That's the whole rule.** A new branch opens only when one closes.
- ~~**+ 1 small slot.**~~ ***Removed September 2, 2026, Iván's call — the section below is now `Backlog`, not a slot.*** *The slot was declared at 1 and held 9 on Aug 12, 11 on Aug 24 and 22 today; five consecutive sessions restated the count and offered the same three options, and none was ever taken.* **A limit violated 22-fold for three weeks was not a limit, it was a label, and the only thing it produced was a paragraph of self-reproach in every hygiene pass.** *His reason for removing rather than enforcing it: "I will do everything anyway" — which is the honest description of how this list has always been worked, and a rule that contradicts observed behaviour loses to the behaviour every time.* **Nothing is to count, report or apologise for the size of the backlog again.**
- 🚨 **An item is not closed until it is struck HERE. Doing the work is only half of it.** *(Added September 5, 2026, Iván — after two items in one session turned out to have been executed days earlier and were still being reported back to him as open: the All-Access `Insert Token in Postgres` check, and the Private 1:1 checkout URL, which `triaperformance-pricing-and-positioning.md` had carried since September 4.)* **This is the September 2 lesson at one remove.** *That pass found 41 items ticked `[x]` and never moved to the archive, and concluded that ticking is not closing. The failure here is one step earlier: work done in the world and never ticked at all.* ***So the test before reporting an item as open is not whether the box is empty — it is whether the owning doc still agrees it is open.*** *Cheap for anything with a home doc; both misses here were one grep each.*
- Every session ends with its branch either **closed** or **parked with an explicit trigger** written here.
- NOW = active this week. NEXT = queued, with its trigger. LATER = parked deliberately, with its trigger.
- When an item closes, move it to `open-loops-archive.md` with its closing note intact. Do not summarise on the way out — the reasoning is what stops the question being re-asked in six weeks.

---

## NOW

**Big branch: the storefront rebuild — opened September 9, 2026.** Home doc: **`storefront-rebuild-brief.md`**, which owns the four phases, each phase's definition of done, and the acceptance criteria. *(Iván: "I am committed to building a site that I am proud of.")*

**Definition of done:** a stranger reaches a plan from `/planes/` in two clicks, sees the members product before paying, and reads the story on `/sobre-ivan/` — and only then do race pages send traffic into it.

✅ **PHASE 1 SHIPPED September 9, 2026** — `/planes/` opens with **12 recommended plans instead of 164**, sport tiles, and an All-Access line in the first screen. **Mobile 47,000 px → 5,657 px; desktop 18,737 px → 3,240 px; all 164 cards still in the DOM and crawlable.** 🚨 ***And a correction: I told Iván the data could not produce hours per week — `plans.js` already derived it and had for months*** *(92% ES / 87% EN / 49% PT).* **Reading a schema is not reading the data layer built on it.** ⚠️ *Criterion 4 half done on purpose — Strength and Duathlon have no hub page, so the grid stays on `/planes/`; splitting needs those hubs first.* 🚨 ***Corrected the same day: the first cap broke all seven sport hubs*** — cycling showed **1 of 33**, HYROX **1 of 7** — *because it hid everything outside the curated one-per-goal set, and only a handful of any one sport's plans are in that twelve.* ⚠️ **A rule that is right for the page it was designed against is not automatically right for the page that reuses the component.** **Two cap modes now:** curated on `/planes/`, a **count of 12 filled as a difficulty ladder** on a sport hub — running and cycling open 4/4/4. Detail: `storefront-rebuild-brief.md` §Phase 1.

✅ **PHASE 1.5 DONE September 9, 2026** — the hub cap finished against a six-rule ticket. **Two rules were already shipped, two passed, and two were real:** 🚨 *`/planes/ciclismo/` opened twelve cards all reading "CICLISMO · FTP" — the v1 ladder varied DIFFICULTY, the axis nobody was looking at.* **Goals are the buckets now: cycling opens FTP/VO2Max/Sprint/Weight Loss 3 each.** ⚠️ ***Then the same bug reappeared one level down*** *— running opened twelve Beginner cards, because taking [0] and [1] from a beginner-first bucket gives two beginners.* **Each goal is now its own difficulty round-robin: running 7/5, cycling 6/3/3.** 🚨 **And `updateDependentFacets()` only ran on change events, so `/planes/running/` offered 1500m as a distance** — called on load now.

🔸 **PHASE 2 STARTED** — **the All-Access page sold thirteen tools and showed zero images of any of them.** *A "míralo antes de pagar" section now carries screenshots of the real built pages driven into a real state — seven zones computed from a 168 bpm threshold, the activation setup with three answers chosen — in all three languages, so the English page shows the English UI.* ⚠️ **Generated from the build: regenerate them there, never edit them by hand.** **NEEDS IVÁN:** *the phase's opening question is still open — ungate a second tool as a public taste, or stop at screenshots.* **The zones calculator's public sibling already proves the pattern works.**

🔸 **PHASE 2, SECOND TRANCHE — the public runner core now captures.** *The phase's opening question ("ungate a second tool, or stop at screenshots") is answered: the tool.* **Offer, pipeline and placement are all built and none of it is live.** Home docs: `lead-magnet-semana-de-fuerza.md`, `tool-lead-runbook.md`.

- [x] ✅ **LIVE — verified end to end against the real stack, September 9, 2026, by Iván.** *`{"ok":true}` on a fresh address · token row carrying `source = tool-lead:runner_week` · HTTP 200 on the unsubscribe with the suppression row written · and* **the same request re-sent returning `{"ok":true,"suppressed":true}` with no second email**, *which is the assertion the whole build exists for.* 🚨 ***It failed on the first request and the way it failed is the finding: HTTP 200 with an empty body, having done nothing*** — *an n8n Set node had dropped the webhook payload, and `responseNode` mode answers 200 when a workflow dies before a Respond node, so the capture script's `r.ok` check would have thanked athletes for emails nobody sent.* 🔑 **The check that caught it was the database one — 200 from the endpoint, zero rows in the table.** *`ai-infrastructure-documentation.md` §49.7.* ~~**Iván: the four steps that make it live.**~~ *(Opened September 9, 2026. Full commands and pass conditions: `tool-lead-runbook.md`.)*
  1. **Twenty** → Settings → Data Model → Person → `leadSource` → add `TOOL_LEAD`. *One value, for every magnet, forever — a value per magnet is the manual step this endpoint exists to remove.* Verify with the `curl` in Step 1, not with the screen.
  2. **Caddy** — the `/api/tool-lead` route is already in `automation/Caddyfile` and ships on the next daily `deploy-website.sh`. To apply now, run that script. ⚠️ *The pass condition is a* **404 from n8n**, *not a 200: it proves the proxy hop works and only the workflow is missing.*
  3. **n8n** — import `automation/tool-lead-workflow.json`, attach five existing credentials, set `TELEGRAM_CHAT_ID`, activate. *No new credentials.*
  4. 🚨 **Send the verification request TWICE.** *The second one, with the address now suppressed, must return `{"ok":true,"suppressed":true}` and no email.* **That is the assertion a happy-path test skips and it is the one this build is about.**
- [ ] 🆕 **Iván: read the week grid on page 4 of the guide.** *(Opened September 9, 2026.)* **It is the only thing in the document he has not already decided somewhere.** The four placement rules above it are his (`methodology.md` §13.1); the grid — *Mon rest · Tue quality + gym after · Wed easy + core · Thu quality + gym after · Fri easy + core · Sat rest · Sun long* — is what those rules produce for a five-run week. 🔑 ***It is derived, not invented, and derived is not approved*** — *and it is the most concrete, most copyable thing in a document that goes to every lead.* **Five minutes.**

✅ **PHASE 1.6 DONE September 9, 2026 — the chooser stopped lying.** 🚨 ***It counted 20 plans twice and left 12 with no door:*** *HYROX and Ironman were tiles beside Running and Triatlón, and both are DISTANCES — every HYROX plan is `sport = Running`, every 70.3/140.6 is `sport = Triathlon` — so the six tiles summed to* **172 against a 164-plan catalogue**, *while Duatlón (10) and Fuerza (2) had no entry point at all.* 🔑 **The audit that opened this caught Ironman and missed HYROX doing the identical thing** — *fixing the one named would have left a chooser that still did not add up.* **Four sports (152) + a secondary "o por objetivo" line + a sentence for the last 12 = 164.** *Also: the sport facet is hidden on sport hubs (`/planes/running/` was offering Ciclismo as a checkbox — a control that can only take you off the page you are on), tiles are a row of links, and five hub heroes went from two paragraphs to one.* Detail: `storefront-rebuild-brief.md` §Phase 1.6.

- [ ] 🆕 **`/planes/duatlon/` and `/planes/fuerza/` have no hub page.** *(Opened September 9, 2026.)* **This is why Phase 1's criterion 4 is still only half met**, and it is now visible on the page: those 12 plans are named in a sentence pointing at the full catalogue rather than given a link, because *a tile that lands on an unfiltered 164-plan grid is a worse answer than a sentence.* **Two pages on the existing hub template closes it.**
- [x] ✅ **CLOSED same day — the library cards are doors now, and the page it was orphaning was worse off than the item said.** 🚨 ***Measured before fixing: the public runner core had THREE inbound links site-wide and all three were itself*** *(its own canonical and hreflang tags). The public zones calculator had* **243**. *"It's published but it's not clickable from anywhere relevant" — Iván, and the number was 0.* **Two doors built: the Recursos nav dropdown in all three languages (3 → 529 inbound links), and the All-Access showcase, where a public entry is now a real link and a gated one deliberately still is not** — *sending a prospect at a login wall converts nothing, which is the rule `tool-cta.njk` already followed and this partial never had.* 🔑 ***The asymmetry is the merchandising:*** *three of sixteen cards carry a heat border, a `GRATIS` tag and "Probar ahora →", so the page now says "you can touch these right now" instead of describing sixteen things equally.* ⚠️ *One layout defect caught by rendering: making the card a column flex container stretched its tag into a full-width bar, because the gated siblings are `<div>`s where an inline-block tag hugs its text.* ~~**`library-showcase.njk` renders no links at all — none of the 16 All-Access library cards is clickable.**~~ *(Opened September 9, 2026.)* 🚨 ***And it makes one of this session's own edits a no-op: `library.json`'s `runnercore` url was repointed from the All-Access anchor to the public page, and the sales page emits no `href` to carry it.*** *A data fix to a field nobody renders.* **The audit's "Core de Corredor is a label with no door" is true of every entry, not one.** *Phase 2 work; the fix is the partial, not the data.*

🚨 **CONTRAST DEFECT ON THE THREE PUBLIC TOOL PAGES — found and fixed September 9, 2026.** *"Recursos" in the nav rendered at* **1.15:1** *— `--ink`, the light theme's near-black, on carbon. Not dim: invisible, in all three languages, on the pages we had just spent the day making public.* **Cause: the dark layer said `.site-nav-sticky nav a { color: … }` and `Recursos` is `menuOnly` — a `<span role="button">`, not an `<a>`.** ⚠️ ***Third instance of one family in four days:*** *`.help a` beat a bare `a` on the auth screens; `.cta-final` declared a background and inherited a colour only right on a light page; and here a rule scoped to `a` could not see the sibling that is not an `a`.* 🔑 **Fixed structurally rather than by adding a selector: colour is set on `.site-nav-sticky nav` and inherited.** *Enumerating selectors converts the elements you thought of; setting colour on the container converts the ones you did not, including whatever is added to this nav next.* **1.15 → 5.81 in ES, EN and PT.** *Iván's report; his breadcrumb half was wrong — that was already 5.81 and fine.*

✅ **Running hubs link the free tool, Iván's call and his placement** — one line BELOW the grid in all three languages, never in the first screen. *His framing: the same motion as a gym posting a free mobility video above its class packs.*

✅ **PHASE 2 DONE September 10, 2026 — All-Access shows the product before it argues the price.** *New order in all three languages: statement hero → how a plan lands on your TrainingPeaks (four steps, a real calendar, a session from the inside) → the members area moving in a phone frame → a tool you can run logged out → the Zwift argument → Haz la cuenta → what's included → the library, cut from sixteen cards to three rows → FAQ.* **Measured: the first picture of the product moved to y≈1,300 and the maths to y≈4,900.** 🔑 ***New revenue argument, and both figures were verified against the source before they went on a sales page:*** *TrainingPeaks Virtual is included with Premium, Zwift is $19.99/mo, Premium standalone is $19.95 — so a cyclist already paying both is spending* **$39.94**, *the ES/EN price of All-Access, with no plans in it.* **On PT those two cost more than the $29.99 product.** *Owner: `triaperformance-pricing-and-positioning.md`.* 🚨 ***An athlete's name (`Luis Casillas`) was on one of the source screenshots and was cropped before the asset entered the repo*** — *customer names never enter this repo, and on a public page it is a consent question too.* ⚠️ **The activation loop is Spanish-only; EN/PT show their own-language still in the same frame,** *because shipping the ES recording on the English page is §40 again.* Detail: `storefront-rebuild-brief.md` §Phase 2 and `ai-infrastructure-documentation.md` §50.

- [ ] 🆕 **Record the activation loop in EN and PT.** *(Opened September 10, 2026.)* **Two-line swap once the files exist** — the markup, the frame and the caption are already identical across the three pages; only the `<video>` vs `<picture>` differs. *Not urgent: the still is honest and in the right language.*
- [ ] 🆕 **Third-party prices now live on the All-Access pages.** *(Opened September 10, 2026.)* Zwift $19.99 and TP Premium $19.95 are quoted in the Zwift section — **and, since September 10, 2026, in the All-Access bullet on all 328 plan pages too** (`planUi.aaCardBullets`). *Two surfaces, one owner: `triaperformance-pricing-and-positioning.md` §Zwift.* ⚠️ ***If either moves, that section is wrong and nothing in this repo will notice.*** **Re-check at each monthly close** — one minute, and it is the only copy on the site that depends on someone else's pricing page.

- [ ] 🆕 **The credential-band badges are being UPSCALED, on every page that carries the band.** *(Found September 10, 2026 while checking image sizes on All-Access; pre-existing, not from that work.)* **Measured file size vs rendered size:** `tp-level2-accredited.png` is **85×85 rendered at 151 CSS px — blown up 1.8× before device pixel ratio even applies**, so it is soft on a plain 1× monitor and mush on a laptop; `esci.png` 242×24 at 234 px (**0.52× at 2x**); `stryd-certified-coach.png` 120×120 at 88 px (0.68×); `ironman-u.png` 394×96 at 141 px (1.40×, marginal). 🔑 ***This is the trust element on the homepage, About and All-Access*** — *the one block whose whole job is to look institutional* — **and it is the blurriest thing on the site.** ⚠️ **Not fixable from here: these are licensed marks and the fix is higher-resolution originals from the four issuers**, not an upscale. *Ask TrainingPeaks, ESCI, IRONMAN U and Stryd for SVG or 2× PNG when next in contact; it is one email each.*
- [ ] 🆕 **The members-tool stills are 0.87× on a retina desktop.** *(Same check.)* `members-zonas-*` and `members-activacion-*` are 1200 px wide and render at 692 CSS px inside `.aa-shot`, so 2× wants 1384. **Visible, but far behind the badges in severity.** *They are generated from the built pages by a script, so the fix is to regenerate at 1400 rather than to hand-edit — same rule as the note that produced them.*

✅ **PHASE 2, second half — THE PLAN PAGE, September 10, 2026, rebuilt twice in one day.** *Morning: Iván asked for colour and a dark card. Afternoon: he read the live page against an external audit, and* ***the read reversed half the morning.*** 🔑 **His two sentences are the brief:** *"before sharing anything about the plan we put in front of the user 2 options… two prices, and they don't even know where they are standing"* and *"the moment you scroll down, we are back to having 4 boxes of bullet points with no separator, no color in between, no nothing."* **The rule the page now obeys: one price and one primary action in the first screen.** *All-Access is a sentence and a link in the buy rail — never a second button — and the carbon card moved to a full-width band mid-page, where it is both the alternative offer and the separator the bullet slab needed.* 🔑 ***What made the trade cheap came from `monthly-close/2026-08.md`:*** *All-Access is* **3 → 1 subscriber, NRR 27.3%, and the one remaining subscriber has never logged in** *— its problem is delivery, not exposure, so the first screen was arguing for a product that converts nobody against the one that sells.* **Also built:** hero chips carrying hours/week (the figure people decide on, previously furthest from the top) · **the week as an object instead of a table row** — stat blocks per discipline, replacing both the breakdown table and the morning's spec list, which stated sessions/week twice · **Heat's first appearance on a plan page, on the longest session** (5.18:1 sampled from rendered pixels; §3.3 licenses it for "the hard end of something") · two prose sections on different grounds · one real TrainingPeaks calendar, captioned as an example because it is not this plan's week · **the foot All-Access module retired**, since three mentions of one product made the last thing a reader saw a different product than the page sells. 🚨 ***The audit's one concrete asset recommendation was the worst move available, and the files decided it:*** *reuse the banners Iván serves inside his TP listings.* **`vp_es.png` bakes five sentences into an 880 KB PNG in another typeface and palette with his email as pixels; `header_running_es.png` carries the RETIRED logo lockup and "+1000 planes vendidos" where nobody can correct it.** ⚠️ *Iván's own message says why they exist — "4000 char limit… I can only use `<p> <h1> <h2>` and `<font>`".* ***An asset built to survive someone else's CMS is evidence of that CMS, not a design decision to import.*** 🚨 ***Two defects found by building:*** *`.chip` is defined only in stylesheets a plan page does not load, so the chips row would have rendered as unstyled text on 328 pages with a green suite —* **sixth instance of this branch's one recurring defect, and the first caught before rendering rather than by it.** *And* ***"Puedes entrenar {sessions} días por semana" called sessions days, in all three languages*** *— on* **66 of 301 plans that asserted more than seven training days a week**, *on 123 it overstated. It survived a month because nothing contradicted it; the week block put the rest-day count above the sentence and it was obvious in one render.* ⚠️ ***And one thing tried and measured away:*** *the rail was written sticky; after scrolling 1,600 px the button sat at viewport* **y = −1,039**. *A sticky child travels only within its own grid area. The rule was deleted and the comment records why, instead of claiming a behaviour the page does not have.* **Measured across ES, EN, PT and one of the 27 plans with no breakdown data:** overflow 0 at 390 and 1440 · one price and one primary above the fold, the All-Access band at y≈1,300 · one form per page · Buy before the week block on mobile · the no-breakdown variant degrades to a single centred rail · `npm test` exit 0 · register sweep 0 lines. Detail: `storefront-rebuild-brief.md` §Phase 2 — the PDP rebuilt.

✅ **THIRD AND FOURTH PASSES, September 10, 2026 — the wrong-fit exit became a comparison, and a stale crawl found two real defects.** **`También puedes`: up to three `.catalog-card`s after the first prose block, ahead of the All-Access band**, replacing two text links at the foot of a bullet list. *A person on the wrong plan needs a comparison, not a whisper — "Ver la versión para Intermedio" carries none of the weeks, sessions, hours or price that made them reject this one.* **Same component as `/planes/`** *(`.catalog-card` lives in `plan-page.css`, which these pages load — checked first, because `.plan-card`, the obvious name, is defined only in the four sport-hub stylesheets and would have rendered nothing)*, **eyebrow carries the relation** *(siblings are same-sport-same-distance by construction, so the catalogue's eyebrow would print one string three times)*, **CTA is a link not a filled button**. **`ref()` in `plans.js` widened to the card's field set, still primitives only** — *that flattening is what stops plan A → sibling B → sibling A blowing Eleventy's data cascade.* **Coverage 303 of 328: 67 rows of three, 129 of two, 107 of one, 25 none.** ⚠️ ***Then Iván relayed a fresh crawl saying the alternatives were still missing — of a deploy that predated the commit.*** *Its "fixed" column describes the rail and the week object as live, so it straddles two deploys.* 🔑 **Working rule worth keeping: an audit of the deployed site is not an audit of the repo, and on a daily-cron deploy those are a day apart.** 🚨 ***Two real defects came out of it, neither the one it named.*** **(1) 71 of 328 plan pages printed the same number twice in the rail** — *41 ES, 22 EN, 8 PT plans are priced at exactly the All-Access figure, so the box read `US$ 39.99 pago único` above `US$ 39.99/mes`.* **The number now leaves that line on those pages only; 0 remain.** ⚠️ *The tempting fix — "por el mismo precio, todos los planes" — was refused: same number is not same price, and this is the page whose only job is a purchase.* **(2) The utility strip was still `--wash`, a pale blue band above a carbon nav, on the three PUBLIC core pages.** *The carbon version existed in `members-dark.css`;* ***the two carbon shells serve disjoint page sets and no page loads both***, *so the nav was themed in one file and the strip above it in the other.* **Same family as the Sept 9 nav rule, one level up: there a rule did not reach a sibling element, here it did not reach a sibling page.** 🚨 ***And the claim that did not hold, with the lesson attached:*** *the note said the carbon nav tokens were "not visible" and `Recursos` is "still dim grey".* **Pixel-sampled: 5.85:1, which clears §3.4's 4.5:1 gate — dim is what a secondary token is for, and dim and failing are different properties.** ⚠️ ***My own first reading said 3.8:1 and was wrong, because the nav is `rgba(--bg,.85)` and a computed-style walk composited that translucency over white.*** **Third wrong computed-contrast reading today against three correct pixel samples — stop computing contrast on anything translucent.** Detail: `storefront-rebuild-brief.md` §Phase 2 — third and fourth passes.

- [ ] 🆕 **One home for the carbon site chrome.** *(Opened September 10, 2026.)* **`.site-nav-sticky` and `.nav-utility` on carbon are now written twice** — `members-activacion.css` for the tool pages, `members-dark.css` for the 16 members pages — *because the two shells serve disjoint page sets and neither is universal.* **The fix is one body-level carbon class in `site.css`, which every page loads; `base.njk` has a bare `<body>` and no hook for it.** *Small, and it retires a duplication that has already produced one live defect.* ⚠️ **Until then, edit both files or neither** — both carry a cross-reference saying so.

✅ **THIRD PASS THE SAME DAY — the wrong-fit exit became a comparison, September 10, 2026.** *Iván: "I want the plan alternative offering to improve", carrying a second audit note.* ⚠️ ***Most of that note was auditing the LIVE page, which is the pre-rebuild version because none of this is pushed*** — *the rail, the week blocks and the single help CTA it listed as missing were already in the working tree.* **Checked item by item against the current build instead of answered from the ticket: H1 renders once, forms one, filled primary buttons three (Buy top, Buy foot, All-Access).** 🔑 **The new recommendation was right and is built:** *"¿No te encaja del todo?" plus two text links was a footnote —* **a person on the wrong plan needs a comparison, not a whisper**, *because "Ver la versión para Intermedio" carries none of the weeks, sessions, hours or price that made them reject this one.* **Now a `También puedes` row of up to three `.catalog-card`s after the first prose block, ahead of the All-Access band**, so the page reads as a ladder: *not this one → these three → or all of them.* **The same component as `/planes/`, not a second card system** *(`.catalog-card` lives in `plan-page.css`, which these pages load — checked first, because `.plan-card`, the obvious name, is defined only in the four sport-hub stylesheets and would have rendered nothing)*; **the eyebrow carries the relation instead of sport · distance**, *which would print the same string three times since siblings are same-sport-same-distance by construction*; **and their CTA is a link, not a filled button** — *three filled blues under Buy is the competing-CTA defect at a distance.* **`ref()` in `plans.js` widened from three fields to the card's set, still primitives only** — *that flattening is what stops plan A → sibling B → sibling A blowing Eleventy's data cascade.* **Coverage: 303 of 328 pages — 67 rows of three, 129 of two, 107 of one, 25 none.** *One sibling shows one card rather than being padded from another sport.* 🚨 ***And the audit's other surviving claim, worth the pattern more than the fix:*** *it said "H1 printed twice". The H1 renders* **once** *— but the breadcrumb printed `plan.displayName` in full directly above an `<h1>` printing the same 90-character string, so the page did open with the same sentence twice.* **The observation was right and its stated mechanism was wrong**, *which is exactly the case that has to be checked rather than accepted or dismissed.* **Breadcrumb truncated to 48 characters.** ⚠️ **Three things re-raised and NOT done:** *the TP banner PNGs (decided against on the files themselves, not an open question), a sticky rail (tried and measured away), and a "next distance" card slot —* ***that one is a real addition rather than a template change***, *since it needs a per-sport distance ladder (5K→10K→21K→42K, Sprint→Olympic→70.3→140.6) that does not exist in the data.* **Iván's call whether it earns its build.** *Also flagged, not touched: "Cómo funciona el entrenamiento" is the same 80/20 paragraph on all 328 pages — true, and it is coaching copy `methodology.md` owns, not a design call.*

**Order, Iván's:** **1** `/planes/` refactor · **2** All-Access refactor, including a way to see the members area from outside · **3** About with the real story · **4** race landing pages.

**Why ahead of race pages** *(NEXT #1, researched and ungated since Aug 6)*: race pages send traffic into this funnel, and **building the top of a funnel whose middle is a warehouse spends the traffic to prove the middle is broken.** Race pages get better the day this closes.

**The measurement that opened it, taken against the live site:** `/planes/` renders **164 cards on first paint, zero hidden, 18,737 px tall at 1920** (~47,000 px on a phone), no images. The plan page puts **Buy at y=556 and the email capture at y=625 — 69 px apart** — two competing primary actions — with zero images and a one-row "what you'll train per week" table. *The athlete then lands on TrainingPeaks and finally sees a photo, chips and a sample week.* ***The storefront hands off to the page that does the selling.*** ✅ ***All three halves of this measurement are now closed*** *— the 164 cards on September 9, the 69 px on September 10, and the empty plan page the same day. It stays here as the evidence that opened the branch, not as a current reading.*

**WIP:** the referral branch keeps its entry below but not the slot — *its own note has said since August 26 that it waits on the calendar rather than on work, and the first payout that can physically exist is October 6.*

⚠️ **Two items already in this list are phases of this branch and are NOT duplicated into it** — read them where they are, and close them there:
- **"Nothing in the library is visible to a prospect"** → phase 2. *Its three options are that phase's opening decision.*
- **"The About page does not tell the story"** → phase 3. *The portrait shipped Sept 9; the story is what is still missing.*

- [ ] 🆕 **`data/testimonials.csv` created September 9, 2026 — 17 rows seeded, 4 publishable, 13 waiting on a number.** *It answers "how do you join?" by removing the join: quote and result live in one row keyed by a repo-assigned slug, not by athlete name — accents drift, some athletes have no surname on file, and a translated page cannot match a Spanish name.* **`source` and `consent` decide what renders: a row needs a `figure` and a consent that is not `pending`.** 🚨 ***`recall` is never publishable on its own*** — a remembered finish time printed beside a named real person is a claim about their race. **Iván's half:** fill the numbers he has from WhatsApp and Instagram (already consented) and mark them `given`. *Full column spec: `storefront-rebuild-brief.md` §Testimonials. Not yet wired into the site — the loader is a phase-1 task.*


- [ ] 🆕 **Decide whether the knee routine is the session you mean to prescribe.** *(Opened September 8, 2026. Home doc: `knee-strength-brief.md` §Athlete feedback.)* **`/members/rodillas/` home + roller now states ≈52 minutes** — 29 sets across warm-up, strength and foam rolling — against its own dosing line of **twice a week on easy endurance days**. *(Achilles ≈45, shoulder ≈38, cyclist core ≈26.)* 🔑 ***Nothing is wrong with the figure. The routines have always been that long; the time estimate added the same day is simply the first thing that ever said so out loud*** — *which is the useful half of shipping it.* **A coaching call, deliberately not taken in the build session.**
- [ ] 🆕 **Design refresh — L1 shipped, L2 is the next tranche.** *(Opened September 6, 2026.)* Home doc: **`design-refresh-brief.md`**, which owns the argument, the findings and the tiered work list. **Both design decisions are closed** — Archivo as the single variable typeface, and a heat signal token taken from the zone model — *and `brand-guidelines.md` is now v1.1 with its three months of inline changelog moved to `ai-infrastructure-documentation.md` §45.*
  ~~**Needs Iván:** *(b)* grant delete permission or run the build himself; *(c)* commit and deploy.~~ **Both done September 6, 2026** — commit `6de4ed2` *"redesign"*, pushed, and the 6am deploy cron has run three times since. ~~⚠️ The one thing that is not verified: the tests pass (12) but were last run against the previous build.~~ **Resolved the same session — full suite green against a fresh build, exit 0.**
  **Still needs Iván, and it is now the only thing holding this:** ***look at the homepage on a real screen.*** *The hero scrim, the wordmark and the whole type scale changed, and the last three fixes were made against a headless render — nobody has yet seen this in a real browser on a real display.* **Verified intact September 9: nothing since `6de4ed2` has touched the design files** — `c4e4b4d` *"nav bar comments"* is `nav.njk` + `nav.json` only.
  ✅ **Homepage tranche DONE September 9, 2026** — items 13/14/15 in `design-refresh-brief.md` §3c: unequal offer row with the featured 1:1 card, testimonial result chips, and the price treatment, in all three languages. ***This is where the heat token first renders — it had been defined and used zero times for three days, because every place it is allowed to appear was a component that did not exist yet.***
  ⚠️ **Two open, both small and both Iván's:** *(a)* **no Google reviews URL exists in the repo**, so the "link the 46 reviews" half of item 14 is unbuilt — `site.json` has the count and no link; *(b)* **the homepage states `$149` twice**, on the featured card and again in the `.pricing` section — *this predates the change (the old card already carried it in `card-meta`)* and is the existing three-option decision about that section, deliberately not taken here.
  🚨 **And the finding worth keeping: the mockup that won the decision showed a chip reading `MARATÓN · 2:58`, and there is no 2:58 anywhere in this repo.** *Every athlete time lives inside a quote as they phrased it; there is no finish-time dataset.* **Chips now restate only their own quote, and quotes naming no result get none — two of four in ES/PT, all three in EN.** *The EN page having zero chips is `NEXT #7` showing up as a hole in a component, not a bug.*
  ✅ **Third tranche DONE September 9, 2026, all from Iván's read of the live page** — `design-refresh-brief.md` §3d. All-Access promoted to a middle tier (blue rule, real price figure, outline CTA — *deliberately not the Heat treatment, which would have rebuilt the flat hierarchy the row was made to fix*); testimonials reworked so the result is **display type at 24px** rather than an 11px chip that read as a tag; the utility strip's wash restored.
  ✅ ***The light/dark split is closed.*** **`members-dark.css` — one override layer on the 15 pages that were still light**: three logins, three password-recovery screens, six Garmin/Carga pages, three Fuerza guides. *Additive: not one word, table or figure in the guides changed, only the surface under them.* **Every page under `/members/` is now carbon, and `brand-guidelines.md` §3.3 states it as a rule** — a members page that renders light is a bug, not one that has not been converted yet.
  **Still open, both Iván's and both small:** *(a)* **no Google reviews URL in the repo** — `site.json` has the count and no link, so "46 reseñas" is still plain text; *(b)* **the homepage states `$149` twice**, featured card and `.pricing` section — *predates all of this* and is the existing three-option decision about that section.
  ✅ **Fourth tranche DONE September 9, 2026** — `design-refresh-brief.md` §3e. **Testimonials v2 (result-first, every figure a real number), the review bar linked to the Google listing, the featured offer card in carbon with Iván's portrait, and the portrait on all three About pages.** 🚨 ***The reason two earlier testimonial passes failed: `site.css` held two implementations, and v0's curly-quote `::before`/`::after` survived every rewrite — a later rule overrides a property, never a pseudo-element that is still declared.*** *Deleted.*
  🚨 **Contrast defect found and fixed: TP Blue as TEXT on carbon on the auth screens, 2.34:1** — a breach of §3.3, which survived the members conversion because `.help a` and `.auth-lang a.current` are class-scoped and beat a bare `a`. ⚠️ *Converting a theme by restyling element selectors silently misses every rule written with a class — worth a check anywhere else this pattern was used.*
  ⚠️ **Hero: tested at four viewports, the image paints correctly everywhere and the reported "desktop is a blur" is a 247 KB unpreloaded CSS background, not a layer-order bug.** *Fixed with a conditional preload rather than by removing the LQIP, which would have restored the flat rectangle it was added to eliminate.*
  ✅ **The duplicate coaching block is DELETED — Iván's call, September 9, 2026.** *"Nuestro enfoque personalizado" and its EN/PT siblings, plus the now-dead `.pricing` / `.price-card` rules.* **The homepage states `$149` exactly once and Home is five sections: hero → three offers → testimonials → credential band → contact.** ⚠️ **`#planes` lived on the deleted section and three things point at it** — the hero's second CTA in all three languages and `/en/#planes` from a published blog post; *it moved onto the Plans card in the offer row.* **Rule worth keeping: before deleting a homepage section, grep the whole repo for its `id` — the references are mostly not on the homepage.**
  **Left in the tier list, none of it queued:** the record strip on Sobre Iván as type, the credential band on `/sobre-ivan/`, plan cards with a discipline mark, and the four items blocked on assets only Iván can make — *the portrait first, which is what turns the featured card from a bigger box into a person.*


- [ ] 🆕 **Read the EN and PT members FAQ before the announcement goes out — the only thing from the Sept 4 link fix that needs Iván's eye.** *(Opened September 4, 2026.)* **The members nav has carried an FAQ item pointing at `#faq` on `/members/en/` and `/members/pt/` since Aug 10, and the anchor did not exist** — *the link did nothing, on the two pages the un-told-cohort announcement (`members-area-announcement-2026-08.md`) is about to send subscribers to.* **Both sections now exist**, adapted from the Spanish one: five answers on TrainingPeaks access, the downloads page, password recovery, billing and what the subscription includes.
  ⚠️ ***It is customer-facing copy that has not had his eye on it.*** *Nothing in it is new policy — every answer describes how the product already works, and the PT scope answer renders `plans.counts.byLang.pt` rather than restating a number.* ~~**Ten minutes of reading, and it is the last gate on two pages a paying subscriber lands on.**~~
  🚨 ***The gate was missed, and this item is the record of it.*** **The announcement went out September 7 and Nadine — the one English athlete in the cohort — was included and sent to `/members/en/`** *(`members-area-announcement-2026-08.md` §3, corrected the same day).* **So an English subscriber has already landed on FAQ copy nobody has read.** *Noted September 9, 2026, not as an alarm — it is descriptive copy about how the product already works, and the realistic downside is a wrong detail rather than a wrong promise — but the item called itself "the last gate before the send" and the send happened without it.* ⚠️ **This is what an item with a real trigger and no owner looks like: nothing failed, nobody was wrong, and it simply did not happen.** *Either read it now, or downgrade it to Backlog and stop carrying it in the list every session reads first.*


**Big branch: the referral program — opened August 26, 2026.** Home doc: **`referral-program-brief.md`**, which owns every decision. *(The previous branch, members area in three languages, closed Aug 21 — WIP limit respected. This was `NEXT #10` and is promoted here; that item is struck below with the three decisions this session reversed.)*

**Definition of done:** the disclosure message sent to every engaged athlete, and one referral attributed end-to-end in Twenty — `referredBy` set, `referralRewardSentDate` filled after a real payout.

**Why this and not race landing pages (NEXT #1):** Iván's call, Aug 26 — CoachMatch acquisition is soft and the SEO build is slow, and this is the only acquisition lever that produces athletes inside 60 days without ad spend. *Stated so it is not later read as a miss: the realistic yield is 2–4 signups over two months against a break-even gap of one signup a month. It closes the gap; it does not replace a channel. Reasoning in `referral-program-brief.md` §9.*

**BUILD COMPLETE — August 26, 2026, one day.** Twenty fields (`referredBy` relation + `referralRewardSentDate`, confirmed by a live API response); terms pages ES `/referidos/` and EN `/en/referrals/`, both `noindex` and link-only; six messages in `sales-playbook.md` B9a–B9e; and `automation/referral-payout-reminder.json` **imported, live-tested and active** — the test read 238 people across 5 pages and returned the two real Feher referrals correctly.

***The WIP slot is released. This branch is no longer blocking `NEXT #1` (race landing pages), which has been ungated since Aug 6.*** **What remains is elapsed time, not work:** the disclosure send, and then a real referral converting. **The first payout that can physically exist is October 6, 2026** (program floor Sep 1 + 35 days), so the reminder correctly stays silent until then — *do not read that silence as a fault.*

**Open, and both are waiting on the calendar rather than on anybody:**
- [ ] **One referral attributed end-to-end** — `referredBy` set, second payment confirmed in the TrainingPeaks payments list, gift card sent, `referralRewardSentDate` filled. **That closes the branch.** Not possible before Oct 6.
- [ ] **Read the result at the September and October closes.** *Realistic yield is 2–4 signups over two months against a break-even gap of one signup a month (`referral-program-brief.md` §9). **Planning for 10 reads as failure at 4, and 4 is a good outcome.***

**The three things this branch settled that NEXT #10 had wrong**, all in the brief with full reasoning: (1) **two motions, not one** — disclosure to every engaged athlete at any tenure, the personal ask stays earned; (2) **referral and review asks are separate lists**, because they no longer share eligibility criteria; (3) **the founding-member framing** — the disclosure message *does* state $149 alongside an unconditional rate guarantee, reversing `sales-playbook.md` B9.

~~**Big branch: members area in three languages — opened August 10, 2026. Skeleton SHIPPED the same day; the branch stays OPEN until the tools exist.**~~ **CLOSED August 21, 2026 — confirmed live by Iván: all three languages working, every tool artifact published in Spanish translated and confirmed correct in English and Portuguese.** Technical record: `ai-infrastructure-documentation.md` §22, §25, §26. *(The previous branch, 1:1 athlete onboarding, closed Aug 9 and moved to `open-loops-archive.md` when this one opened — WIP limit respected. This branch's still-open library/content items move to a carried-forward list below rather than blocking closure — none of them were the branch's own definition of done. WIP slot is now open for the next big branch.)*

**Why this and not race landing pages (NEXT #1):** the All-Access welcome email now goes out in ES/EN/PT and promises "new tools every week" against a members area that had ten Spanish pages and zero EN/PT. Race pages grow the top of the funnel; this branch is the thing already sold and not delivered. It also removes a real constraint — until the shell existed in three languages, every tool built could only ever be built once, in Spanish.

**Shipped August 10, 2026 (skeleton only — no tools):**

- `/members/en/` and `/members/pt/` — home, login, forgot-password. Library sections render a **designed empty state**, not cards linking to the Spanish pages. Deliberate: pointing an EN member at a Spanish tool sets the expectation that the site is translated when it isn't.
- **Login now routes by the token, not by the page.** All three login pages POST to the same `/members/login`; `app.py` reads `preferred_language` off `subscriber_tokens` and sends the subscriber to their own language's home. That column has existed since July and this is the first thing to actually use it.
- **Three Caddy gates**, `/members/en/*` and `/members/pt/*` declared *before* the catch-all `/members/*` so a 401 redirect lands on the right-language login. Language is a path **segment**, not an `/en/` prefix like the public site, specifically so one auth tree keeps covering everything — a prefix would mean three trees, and an ordering mistake in one exposes a whole language's paid content with no error thrown.
- **Open-redirect fix, out of scope but fixed here:** `next` was passed through unvalidated. Now same-site absolute paths only, server-side and client-side.
- **"Acceso Miembros" / "Members Access" / "Acesso Membros" button in the public nav**, all three languages — ghost/outline with a padlock, deliberately secondary to the solid Contacto CTA. Points at the members *home*, not the login page, so a subscriber with a live cookie walks straight in.

~~**Definition of done for this branch:** the zone calculator live and working in all three languages.~~ **MET August 13, 2026 — branch ready to close.** Calculator live in ES/EN/PT (12 public pages, localised slugs, hreflang wired), `zonesUi.json` fully translated, and the lead-magnet guide built as three per-language PDFs. The three items held back pending translation all released in the same pass: EN/PT article CTAs restored, EN/PT `Recursos` dropdowns added, and the n8n workflow stopped hardcoding the Spanish PDF. Detail: `ai-infrastructure-documentation.md` §25. ~~*Remaining before this can be struck: Iván deploys and spot-checks a page per language, and the members-area tools themselves are still Spanish-only — the branch shipped the shell and the calculator, not a translated tools library. Decide whether that is a new branch or out of scope.*~~ **Struck August 21, 2026.** Both resolved: Iván confirms all three languages working in real use, and every tool artifact published in Spanish has been translated and confirmed correct in English and Portuguese — not the shell and the calculator alone. *(The tool-translation item elsewhere in this file already showed "eight tools in three languages" as of Aug 13 — this caveat had gone stale before it was last read. Worth remembering as a caution: a branch-closing note is exactly the kind of line that doesn't get revisited same-day.)* **Branch CLOSED.**

> 🚨 **Correction, August 24, 2026 — weekly hygiene pass. This closure's "every tool artifact published in Spanish translated and confirmed correct in English and Portuguese" is not true today.** `site/members/core-ciclista/` ("Cyclist Core") shipped Spanish-only on **August 18, 2026** — three days before this branch closed — with no EN/PT sibling anywhere in `site/members/en/` or `site/members/pt/`, and no home doc (unlike `achilles-brief.md` / `knee-strength-brief.md` for the other strength-engine tools). **It was never logged as an open item, so it was invisible to every session that started by reading this list — including the one that wrote the closure note above.** Worse: `site/_data/library.json` — the single source for what all three All-Access sales pages claim the library contains — lists `cyclistcore` as `gated: true` and **live**, with fully-translated English ("Cyclist Core") and Portuguese ("Core do Ciclista") marketing copy, each pointing at that language's own All-Access page. **An EN or PT prospect is being sold a tool that does not exist once they subscribe in their language.** Not fixed here — fixing it means either building the EN/PT pages or pulling live marketing copy, and that choice is Iván's. New item below.
>
> ✅ ***RESOLVED September 2, 2026, weekly hygiene pass — option (a) was taken and nobody logged it.*** *`site/members/en/cyclist-core/index.njk` and `site/members/pt/core-do-ciclista/index.njk` both exist, `cyclist-core-brief.md` exists as the home doc the Aug 24 finding said was missing, and its Status line reads "v1 SHIPPED in all three languages, August 18, 2026" — **the same date the Aug 24 pass recorded as the ES-only ship date.*** **So either the EN/PT pages shipped alongside the ES one and the Aug 24 pass checked `site/members/` without checking `site/members/en/`, or they were built between Aug 24 and today and the item was never ticked.** *The evidence favours the first: the brief is dated Aug 18 and claims all three languages. Either way the marketing copy in `library.json` is now true. Item closed and moved to `open-loops-archive.md`.*

**Deployed and verified live the same day.** Container rebuilt, site + Caddyfile deployed, and six checks run against the real stack from the VPS: three gate redirects landing on the right-language login with `next` preserved, two failed logins returning to the right-language screen, and a real PT token routing to `/members/pt/`. All passed. Detail in `ai-infrastructure-documentation.md` §22.

### Carried forward from the members-i18n branch (branch closed Aug 21, 2026 — none of these blocked closure; ongoing library/content backlog)

- [ ] **Get the transparent TrainingPeaks wordmark from TrainingPeaks.** *(Opened Aug 27, 2026.)* Iván asked for the plain TP logo to represent Coach Match in the credentials band. **The file he has (888×356) carries an opaque dark-navy background** — `alpha_min` 255, corners `#040F2F` — so in a white band beside three transparent wordmarks it renders as a dark rectangle. ***Keying it out is not an option: that alters a trademark***, and TrainingPeaks publishes usage rules alongside the badge they do license. They ship a transparent variant. **When it arrives:** split the `tp` row's `items` in `site/_data/credentials.json` into two band rows and TrainingPeaks takes two tiles. *Not blocking — the licensed Level 2 shield currently carries both credentials, and it names the credential where the bare wordmark would not.*

- [ ] **Ask ESCI for a larger logo, ideally SVG.** *(Opened Aug 27, 2026.)* The file is 252×36 and crops to 242×24, so it renders at 1× and looks slightly soft on a retina screen. It is stored **native on purpose** — a 3× upscale was tried and rejected at 71KB and visibly blurry, and *soft beats blurry*. Cosmetic, one file, fixes itself the moment a better asset exists.

- [ ] **Remove the IRONMAN U entry in 2027.** *(Opened Aug 27, 2026, with its trigger stated so it is not re-litigated.)* Iván holds **both** IRONMAN U (2025) and ESCI today and states IRONMAN U disappears in 2027. It is printed **with its year** for exactly this reason — `IRONMAN U Certified Coach · 2025` stays true after the programme is gone, where a bare present-tense claim would silently become false. **When it goes: delete the `ironmanu` entry from the three `items` arrays in `site/_data/credentials.json`. Nothing else needs touching** — the strip, the three About pages and the `Person` schema all read from that file. *ESCI is the survivor and Iván notes its awareness is currently low, which is the argument for keeping both while both are true, not for dropping the newer one.*


- [ ] **Check the live n8n email bodies for links to the four retired pages.** `/members/carga/` was found pointing at `/members/zonas/` — retired the same morning — and only *worked* because of a Caddy redirect, under link text naming a page that no longer exists. **Fixed there, and the rest of the repo is verified clean**: a grep of `site/` and `automation/` for `zonas`, `tests`, `carrera` and `kettlebell` returns only the two intentional Caddy redirects. *What a grep cannot reach is n8n* — the welcome, resend-password and nurture bodies live in the n8n instance, not the repo copies, and several predate the retirements. Iván to check them in the UI. **Note `carrera` and `kettlebell` have no redirect at all, deliberately** (gated + `noindex`, so only a subscriber's own bookmark can reach them and a 404 tells the truth) — but that also means an email linking either one now dead-ends. *The reason this needed a real pass rather than a check for 404s: a redirect makes a broken link invisible to every automated check while leaving the wrong promise in the copy.* (Found Aug 13, 2026.)

- [x] ✅ ~~**Send the All-Access PT subscriber outreach — drafted September 4, 2026, unsent.**~~ **SENT September 4, 2026**, token rotated first, same day it was drafted. Drafts and the rotation record: `all-access-pt-subscriber-outreach-2026-09.md`. ***Measurement is the open half: re-check `access_count` on or after September 14*** — this subscriber had never logged in, so a non-zero count is the entire read on whether the message worked. Record it in `monthly-close/2026-09.md` and retire the doc. → move to archive after the check. *(Original item, for the record:* Email + WhatsApp in Portuguese, both in `all-access-pt-subscriber-outreach-2026-09.md`, which also carries the token-rotation commands and the figures each claim rests on. **Audience is one person**: the only live All-Access subscriber, PT tier, never logged in. *The trigger is real rather than manufactured — coach support was added to All-Access today and they get it retroactively, so there is something to hand over that did not exist when they subscribed.* **Three steps, all Iván's:** identify the right PT row (two exist — cross-check `customerType = ALL_ACCESS` in Twenty), rotate the token, send. ***Do not fold this into the members-area announcement*** — that is 33 coaching athletes in Spanish being told "your coaching includes this"; this is a paying subscriber in Portuguese being told "your subscription includes this."*)


- [ ] 🚨 **Sell All-Access to Portuguese CoachMatch leads — everything is built, nothing is live.** *(Iván's idea, September 4, 2026; both workflows rebuilt as importable JSON and handed over September 5.)* **A Brazilian CoachMatch lead used to get no WhatsApp and three Spanish emails, then close as `LOST_NO_RESPONSE`.** *The sequence now forks on **language, not price**: $149 coaching in ES/EN stays the lead offer, All-Access at $29.99 answers the objection that creates.* Copy, framing and the full build record: `coachmatch-portuguese-sequence.md`. **Settled and not to be re-litigated:** TrainingPeaks' CoachMatch team is fine with the 3.5% routing; the PT checkout title is **not** being renamed (live subscriber, and `'acesso total'` anchors two nodes in `subscription-lifecycle-automation.json`) — the email names the old title instead; Brazil comes out of the WhatsApp skip, **Argentina stays**.
  ✅ **DEPLOYED September 5, 2026.** ~~(1) import both JSONs~~ **both imported, credentials checked, both `active: true`**; ~~(2) commit and push `automation/twenty_followup_check.py`~~ **done**; ~~(4) re-export both workflows~~ **done — repo copies verified against live the same day: 19 and 13 nodes, `Route by Language` with `fallbackOutput: 1`, the four-way nurture Switch, `preferredLanguage` in the GraphQL query and carried through, `Skip WhatsApp Outreach — AR` holding only Argentina and the no-phone check, no orphans, no secrets.**
  ⚠️ **Still open: (3) verify on a multi-item execution — and Iván's call, September 5, 2026, is to WAIT FOR REAL LEADS rather than replay a mock.** *The reason is volume:* **Iván gets Portuguese leads almost every day**, *so the first real Brazilian lead arrives within days and the synthetic test buys little.*
  🚨 ***But be precise about what waiting does and does not test.*** *A daily single lead exercises the language branches and nothing else.* **The once-for-all-items bug only appears when one IMAP poll delivers two leads at once** *— confirmed live July 31, 2026, when exactly that updated only the first lead with no error thrown — and that is rarer than a daily lead, so it may go unobserved for weeks and will then be observed on real people.* **So: read the first Brazilian lead as proof the fork works, never as proof the multi-item path does.** *The two-lead case stays unverified until a real poll carries two, or until someone replays one.*
  ✅ ***And the volume figure is the more useful half of this note:*** **Portuguese CoachMatch leads arrive almost daily** *(Iván, September 5, 2026)*. **That makes the baseline below worth pulling now rather than later** — a channel producing a lead a day and converting none of them is the whole reason this item exists, and it is measurable today.
  ⚠️ ***The repo's workflow copies were stale and it cost a translation.*** *Portuguese email 1 was first written from the repo's Spanish body, which no longer matched the live one — the live email had been rewritten and the "$149 vs $229 vs $359" pitch was gone from it.* **Before translating or quoting any n8n copy, get a fresh export; the repo copies are documentation, not the live source.** *Both were replaced from Iván's exports on September 5.*
  ✅ ***Baseline CONFIRMED September 6, 2026, after Iván reviewed and corrected the statuses by hand:*** **34 Portuguese people in Twenty all-time — 30 `LOST_NO_RESPONSE`, 2 `MESSAGE_SENT`, 1 `WON_CUSTOMER`, 1 test record. Mailable backlog: 30.** 🚨 ***And the question this item was opened on is answered: Portuguese CoachMatch leads have converted ZERO times, ever.*** *The one `WON_CUSTOMER` is the All-Access subscriber, who came through `WEBSITE_FORM`, not CoachMatch — Iván confirmed it.* **So there is no cannibalisation risk in Portuguese at all, and the 6:1 net-revenue caution never applies here.** ⚠️ *But do not carry that conclusion into a Spanish rollout — Portuguese can prove the offer converts; it cannot prove it is safe beside a funnel that works.* ***Also settled: no PT lead has ever replied, so `LOST_PRICE` and `LOST_NOT_INTERESTED` are both empty and the planned second email variant is retired unsent.*** *Volume read: ~15–17 PT leads/month, so the Sept 5 automation touches more leads by November than the whole backlog holds — the backlog is a one-time 30; the automation is the asset.*
  ***Baseline to pull before or alongside the first send:*** how many Brazilian CoachMatch leads all-time, how many ever reached `WON_CUSTOMER`. **If zero, there is no cannibalisation risk to reason about.** If not zero, the trade is 6:1 against ($19.94 vs ~$119.20 net) and the language framing is the only thing protecting the coaching price.

- [ ] **Nothing in the library is visible to a prospect.** All 24 members pages are `noindex` behind the token gate, by design. So the strongest evidence All-Access has — a working calculator, an adaptive routine builder — cannot be seen before paying, and the sales page can only assert it. **Options, none decided:** ungate one tool as a public taste (the zones calculator already has a public sibling, so this is nearly free), put real screenshots on the All-Access page, or record one short screen capture. *Iván's call; note the public zone calculator already proves the pattern works — it exists, it is indexed, and it is the only tool a stranger can touch.*

- [ ] **The library has no race in it.** *(Gap analysis, Aug 13, 2026.)* Two retirements the same day both removed race-day support: `/members/carrera/` (pacing and race plan by distance) and `/members/nutricion/` (fuelling and carb loading). What remains covers the athlete's **body** (activation, core, knees, Achilles, breathing) and their **data** (zones, training load) — and nothing covers the day the outcome is decided. **Pacing and fuelling are the two most common amateur race failures**, and race week is when an athlete most wants the coach's voice. *Both were retired on purpose and neither should be restored as-was; the observation is that the shape of the library now has a hole where its most emotionally valuable moment used to be.*

- [ ] 🚨 **The inventory tables are not covered by the standing check, and that is now a three-time failure.** *(Found September 3, 2026 while adding the mobility row — the same way the Sept 2 instance was found.)* `/members/hombro/` shipped in all three languages that morning and appeared in **neither** inventory of record: not `triaperformance-business-overview.md` §Interactive tools, not `artifact-publish-runbook.md`'s Published-artifacts table. **Both rows are now added, along with the mobility rows — the residue is the process gap, not the data.**
  ***The pattern across `cyclistcore` (Aug 24), `strength` (Sep 2) and `hombro` (Sep 3) is identical: the row gets added when the tool is translated or audited, never when it ships.*** `kb-hygiene-prompt.md` Step 0 diffs `library.json` against `site/members/`, which is the check that catches mis-selling — **it does not diff either inventory table, which is the surface that has actually failed three times.** *The Sept 2 note ends "an inventory is only a control if the thing it is meant to catch would appear in it", and the very next tool to ship did not appear in it.*
  **Two options, neither taken — Iván's call:** extend Step 0 to diff both tables against `site/members/` as well (cheap, catches it a week late), or make the row part of shipping (free, requires nobody to forget). *Recording it rather than picking, because a fourth instance is the real argument for the second option.*


- [ ] **The members area has sixteen cards and no first step.** *(Was "thirteen"; re-derived from `site/_data/library.json` September 8, 2026 — 16 live entries per language, `train` 7 · `pain` 3 · `numbers` 6. The Sept 4 update below states the groups as 5·3·5 = 13 and was correct on its date; three tools have shipped since. **The item's substance is unaffected and gets stronger with every tool: more cards is more grid to guess at.**)* The onboarding block was removed Aug 10, 2026 with an explicit note: *"a new onboarding block belongs here once there is a real sequence to walk someone through, not before."* **There is now.** A new subscriber lands on a filtered grid of eight and has to guess; the honest sequence is roughly test → zones → read your load → prehab if something hurts → activation before quality → breathing after. *The trigger that note was waiting for has fired.*

  *Update, September 4, 2026 — partially eased, deliberately not closed.* The flat grid is now **three labelled groups** (`Antes y después de entrenar` 5 · `Cuando algo duele` 3 · `Entender tus números` 5), which replaced six filter chips of which three matched exactly one card and one held six unrelated ones. **That is orientation, not a first step** — it tells a subscriber what kind of thing each tool is, and still not which one to open on day one. The sequence in this item is unchanged and still unbuilt. *Also note the card count in this item's own title was wrong: it said eight, and there have been thirteen since the strength guide shipped.*

- [ ] **Nothing in the library remembers anything.** Every tool is stateless: no history, no streak, no "you did this twice this week", no record that an Achilles programme is in week 3 of 12 — which is the one thing a 12-week loading protocol actually needs. *This is the difference between a tool someone visits and a tool someone uses,* and for a subscription it is the retention lever. **Non-trivial:** needs per-subscriber storage, and the members-area auth already has a subscriber identity to hang it on (`ai-infrastructure-documentation.md` §21). Not a v1 item; the biggest single upgrade available to the library as a whole.


- [ ] **Wire `sent_at` into the send workflow — the sequence board reads 0 sent until it exists.** *(Opened September 6, 2026, with the board.)* `/admin/secuencias/` is live and every rate on it divides by **`sent`**, never by minted — *because the send skips anyone already suppressed, and dividing unsubscribes by a roster that includes people who were never mailed understates the burn rate, which is the one number that must never look better than it is.* **So the send workflow has to `UPDATE unsubscribe_tokens SET sent_at = now() WHERE token = $1` after SMTP accepts.** *Do it in the same pass as the suppression retrofit below — same workflows, same node.* ⚠️ ***The September 6 PT backlog send predates this, so its 30 rows will never have `sent_at`.*** *Backfill them by hand from the execution log rather than leaving the first campaign permanently reading zero.*

- [ ] 🚨 **Retrofit the suppression check into every sender — six workflows send mail today and none of them check it.** *(Split out of the suppression build when that closed, September 6, 2026. Exact node pattern: `unsubscribe-runbook.md` §Retrofit.)*
  **The list:** `coachmatch-lead-automation.json` (email 1, both languages) · `coachmatch-email-nurture-2-3.json` (emails 2 and 3, all four branches) · `plan-lead-workflow.json`, `zone-workouts-workflow.json`, `contact-form-workflow.json` (lead-magnet delivery) · `subscription-lifecycle-automation.json`.
  ⚠️ **One of these needs a decision, not a copy-paste: whether welcome and password-resend mail gets an account-mail exception.** *Decide it explicitly and write it down — a quiet exception is how a suppression list stops meaning anything.*
  *Pattern per workflow: a Postgres node before the send —* `SELECT 1 FROM email_suppression WHERE lower(email) = lower($1)` *with `alwaysOutputData: true`, then an If that sends only when it comes back empty.* 🚨 ***The send node must read the recipient from the node that produced it, never bare `$json`*** *— at that point `$json` is the suppression query's result. That exact mistake cost three debugging rounds on September 6.*
  *`automation/twenty_followup_check.py` is out of scope: WhatsApp, and no suppression concept exists for it today.*

- [ ] **Measure the CoachMatch PT backlog send — 30 emails sent September 6, 2026.** *Source tag `coachmatch-pt-backlog-2026-09`; workflow `automation/send-pt-backlog-workflow.json`, framing in `coachmatch-portuguese-sequence.md`.* **Two things to do:** (1) the arithmetic follow-up email **around September 12**, to non-responders only, minus anyone who replied or unsubscribed; (2) read the result at the September close.
  ***Record in `monthly-close/2026-09.md`:*** *subscriptions from the 30, landing-page sessions on `utm_campaign=pt-backlog-2026-09`, and unsubscribes — `email_suppression.source` carries the campaign tag, so the burn rate is measurable for the first time.* **Baseline: this channel has converted zero Portuguese leads in its life.**
  ⚠️ **Do not set these leads back to `MESSAGE_SENT`.** *They are `LOST_NO_RESPONSE`, which is the only reason the nurture workflow ignores them; flipping the status would enrol all 30 into the CoachMatch sequence and fire emails 2 and 3 at them.*

- [ ] **`Recursos` has no page of its own.** *(Aug 12, 2026: it now has a first piece of real content waiting for it — the TSS/PMC explainer, **NEXT #11**. Pointer only; that item owns the scope.)* It renders as a menu-opener, not a link, because a dropdown parent pointing nowhere real is worse than one that doesn't pretend to. **A real `/recursos/` hub is the proper fix, and it would double as the public landing page for the lead magnets** — the small-slot item below records that there is currently nowhere public to capture an email at all. One page, three languages, and it closes both.
- [ ] **Running has no threshold-improvement plans to offer** after the capture. Cycling and swimming resolve by rule from the catalogue; running's plans are faceted by race distance, so the block renders an honest empty state. Needs hand-picked IDs from Iván.


- [ ] **Four of the five downloadable guides are still Spanish-only PDFs.** *(Was "the downloadable guides are still Spanish-only"; corrected Aug 13, 2026 — the zone-sessions guide now exists in all three, and `/members/en/downloads/` and `/members/pt/downloads/` opened to hold it.)* The nutrition and mental-prep guides, long and short, remain ES-only. The EN/PT pages say more are coming rather than hiding it. Separate work from the tools — translating a PDF is not the same job as building a calculator three times.
### Carried follow-up from the onboarding branch


### Carried over from the storefront branch — catalogue strategy, not build work

- [ ] **Duathlon is the most under-built category in the catalogue.** Earnings per published plan by sport: **Duathlon $121** (15 plans), Strength $70 (2), Triathlon $66 (51), Swimming $41 (40), Running $37 (135), **Cycling $29** (60). *(Triathlon updated Aug 10, 2026 — 49 → 51 plans, $69 → $66, after the two EN Ironman flags were corrected. Ranking unchanged.)* Cycling has four times the inventory of Duathlon and a quarter of the return per plan. This says nothing against the new Ciclismo hub — 60 plans need a door — but it does say the next plans built should be Duathlon, not Cycling. Worth a session to decide which Duathlon gaps to fill. Source: `training-plans-analysis.md` §5. (Aug 6, 2026)
- [ ] **Run a pricing test — the site finally makes it possible.** *(Iván, Aug 6, 2026.)* Units are flat (160 → 170 → ~204 annualised) while average sale doubled ($21 → $49), so **price has been the only working growth lever and it has never been tested deliberately** — it drifted upward as the catalogue changed.
  **Do the free version first: elasticity from existing data.** 507 sales spanning $19.99–$79.99 already exist in `data/plan_sales.csv`, and per-plan view counts exist in the BigQuery pixel data / `plan_views_clean` on the VPS. Conversion by price band is computable today with zero risk. **Confound to control for:** average price rose partly because the *mix* changed (HYROX at $56 avg arrived in Aug 2025), so compare within a sport and plan family, never across the whole catalogue — otherwise the analysis just rediscovers the mix shift.
  **Then the live test.** TrainingPeaks has no split-testing, so a true A/B isn't available; two workable designs — (a) **sequential**: move one plan family ±20% for 8–12 weeks against unchanged siblings as control, using the same before/after window logic as the listing-rewrite experiment; (b) **matched pairs**: the same plan already sells at different prices across languages, which is a natural experiment sitting in the data.
  **Why it matters now:** a 10% price move worth taking is worth ~$600/yr on current volume and costs nothing to implement. Seasonality says run it outside Jan and Jun so a peak doesn't masquerade as a result. Trigger: any time; natural companion to the TP listing-rewrite read in late October.

### Backlog

- [ ] 🆕 **The per-tool ranking is fragmented by language, and merging it changes which tool is on top.** *(Opened September 8, 2026, and it follows directly from the decision above: if the language an athlete lands in is incidental, then* ***the tool is the unit of analysis and the path is not.***)
  **`member_tool_usage` groups by `path`, so every tool is split across up to three rows** — *and with 38% of views landing on English paths, that split is not a rounding artifact.* **Merged by tool, the first real reading of the library reorders:**

  | tool | ES + EN views | *stated ranking by path* |
  |---|---|---|
  | **recovery** | `recuperacion` 4 + `en/recovery` 5 = **9** | *3rd* |
  | **knees** | `rodillas` 6 + `en/knees` 1 = **7** | *1st* |
  | **activation** | `activacion` 3 + `en/activation` 2 = **5** | *4th* |
  | **mobility** | `movilidad` 2 + `en/mobility` 2 = **4** | *5th* |

  ***Recovery is the most-used tool in the library and the path-level view reports it third.*** *It also owned the most-clicked `/w/` code on September 7 (`recuperacion-semana`, 7 clicks) — two independent signals agreeing, which the fragmented view hid.*
  **Fix:** *resolve `path` → `library.json` key in the view, the same mapping `/w/` already performs at request time — no new inventory, which was §43's own design constraint.* ⚠️ **Athlete counts cannot simply be added** *— someone who opened `rodillas` and `en/knees` is one athlete in two rows, so the merged figures above are view-accurate and athlete-upper-bound. A tool-keyed view fixes both; hand arithmetic fixes only the first.*
  ⏳ **Trigger: before the September close reads a per-tool distribution.** *This is the row that answers "what do I build next", and today it answers it wrong.*

- [x] ✅ ~~**`member_tool_usage` counts the members HOME as a tool, which will corrupt the "which tools" ranking the close is built on.**~~ **FIXED in the repo September 8, 2026 — `automation/members-area/schema.sql`.** *The three language homes and the login pages are filtered out of `member_tool_usage`, and the browse signal moved to a new `member_home_visits` view.*
  🔑 ***Two design calls worth keeping.*** *(1)* **Filtered in the view, not documented as a caveat** — *same rule as `token_roster` twelve lines above it: the query people actually type is the short one, and a warning asks someone to remember. Second application of that rule in this file.* *(2)* **An `is_tool` flag was the first design and was rejected** — *a flag still hands the wrong ranking to anyone who forgets to filter on it, and it would have changed the column list, which `CREATE OR REPLACE VIEW` refuses rather than warns about (the trap already recorded in `schema_analytics.sql`). Splitting the rows into two views keeps the column list identical, so the migration is a plain re-run.*
  ⏳ **Not live until Iván runs the file against the `members` DB** — *idempotent, every statement `IF NOT EXISTS` / `CREATE OR REPLACE`.*

- [ ] 🆕 🚨 **`ivankoch87@gmail.com` is not flagged `excluded_from_metrics`, so Iván counts as an active member in every members-area figure.** *(Found September 8, 2026, from his own question.)* **Proven, not suspected:** *he appeared in a query whose `WHERE` clause contained `excluded_from_metrics = FALSE`.*
  🔑 ***This is a miss against the flag's own stated purpose, not an open design question.*** *`schema.sql` says the column was added* **"for the Bogotá tester and Iván's own tokens"** *— plural. The four `coach+` accounts were flagged; the personal one was not.* **Almost certainly because whoever ran the UPDATE matched on `coach+`, and `ivankoch87@gmail.com` contains none of `members_access_gap.py`'s `TEST_EMAIL_MARKERS` (`coach+`, `curl`, `prueba`, `test@`) either** — *so both mechanisms in this system independently classify him as a real athlete, by the same blind spot.*
  ⚠️ ***And it is the GA4 lesson repeating in an instrument three days old.*** *This repo has measured, at length, that* **25–46% of GA4 sessions were Iván's own**, *and built `analytics_internal_devices` and `ga4_selftraffic_month` in response. The members-area log launched September 5 with the same hole — and worse, because the population is ~38 people, so one heavy internal user is* **2.6% of the denominator and the single most active account on the site.**
  **The decision is his and it is not obvious:** *he coaches himself, so he is arguably a real athlete using the tools. But the roster already resolved the analogous question the other way —* `athlete-onboarding-flow.md` *subtracts him explicitly to reach 37 — so today the revenue side excludes him and the usage side includes him.* **Whichever way it goes, the two sides should agree, and right now nothing has ever compared them.**
  *Also unverified in the same breath: whether the Bogotá tester — the other person the schema comment names — was actually flagged. Same one-line query answers both.*

- [x] ✅ ~~🚨 **A Spanish athlete on an English-locale phone lands on the English members page.**~~ **WON'T FIX — Iván's call, September 8, 2026, and it is right.** *Measured first: **5 Spanish-token athletes, 11 views on English pages** — 38% of all tool views, and Nadine, the only English token, is not in the data at all. So it is common, not rare.* **And it does not matter.** *His reasoning: plenty of his athletes run English-locale phones, as he does, and* ***every tool exists in all three languages*** *(verified this session — 48/48 `library.json` entries), so an athlete routed to English gets the same routine, not a worse one.* 🔑 **The repo's own test settles it: who acts differently once this is fixed? Nobody.** *The `login()` code path is unchanged and still ignores `preferred_language` when `next_url` is set — that reading was correct — but a defect nobody experiences as a defect is not a defect.*
  ⚠️ ***One narrow exception, recorded so it is not rediscovered as a surprise:*** **`/members/en/downloads/` carries 1 PDF against Spanish's 5** *(`artifact-publish-runbook.md`; the four Spanish-only guides are their own open item above).* **That is the single page where landing in English delivers less**, *and it closes when those guides are translated — not by touching the redirect.*
  🚨 ***The trap this leaves behind, and it is the reason this item is archived rather than deleted:*** **English page usage is NOT English-language demand.** *A future session reading "38% of members-area views are on `/members/en/`" would reasonably conclude there is an English-speaking audience worth building for. There is not — it is Spanish athletes with English phones.* **Anyone segmenting this data by page language is measuring device settings.**

- [ ] 🆕 **The project's Custom Instructions field goes stale by design — make it static instead of remembering to repaste it.** *(Opened September 8, 2026, from Iván's own question: "should I start every day with a copy paste?")* 🛑 **No. Daily pasting is the exact thing this project killed on August 12** *— twenty documents re-uploaded by hand every morning, removed because they cost his time forever, which is the test this project applies to everything.* **Rebuilding that ritual in a smaller box is the same mistake.**
  **The real shape of the problem:** *the mirror is edited by sessions, automatically, whenever a doc is added, renamed or retired. The field is edited by hand. So the gap opens on doc churn — roughly weekly — and never on a schedule, which is precisely why a daily habit is both too often and unreliable.* *Measured twice: 14 entries missing September 2, 14 again September 8.*
  ***The fix that takes his time once:*** **most of the field does not go stale — the working relationship, the guardrails and the hygiene rules have barely moved in a month. Only the KB index churns.** *A session's first act is already to open this repo. So the field can carry the durable half plus one line — "the document index lives in `triaperformance-project-instructions.md`; read it from the repo before answering anything about the knowledge base" — and the index maintains itself in git, where sessions already edit it.* **The field then changes when the working relationship changes, which is rarely, and the daily-paste question stops existing.**
  ⚠️ ***One thing to verify before doing it, because it is the whole load-bearing assumption:*** *that a session reliably reads the index from the repo when the field only points at it. Today the field carries the index inline, so nothing has ever tested the pointer.* **Trigger: any time. Until then the interim rule below stands.**
  **Interim, and it is two occasions rather than a habit:** *(1)* **whenever any session tells him a doc was added, renamed or retired** — every session is already required to say so; *(2)* **unconditionally after each weekly hygiene pass**, which is the only thing that reliably detects the drift. *Twenty seconds, once or twice a week, not daily.*

- [ ] 🆕 **`members_access_gap.py --expect` goes stale every week by design — decide whether it earns its keep.** *(Opened September 8, 2026, first live use.)* **It failed with `--expect 290, fetched 305`, and nothing was wrong:** *`--expect N` is an exact-equality gate (`len(out) != expect`), Twenty had gained 15 people since the number was written on September 5, and the CRM gains people continuously.* ⚠️ ***So this gate will refuse almost every weekly run, and a check that cries wolf weekly is the thing this repo has already written down twice as training people to skim.*** **The guard that actually catches the original bug is the other one** — *`totalCount` vs fetched, line 239, which did not fire, which is how we know pagination is healthy.* **Two options:** *(a)* drop `--expect` and rely on the `totalCount` guard, *(b)* change it to `--expect-min` so it catches an under-read without going stale as the book grows. *(b) keeps a second gate independent of `totalCount` — which was the point — without the maintenance. **Either is ten minutes.** Trigger: any time; next hygiene pass at the latest.

- [ ] 🆕 **Read the members-area announcement — 48-hour snapshot recorded, next read at the September close.** *(Opened September 8, 2026. Baseline: `data/monthly_close/2026-08-metrics.csv`.)*

  📊 ***SNAPSHOT AT 2026-09-08 13:00 UTC, ~48h after the send. Recorded here deliberately and now, for a reason that matters more than the numbers:*** *Iván starts pasting `/w/` links into TrainingPeaks workouts this week. Once that lands, the September close cannot separate "the announcement worked" from "the workout links worked" — **two interventions inside one reporting period with no measurement between them is one uninterpretable result.** This snapshot is the divider.*

  | | Before (lifetime, to Sep 6) | **After (Sep 7–8)** |
  |---|---|---|
  | Distinct athletes in the members area | **2** | **13** |
  | Page views | 7 | **30** |
  | Athletes opening 2+ tools | 0 | **3** *(one opened 4)* |

  *Denominator is* **38** *real token holders (43 active tokens − 4 QA − Iván's own athlete account), so* **34% of the book walked in inside two days, against 5% in the fourteen months before.** *One more athlete clicked a link without reaching a page.*

  🔑 ***The finding is the delivery mechanism, not the size.*** *`workout_link_clicks` shows* **30 clicks across 9 `/w/` codes, 27 of them anonymous** *(**29 athlete clicks** — one of the anonymous rows, `movilidad-post` at 12:04:23 on Sept 7, was Iván's own: his page view lands 13 seconds later at 12:04:36. Corrected September 8, 2026 when the per-athlete timeline made it visible.)* *— and the timing proves those are not lost: `recuperacion` last click 12:28:26, `dfmartinezenriquez` first page 12:28:41; `activacion-bike` 20:08:13, `smarizmendi` 20:08:17; `activacion-swim` 00:10:28, `lopezmarino.mariae` 00:10:51.* **Four, fifteen and twenty-three seconds. The anonymity is the login wall sitting between the click and the cookie, exactly as `schema.sql` predicts — it is not leakage.** ***So the per-athlete link is what moved this, and it did so before a single link exists inside a TrainingPeaks workout.*** **That is the evidence the library-pasting item needed and did not have.**

  **Two rows to carry into the September close, both currently zero:**
  - `tischhausern@gmail.com` (Nadine, ENGLISH) — token and members area shared September 7; `access_count = 0`. *No conclusion at two days; it is the first English data point this business has ever had.*
  - `milhoci@yahoo.com.br` (PORTUGUESE, the one paying All-Access subscriber) — outreach sent September 4, `access_count` still **0** at four days. **That is the Sept 14 re-check, and it is currently reading as a null result.**

  ⏳ **Next read: the September close.** *Nothing between now and then, and that is deliberate.*
- [ ] 🆕 **The weekly hygiene pass cannot run on its schedule — the scheduled task has no folder bound.** *(Opened September 8, 2026, by the first scheduled run failing.)* **`folders_state: FOLDERS_STATE_NONE`, `folders: []`** on task `trig_01Pfo8TnD7Hoj6wCnXEp22VM`, so the session opens with no access to this repo and cannot read `kb-hygiene-prompt.md`, which is its first instruction. *It fired 13:25 UTC, failed at step zero, and was re-run by hand.* **Folders attach at approval, and cannot be added to an existing task**, so the fix is to recreate it from a desktop session with `triaperformance-docs` connected and delete the old one. ***Iván's, and nothing else in this file is blocked by it — but every Tuesday until then is a wasted run that reports only its own failure.***

- [ ] 🆕 **Decide what `members-exercise-dedup-review.md` is — a doc, or scratch.** *(Opened September 8, 2026.* **Owner: Iván — confirmed September 8 that the review itself is pending on his end**, *so this is a real initiative awaiting his read, not an abandoned artifact. It still needs indexing or retiring either way.)* **56 KB, generated September 7 by `automation/extract-members-exercises.js` + a clustering pass; 246 (artifact, exercise) records → 205 candidate canonical exercises, in Tier A/B/C clusters.** *It is the only `*.md` in the repo the KB index has never named — the same gap `brand-board.html` had until Sep 2.* ⚠️ ***It is also a list of proposed work sitting outside `open-loops.md`, which is the shape this repo's "one home per list" rule exists to stop*** — *and `_to_delete/.next_items_tmp.md`, found the same session, is what that shape costs.* **Three options, none taken:** *(a)* it is a generated report → leave it unindexed and say so in the generator, *(b)* it is a real initiative → index it, give it a home-doc header and put its actionable half here as one item, *(c)* the merge is not happening → retire it. **Nothing depends on the answer; it only gets more expensive to answer.**

- [ ] 🆕 ~~**Confirm the two stale zone-calculator test records are gone from Twenty.**~~ 🚨 **THIS WAS A DUPLICATE OF AN ITEM THAT ALREADY EXISTED** *(the "two stale test records sitting in the CRM at `MESSAGE_SENT`" item under the CoachMatch greeting-name section). Opened September 5, 2026 by a session that had read this file the same day and still wrote a second copy — the exact failure the September 2 pass found four instances of.* **Merged into the original, which is the one to read. The answer, from Iván the same day: they are STILL IN TWENTY**, so the 292 → 290 drop was two other records leaving, cause unknown. *(Original text:* `coach+zone-en@` and `coach+zone-pt@`, `leadSource: ZONE_CALCULATOR`, sitting at `MESSAGE_SENT` since the August zone-magnet testing. **The evidence they may already be deleted is indirect and worth stating as such:** *`backfill_person_names.py` fetched* **292** *people on September 4 and* **290** *on September 5, and these two are the obvious candidates for the difference.* ⚠️ ***That is a coincidence of arithmetic, not a confirmation*** *— two other records could have gone instead.* **One filter in Twenty settles it** (People → `leadSource = ZONE_CALCULATOR`). *If they are gone, the existing item under the CoachMatch greeting-name section closes with them; if not, delete them or move them to a terminal status.*)*

- [ ] 🆕 **Decoupling now has a home doc; eight other places still explain it inline.** *(Opened September 4, 2026, the session the guide shipped.)* Owner: `/blog/desacople-aerobico-interpretar-pa-hr/` and its EN/PT siblings.
  **The one-home rule applies to explanations, not only to figures.** *`pacing-triatlon-larga-distancia-ironman` (all three languages) states the 5% rule and derives it; `micro-intervalos-umbral-vs-tempo-running`, `metodo-noruego-*`, `w-prime-potencia-ciclismo-calcular-fatiga`, `como-entrenar-series-de-umbral-potencia-rpe` and `lead-magnet-sesiones-por-zona.md` each carry their own partial definition.* **None of them is wrong today** — which is exactly how the storefront brief started — *and the fix is a pointer per file, not a rewrite: keep the sentence that serves that article's argument, link the guide for the rest.*
  ⚠️ *Do not sweep this without reading each passage. Two of them use decoupling as a retest trigger and one as a pacing rule; those are different claims and collapsing them into "see the guide" would lose the point of the paragraph.*

  **Done:** the three homepages state it on both coaching surfaces (services card + $149 price card), the owning rule is struck with its reasoning, and `referral-program-brief.md` and `sales-playbook.md` both carry a dated flag.

  1. ✅ ~~**The referral program has lost its reward and nobody has replaced it.**~~ 🚨 ***Wrong, and withdrawn the same hour on Iván's correction. The program was never resting on Premium.*** *`referral-program-brief.md` §1 states the offer in two lines and always has:* **$50 Amazon gift card to the referrer, and `NOSTARTUP` — the $50 startup-fee waiver — for the person referred.** *Premium appeared only in the LTV parenthetical and in the "same plan" list on `/referidos/`, which describes the standard offer rather than a referral perk.* **Nothing about the program changes; the two-sided reward is intact.** ⚠️ *The lesson worth keeping: the alarm was raised off a side-note in the economics paragraph without reading §1, which is three lines above it and is the section that owns the offer.* **`/referidos/` needs no edit either — its list is headed "El mismo plan", so the Premium line became more true, not less.**
  2. ✅ ~~⚠️ **`sales-playbook.md` A5 does not mention Premium**~~ **DONE September 5, 2026 — and the item was half wrong, which is the useful part.** *Private had carried the Premium line since September 4; **CoachMatch never had it**, and a CoachMatch athlete gets Premium too (TrainingPeaks pays it out of the 20%).* **So the website promised Premium on both coaching surfaces while the message half the leads actually receive said nothing about it.** ⚠️ ***An item phrased as "A5 does not mention X" invited a session to open A5, find X, and close it wrongly*** — *name the block, not the section.* *(Original:*, so the website now promises something the closing message a lead actually receives does not. *Premium is a $19.95/month retail item — the most quotable single line in the offer, currently missing from the one place a buying decision gets made.* **Message copy is Iván's voice and was deliberately not invented for him.**
  3. ✅ **The cost is small and is now counted rather than estimated: $18/month, upper bound $27.** ~~*Upper bound 36 × $9 = $324/month, so the worst case more than doubles the cost base.*~~ 🚨 ***That estimate was wrong by 12–18× and is struck — corrected by Iván, then verified against `data/monthly_close/2026-08-roster.csv`.*** **TrainingPeaks pays Premium for CoachMatch athletes as part of the 20% commission, and All-Access already has its own $9 deducted before payout — so Iván only pays for PRIVATE athletes.** *Roster: COACHMATCH 35 · PRIVATE 4 · ALL_ACCESS 3, and `premium_fee` = 9 on exactly three rows in the file. Three Private athletes are active and one already carries the fee, so the incremental cost is two.* ⚠️ ***The lesson is that `pricing-and-positioning.md` already said in bold "never derived here — counted at decision time from the live roster", and the estimate was derived anyway, off the wrong headcount.*** **Nothing to do here; it is kept as the record of how the number went wrong.** *Still worth eyeballing at the September close as Private headcount grows — that is the only line that scales.*

  ✅ *One thing genuinely improves: the open tension logged in the referral brief — a service difference keyed to acquisition channel, against pricing rule 4's "one uniform service" — is resolved in favour of rule 4.*


  *The services section now reads as a ladder — Coaching $149 → All-Access $39.99 → Planes desde $19.99, ordered by service level, each card carrying its price.* **The pricing section immediately underneath is still a single `.price-card` for 1:1 coaching only**, headed "Un precio claro" / "One Clear Price". *That was coherent when the section above made no price claim. It is now a visible gap: a reader who has just been shown three prices scrolls into a section that names one and calls itself the price.*

  **Not fixed here on purpose — it is a design decision with real options and no obvious winner:** *(a)* widen it to a three-column price comparison, *(b)* leave it as the coaching-only conversion block and retitle it so it stops claiming to be the whole picture, or *(c)* delete it, since the services cards now carry the prices. ⚠️ *Note (a) is the one that changes what the homepage is for — a three-way price table invites comparison shopping on a page whose single CTA is a coaching enquiry, and coaching is the $2,773 line. Worth deciding deliberately rather than by symmetry.*


  **The problem in his words:** *if you don't know TrainingPeaks — and even if you do — that claim does not convey that you get his full catalogue and can load those plans into your own TrainingPeaks calendar.* **The buyer has to already understand a third-party product's plan-assignment model before the headline inclusion means anything**, and nothing on the page teaches it. *This is a different failure from the library one just fixed: that page was **wrong**, this one is **true and unintelligible**.*

  **Interim copy shipped the same day, so the page is not left as it was:** the card is now *"Todos mis planes, dentro de TrainingPeaks"* and states the catalogue size, that plans load into the TrainingPeaks calendar, that more than one can run at once, and that switching is free. **Counts render from `plans.js` `counts` — never typed.** *That is a patch on a text card; it is not the fix.*

  **The real fix is a demonstration, and Iván's instinct is a YouTube video showing what it means.** *Options, none decided:* **(a)** a short screen recording — subscribe → open TrainingPeaks → browse the catalogue → drag a plan onto the calendar → swap it; **(b)** a public page with real screenshots, which doubles as indexable content where a video does not; **(c)** both, the page embedding the video. ⚠️ **Whichever it is, this belongs in front of the paywall** — it is the same "nothing is visible to a prospect" gap the library item records, applied to the larger half of the product. *A video also answers the "¿Cómo accedo después de suscribirme?" FAQ better than the FAQ does.*

- [ ] 🆕 **`all-access.css` styles bare `h2` and `.section-intro`, and it leaks into every shared partial the page includes.** *(Root cause of the "Acreditaciones text only uses half the area" bug Iván reported September 4, 2026 — his words: **"this bug keeps happening"**, which is the part worth acting on.)*

  **What happened:** `all-access.css` line 33 is a bare `h2 { … text-align: center }` for the page's own sections. `partials/credentials-band.njk` is a **shared** partial designed left-aligned — label, lead, logo grid and "Sobre Iván" link all start at the same left edge — and it renders correctly on the homepage and the three About pages, *which do not load this stylesheet.* On All-Access the bare rule reached in and centred only its heading, over a lead paragraph capped at `65ch`. **Measured: every element at `left=204`, the h2 alone at `text-align: center`.** *A centred heading over a left-aligned half-width paragraph is what "only uses half the area" looks like.*

  **Fixed narrowly** with `.cred-band h2 { text-align: left; }` — restoring the partial's own design rather than un-centring the page, since the centred h2 *is* correct for this page's own sections.

  🚨 ***The item is the pattern, not this instance.*** *A page-scoped stylesheet using bare element selectors will silently restyle every shared partial that page includes, and the damage shows up on the partial — far from the rule that caused it.* **Same family as `members-home.css` `.site-nav` vs `.site-nav-sticky` (§41) and §26 before it: a rule that is correct for what its author was looking at, and wrong for what they weren't.** *Worth a pass over the page-scoped stylesheets for bare `h1`/`h2`/`h3`/`p`/`a`/`ul` selectors and scoping them to their own sections. Cheap, and it retires a recurring class rather than another instance of it.*


- [ ] 🆕 ⚠️ **Paste the `/w/` links into the TrainingPeaks library workouts.** *(The build behind this closed September 5, 2026 and is in `open-loops-archive.md`; `ai-infrastructure-documentation.md` §43 is the technical record.)*
  **Everything is live and verified end-to-end** — 34 codes, `/admin/enlaces/` to copy from, and real rows in `member_access_log` for both a `/w/` click and a page open. ***What has not happened is the only part that produces value: the links are not in any workout yet.***
  🔑 ***Scope, settled and not to be re-opened:*** *library workouts and every plan built from them going forward. **Not** the 328 already-published plans — each holds its own static copy, so that is one manual edit per workout per plan for an audience of static-plan buyers.*
  - [ ] **The library pass itself.** *One edit per workout; every future application carries the links for free. This is the "his time once" case in its purest form.*
    ✅ ***The evidence this was waiting for arrived September 7–8, 2026, and it arrived from the manual version of itself.*** *Iván sent one `/w/` link per athlete by hand in the weekly feedback — no workout edits at all — and* **30 clicks across 9 codes put 13 of 38 athletes into the members area inside 48 hours, from a lifetime baseline of 2.** ***The link is doing the work, not the room.*** *Confirmed by timing rather than assumed: three separate anonymous clicks are followed by that athlete's first identified page view 4, 15 and 23 seconds later.* 🔑 **So the pasting pass is no longer a bet on a mechanism — the mechanism is measured, and the pass only changes who has to send it.** *Iván confirmed September 8 that he is starting it.* ⚠️ ***Do the pasting AFTER reading the snapshot already recorded in the Backlog item*** — *the two interventions land in the same reporting period and the divider only exists because it was written down first.*
  - [x] ✅ ~~**`recovery` has no EN/PT page**, so the three `/w/recuperacion*` codes fall back to the members home for those athletes.~~ **CLOSED September 5, 2026 — `/members/en/recovery/` and `/members/pt/recuperacao/` are live**, so all three `/w/recuperacion*` codes now resolve in every language and `/admin/enlaces/` should stop greying those badges. *It closed as a consequence of the Recuperación activa build, never as work of its own — which is what a pointer is for.* ⚠️ ***Confirm the badges when you next open `/admin/enlaces/`; nothing re-derives that.***
  - [ ] **Read `workout_link_clicks` at the September close.** *`anonymous_clicks` is the column that matters — a code with clicks and no athletes is a login problem, not a dead link, and it is the population no UTM scheme could ever have seen.*

- [ ] 🆕 **Build athlete-facing FAQs and help articles — reactively, from questions athletes actually ask.** *(Iván, September 3, 2026: "review the FAQs and create help articles when athletes start asking questions — this will happen.")*
  > ***There is no FAQ or help surface anywhere on the site today*** *— no `/faq/`, `/ayuda/`, `/help/` or `/preguntas/` in any language. Confirmed by search, not assumed.*
  > **The reactive framing is the right one and worth protecting:** *writing an FAQ from imagination produces answers to questions nobody asked. Writing one from the next ten real questions produces a page that removes ten future messages.* **Rule to hold: nothing goes on the page until a real athlete has asked it.**
  > *Two seeds already exist and they are different audiences — do not merge them:* `sales-playbook.md` **owns the pre-sale objection bank**, built from 13 real converted-athlete transcripts *(that is prospect-facing, and it belongs in sales copy, not a help centre)*; *the athlete-facing set — how do I read my plan, what happens if I miss a week, how do I re-test — does not exist anywhere.* *The Monday check-in is the natural collection point: the questions arrive there already.*
  > *Related: the members area "has eight cards and no first step" item. A help page is one plausible first step, which makes these two worth scoping together rather than separately.*

- [ ] **Improve the Portuguese offering — real demand exists, the site is on page 6 for it, and the one page that ranks is answering the wrong question.** *(Iván, September 2, 2026. Measured from `gsc_url_query`; query at `automation/analytics/queries/pt-hub-queries.sql`.)*
  > 🚨 ***Correction to what this item said an hour earlier, and the correction is the whole point.*** *It read "`/pt/planos/ironman/` at position 10.4 is the closest thing this site has to a page-one asset — one position is the difference between 30 impressions and clicks."* **That was wrong, and looking at the position without looking at the queries is how it went wrong.**
  > ***Every query the Ironman hub ranks for is about race distances, not about training plans:*** *`distancias 70.3` (12), `ironman 140.6` (10), `kilometros ironman`, `iron man distancia`, `distancias half ironman`, `70.3 distancias`, `iron man completo distancia` — all at positions 7–12.* **It ranks well because its title carries "70.3 e 140.6", so Google reads it as a reference page for how long the race is.** *Nobody in that query set wants a plan. Pushing it from position 10 to position 3 buys clicks from people who wanted a number and will leave with it — the zero clicks are not a ranking failure, they are the correct outcome for that intent.*

  **The commercial demand is on the marathon hub, and there the position is the problem:**

  | query | impressions | position |
  |---|---|---|
  | `plano de treino maratona` | 25 | 57.7 |
  | `plano de treinos maratona` | 24 | 67.0 |
  | `plano treino maratona` | 24 | 55.1 |
  | `planos de maratona` | 14 | 18.4 |

  ***That is ~73 impressions of exact purchase intent, and the site sits on page six for all of it.*** *Not close, but it is proof the demand exists in Portuguese and that Google already associates this page with it. `planos de maratona` at 18.4 is the one within reach.*

  *Also worth carrying: `/pt/blog/treinar-o-limiar-sem-tiras-de-lactato/` holds positions **9.5–12.2** for `limiar de lactato`, `lthr o que é`, `teste limiar de lactato` — informational, but topically exactly what a coach should own, and it is the only blog article in any language earning anything. And `/pt/all-access/` appears at position 11 for `trainingpeaks valor`, which is somebody pricing TrainingPeaks.*

  **Portuguese has 53 published plans against Spanish's 164** *(28 running, 9 cycling, 8 triathlon, 4 swimming, 4 duathlon)*, **and it is still the language where this site is most visible.** *Directions, none decided: work the marathon hub's title and content against the exact phrase `plano de treino maratona`; build out the thin PT sports; or translate the best-selling Spanish plans rather than writing new ones.* **No trigger and no owner — written down to come back to, not to start now.**

- [ ] ⏳ **DO NOT TOUCH UNTIL THE OCTOBER CLOSE — 98 articles, 79 impressions, zero clicks — decide whether the content engine keeps running at this cadence.** *(Measured September 2, 2026. **Trigger: re-read at the October close (early November), when the August and September cohorts have aged past four weeks. Nothing to do before then — the corpus is too young to judge and re-measuring earlier will only produce the same answer.** Source: `gsc_url_query`, every published blog URL joined against Search Console. Query kept at `automation/analytics/queries/blog-indexation.sql`.)*
  | age band | articles | with any impression |
  |---|---|---|
  | ≥4 weeks | 16 | **4** |
  | 2–4 weeks | 24 | **2** |
  | <2 weeks | 41 | **0** |
  | 1 day | 20 | **0** |

  ***The young half of the corpus proves nothing — 61 of 98 articles are under two weeks old and indexing takes longer than that.*** **The oldest cohort is the one that carries information: 16 articles at four weeks or more, and 12 of them have never been shown to a single person.** *Best article all-time is `/blog/plan-entrenamiento-maraton-valencia/` at 42 impressions, and it stopped being shown on Aug 14.* **Zero clicks across the entire blog, all-time, in every language.**
  > *This is not "the engine failed" and it is not a reason to stop it — it is the first honest measurement of what it has bought so far, and the number to re-read at the October close when the Aug/Sept cohorts have aged past four weeks. What it does settle: **distribution, not volume, is the constraint.** Publishing 21 more articles in a day does not change a corpus that has never been clicked.*
  > *Related and already known: the research agent's near-miss view reads GSC, and GSC is telling it almost nothing about this blog yet.*


- [ ] 🆕 **Generate `triaperformance-business-overview.md` §Interactive tools from `library.json`, or delete the table and point at that file.** *(Opened September 8, 2026, on the fourth consecutive instance.)* **That table is the repo's stated inventory of record for the members library and it has now missed four entries in a row** — `cyclistcore` (Aug 24), `strength` (Sep 2), `hombro` (Sep 3), `garmin` (Sep 8) — *and its count line was separately wrong about a fifth (runner core, listed as Spanish-only while the row beneath it said all three languages).* **Each miss was found the same way: by opening the table to add an unrelated row.** ⚠️ ***Three dated notes in that document already diagnose the pattern correctly and none of them changed it, so a fourth note is not the fix.*** *`site/_data/library.json` is machine-read by the build, rendered onto the three sales pages, and has been right every time; the table is hand-maintained prose that nothing reads at build time.* **Two honest options and no third: render the table from `library.json` in the build, or delete it and make `library.json` the named inventory of record.** *`artifact-publish-runbook.md`'s Published-artifacts table is the same defect and should go the same way in the same pass.*

- [ ] **Build, translate and publish the carb loading calculator.** *(Named by Iván, September 2, 2026.)* Same three-language rule and same process. Note it is the one tool on the list with a content dependency: the carbohydrate dosing it outputs has to agree with `methodology.md` §fueling, which is also the subject of an open verification item in the content group.


- [ ] **Public lead-magnet landing page with email capture** — the 3 Spanish lead magnets each have an 8-email nurture sequence attached, but since the migration there is no public page to capture an email on: bare `/guias/` 404s, `/members/guias/` is gated + `noindex`, and the only public path is a direct PDF link that captures nothing. Any external campaign (GBP posts, IG bio, TP listings) currently has nowhere to send cold traffic that starts a sequence. (Found Aug 2, 2026; Iván taking this next.)
  ⚠️ ***Sharpened September 8, 2026, and it now has a second reason to exist that does not depend on the lead magnets at all.*** *`Recursos` in the nav is `menuOnly` — a dropdown parent with no page behind it — and the pace converter took it to six rows in every language (Blog, the converter, the zones hub, its three sport pages). **There are now two free public tools and a blog sitting behind a menu label that leads nowhere**, which is the cheapest cold-traffic destination this site owns and it cannot be linked to.* **Build `/recursos/` (+ `/en/resources/`, `/pt/recursos/`) as the one hub: the two calculators, the blog, and the guides with the email capture on them.** *That is a single page per language that closes this item and gives every future calculator a home to be listed on, rather than a fifth nav row.*
  🔑 ***And it settles the shape of the calculator library, which was Iván's actual question on September 8.*** *He asked whether the finish-time, altitude-equivalent and heat-equivalent calculators he wants next should live as one multi-tool page or as separate ones.* **Separate URLs, listed on `/recursos/` — same reasoning as `zones-calculator-brief.md` §4: `calculadora ritmo altitud` and `conversor min km millas` are different searches with different intent, and one page can only win one head term.** *The line between "one tool" and "two tools" is intent, not subject matter: the unit converter and the treadmill speed shipped together because they are one question — the same pace written four ways — while a heat-adjusted pace is a different question that happens to use the same units.*
- [ ] **Win-back sequence for churned customers** — now that `CHURNED_CUSTOMER` is set automatically, the audience is finally queryable. They're the warmest re-activation list there is: they paid, trained on the plans, and left for a reason. Nothing targets them today. Trigger: after the legacy-contact re-engagement blast (NEXT #3), which will have taught us what re-activation copy does. (Aug 6, 2026)
- [ ] **Broader `is_published` audit — PART 1 DONE Aug 12, 2026; the TrainingPeaks crawl is the remaining half.** *Findings and method: `training-plans-analysis.md` §11.*
  **(a) The two CSVs agree. Closed.** `training_plans_inventory.csv` and `plan_performance.csv` both show 303 published, zero disagreements, zero rows missing from the inventory. The Aug 10 correction held. *(The 15 extra rows in `plan_performance.csv` are retired plans kept so lifetime sales stay complete — 23 units, $592 — all `in_inventory=FALSE`, none published. Not a defect; noted so it isn't re-raised.)*
  **(b) Root cause found for why four staleness cases went undetected: `check-plan-links.py` filtered on `is_published != TRUE` and skipped the rest.** The flag was the checker's own input, so it could only ever confirm the TRUE set — and three of the four known cases were FALSE-but-live. **This is the warning in this very item ("do not verify the inventory against a file derived from the inventory") existing in code, where the derivation was the filter on line one.** Fixed: `--audit` mode checks unpublished plans too, reports both directions, writes a separate report and deliberately leaves `plan_link_status.json` untouched so an audit run cannot alter what the build believes.
  **(c) Five plans are provably mis-flagged, found without any network call.** `plan_sales.csv` is independent of the inventory — transactions come from TrainingPeaks payouts — so *a plan cannot have sold without having been published at the time of the sale.* Five PT triathlon plans are `FALSE` with sales as recent as **2026-07-30**, thirteen days ago: `567302`, `567303`, `567304`, `567305`, `567560`. A sixth, `567561`, has no sales but completes the same ladder and sits in the same state; `567564` is its Spanish sibling. **These are the "PT triathlon siblings" named above as a confirmed staleness case — identified previously and never actually flipped.** Publishing them takes PT from 29 to 35 (+21%) and restores a complete beginner ladder (Sprint 12wk → Olympic 16wk → Middle 18wk → Full 21wk) in the thinnest catalogue. *Iván to confirm in TrainingPeaks and flip the flag in **both** CSVs.*
  **(c2) Resolved same day, and bigger than expected.** Iván filtered his own spreadsheet on unpublished, checked TrainingPeaks, and confirmed **24 plans were live all along** — not five. Flipped in both CSVs. **Catalogue 303 → 327; Portuguese 29 → 52, up 79%.** *The five sales-derived suspects were a subset of a much larger set, which is the honest lesson about the method: the sales join gives certainty about the plans that have sold and says nothing at all about the ones that haven't. It found 5 of 24. Iván's "filter the column and look" found all of them in a couple of minutes. **A clever test that covers 20% is not better than a dull one that covers 100%** — it is a way of narrowing the list, not of replacing the check.*
  **(d) The crawl ran twice and found nothing that wasn't already known.** Run 1 (pre-flip): **78 of 78** unpublished plans returned HTTP 200. Run 2 (post-flip): **54 of 54.** Zero dead, zero unresolved, both times. **Run 2's 54 are exactly the 51 race plans Iván deleted plus the 3 rows already flagged for a decision** — a perfect overlap with what he found by filtering a column, and no new information in either direction.
  ***The reason is sharper than "TrainingPeaks keeps old pages up", and I overstated it in the first version of this note.*** *I do not know whether those race plans are still purchasable on TrainingPeaks, and it does not matter, because* **`is_published` in this CSV is not purely a TrainingPeaks fact — it is partly a business decision about what the website should list.** *The race-stamped plans may well be perfectly alive on TP; the decision is that Triaperformance no longer sells them.* **No crawler can answer a question that is half a decision.** *That is the real ceiling on this approach, and it is not fixable by crawling harder.*
  **What this leaves.** `is_published` has exactly two reliable sources: the **sales join** (certain, but only speaks about plans that have sold — it found 5 of 24) and **Iván looking at TrainingPeaks and deciding** (slow, total, and the thing that actually resolved it, twice). `--audit` keeps real value in the *other* direction — `TRUE`-but-404 is a genuine dead link a customer can hit, and that does discriminate. **Do not re-run it to find unpublished-but-live plans; it structurally cannot distinguish them.**
  *`data/plan_publish_audit.json` holds run 2 and is stale: of its 54 rows, 51 are deleted, 1 published, 2 removed. Regenerate rather than read.*
  ***All 52 race-dated plans were unpublished and every one was for a 2026 race already run — correct, not a finding. 51 of them were deleted from the inventory later the same day*** (see `training-plans-analysis.md` §3).
  *Method note worth keeping: run the sales join before the crawl. It is instant, needs no network, and yields near-certainty; a live URL only proves a page resolves, a sale proves money changed hands.*

  *Original entry, retained for the reasoning that fired the trigger:* The flag has now shown confirmed staleness in both directions **four** times (Lima, PT triathlon siblings, `612978`, and the two EN Ironman plans), and the stated trigger was "when the EN Ironman check turns up a third case." It did. One systematic pass, before more facets get built on top of a flag that is wrong roughly monthly: reconcile every row in `data/training_plans_inventory.csv` against what TrainingPeaks actually serves, and reconcile the `is_published` column between the inventory and `data/plan_performance.csv` — the second copy has already drifted once. **Do not verify the inventory against a file derived from the inventory**; that only ever confirms itself, which is exactly how the Rio plan survived the Aug 6 sweep. Detail: `race-page-data-schema.md`.
- [ ] **`plan-tracker-bigquery` still on the decommissioned Python 3.9 runtime** — Cloud Function, 2nd gen; source archived at `automation/pixel-tracker/main.py`. Migrate to 3.12. 🚨 ***Now carries a second, larger job, added September 5, 2026: harden the endpoint at collection time.*** *`main.py` writes a BigQuery row for any request whatsoever — `plan_id` defaults to the literal `'not_set'` — and four flood incidents have exploited exactly that (2025-11-24 ×2, 2026-03-14, 2026-08-31; three of the four running one pinned `okhttp/3.14.9`). The August flood put 1,008 junk rows into the warehouse **the day before close #1 read it**. The fix is three lines: if `plan_id` is not well-formed, serve the GIF and write nothing.* **The read-side fix is already live** *(`schema_plan_views.sql`), so this is defence in depth rather than the only guard — but the read-side fix does not stop the endpoint being billable, and F8 is now a data-integrity finding rather than a cost one.* ⚠️ ***Blocked on a contradiction that must be settled before anyone edits the file:*** *`automation/pixel-tracker/main.py` line 3 says "Runtime: Python 3.12 (migrated off decommissioned 3.9 — July 2026)", while this item says 3.9 and the archive records Iván confirming it open on Aug 14. One is false. **If the deployed source is not the repo copy, editing the repo copy changes nothing and produces a note saying it is fixed** — the §18 trap, logged five times.* **Confirm the live runtime and diff the deployed source against the repo copy first.* (The `referrer`-column question is permanently closed, not open — see `ai-infrastructure-documentation.md` §10 for why it can never be fixed via this embed method.)
- [ ] **MCP tool integrations — Google Workspace, and Twenty via the community MCP server.** Planned, not started. Twenty's native MCP server is a Cloud Pro feature; self-hosted needs the separately deployed open-source one.
- [ ] **Hermes not threaded into the live website workflow** (drafting content, updating pages, weekly SEO checks). The infrastructure is ready; day-to-day usage isn't set up. Keep command-approval on "ask", not "off", for anything touching the live site. The Monday coaching check-in is the one existing exception.

---

### Opened by the Gemini model migration (August 17, 2026)

*Technical record: `ai-infrastructure-documentation.md` §33 and its September 4, 2026 addendum.*

**Hermes and the research agent run `gemini-3.8-flash`** (`gemini-3.7-flash` from August 17 to September 4, 2026). **The writer, the translator and the n8n intake briefing use `gemini-3.1-pro-preview` — Iván's decision, September 4, 2026, and it is settled rather than pending.** *Open from August 17 across three Flash releases as "draft both and read the Spanish"; closed by a call instead of by a test.* ⚠️ **Do not re-raise it on the next Flash release.** *What would legitimately re-open it: Google publishing multilingual or prose evaluation for the Flash line, or `gemini-3.1-pro-preview` ceasing to answer. Closing note in `open-loops-archive.md`, and in `writer_agent.py`'s docstring, which is where a session actually looks.*

*Two lines corrected here September 4, 2026, both stale rather than wrong: this paragraph read "nothing is deployed yet" (deployed August 17–24, confirmed by §14's Aug 24 note) and "all five items below" (one remained open). Neither recorded a belief that turned out false, so neither is kept struck through.*

- [ ] **Watch the first deploy after that push.** *(Opened September 4, 2026.)* The 6am run should print `hermes config.yaml unchanged` — **not** `updated and containers restarted`. *A restart on the first run means the repo copy and the live file already differ, i.e. the `scp` picked up something stale or the dashboard rewrote the file in between; the backup it takes first is what makes that recoverable.* **`cmp` is byte-level, so if the dashboard ever rewrites `config.yaml` with reordered keys the two will differ every day and Hermes will restart every morning.** *If that starts happening, the fix is to compare parsed content rather than bytes — not to remove the sync.*

- [ ] **Cron pin drift check — verify it prints, then leave it alone.** *(Opened September 4, 2026.)* `deploy-website.sh` now reports every Hermes cron job's pin against `model.default` each morning. **Deliberately a report and not a sync**: `~/.hermes/cron/jobs.json` is rewritten by the dashboard whenever a job is created or edited, so tracking it would fight the UI daily for no gain. ***What actually hurt was never the file being untracked — it was a pin going stale silently.*** *An agent-mode job pinned to a superseded model fails **closed**, which is the safe direction and also the invisible one: on August 24 that cost a Monday coaching check-in, announced as one line in Telegram among other traffic.* **It cannot fail the deploy** — instrumentation must not be able to kill the thing it measures (§33's `log_usage()` rule). *`no_agent` jobs are reported as immune rather than as unpinned, because they are.*

- [ ] **Check `model_usage` for a thinking-token regression on 3.8, after two or three Mondays.** *(Opened September 4, 2026.)* 3.8 is priced identically to 3.7 on both sides, **but it defaults to reasoning level "medium" and thinking bills at the output rate** — so the same job can cost more at the same rate. *This is the first real question the August 30 `model_usage` build exists to answer, and the column that answers it (`thinking_tokens`, stored separately because `in + out ≠ total` on 3.x) was added for exactly this.* Group by model where `caller = 'research'` and compare `thinking_tokens / output_tokens` across the 3.7 and 3.8 rows. **Needs a few real runs before it can say anything** — the research agent runs Mondays only, one call.

---

### Small slot: CoachMatch greeting name (opened September 4, 2026)

**The first WhatsApp message was addressing leads as "Hola Paula Maria Gonzalez".** The parser popped only the last token as the surname, so every given name past the first stayed in `firstname` — correct on a two-token name, wrong on the Latin two-surname norm. Iván had been catching them by hand before sending. Fixed to *first non-initial token = `firstname`*, plus title-casing of SHOUTED/lowercase entries and a guard for the single-token name that used to produce an **empty** `firstname` and a bare "Hola ". Full record: `ai-infrastructure-documentation.md` §8, dated note.

**Part 1 (the lead workflow) is DONE — pasted, tested and confirmed working by Iván, September 4, 2026.** Two things opened while writing the backfill, both still open:


- [ ] **Two stale test records sitting in the CRM at `MESSAGE_SENT`** — `coach+zone-en@` and `coach+zone-pt@`, `leadSource: ZONE_CALCULATOR`, left over from the August zone-magnet testing. **They generate no daily noise** *(`twenty_followup_check.py` skips a lead with no phone before it builds anything)*, **so nothing has ever pointed at them** — but they are two live-looking leads in any funnel count taken off this table. *Excluded from the backfill via `--exclude` rather than renamed. Decide whether to delete them or move them to a terminal status.* **Found by reading a backfill dry run, which is the only thing that has ever listed the whole table row by row.**

***The transferable half is where the fix had to go.*** There are four greetings on a CoachMatch lead and only one of them is the one Iván sees. WhatsApp 1 and email 1 read the parse; emails 2 and 3 read `person.name.firstName` from Twenty two and three days later. **Patching the message he noticed would have left the two he doesn't watch still wrong** — the value had to be fixed where it is stored, not where it was seen.

## NEXT (queued, in order, each with its trigger)


### Opened by the first monthly close (September 1, 2026)

**The decision close #1 produced — exactly one, per the runbook.**


**September focus, Iván's call: Private, referrals, and making All-Access worth sharing. Paid acquisition starts this month.**

- [ ] 🚨 **Paid acquisition stops being $0 — small Instagram test budget.** *(Iván, September 1, 2026.)* **The `marketing` P&L line has carried a deliberate zero with the standing note that a visible zero makes the moment it changes a decision rather than a drift. This is that moment, and it is being recorded as one.** *The project guardrail — "don't expand paid acquisition before the CRM catches leads reliably" — is **satisfied**: Twenty is live and the contact-form path was re-tested end to end on Sept 1.*
  ⚠️ ***Two prerequisites, both about a day, both before the first dollar:*** *(1)* **deploy the named conversion events** *(written Sept 1, not yet on the box — `select_plan` fired **zero** times in August and the zones calculator produced two real leads while firing no GA4 event at all, so ad-driven conversion is currently unattributable);* ~~*(2)* **fix the UTM tags first** *— untagged ad traffic lands in `Other: (not set)`, which is already 44.6% of sessions, i.e. spending into the one bucket that cannot be read.*~~ ***Struck September 2, 2026 — the premise was false and the correction is the useful part.*** *The 44.6% was **not** a tagging failure. Every UTM-tagged link in the property was being read correctly the whole time; the sync was mislabelling **direct** traffic, and the fix was two `NULLIF`s. **The tags were never broken, so there is nothing here to fix before spending** — a properly tagged ad click would always have been attributed.* ⚠️ **Prerequisite (1) is untouched and is the one that actually blocks the first dollar:** *`select_plan` fired zero times in August, so an ad's conversion is still unmeasurable no matter how the click is tagged.* 🚨 ***A real prerequisite that this diagnosis surfaced instead:*** **Direct is 46.6% of external sessions (55 of 118) and is now the largest named channel** — *on a site with no brand search to speak of, that is mostly untagged links Iván pastes himself, into WhatsApp above all, plus whatever self-traffic the cookie-bound rules miss.* **Tagging the links he sends by hand is the higher-return version of "fix the UTM tags", and it is a different job from tagging ads.**


- [ ] **Search demand evidence for the race-pages branch — attach to NEXT #1, do not open a second item.** *August GSC: **PT marathon ≈ 94 impressions** across five query variants at positions 16–66, and **Valencia marathon ≈ 38 impressions** across five ES queries at 36–66, with `/en/blog/valencia-marathon-training-plan/` already pulling 5 sessions.* **`race-landing-pages-longlist.md` concluded from the catalogue alone that PT is a marathon-only play; search demand independently confirms it.** *Valencia is a December race with live demand and nothing ranking.* ⚠️ **Blocked by WIP** *— the referral branch holds the big slot.*

- [ ] **No backups on the VPS.** *(Recorded in close #1's cost inventory as $0 — a risk, not a saving.)* *Twenty holds every customer record, `analytics-postgres` holds the whole warehouse, and n8n holds every workflow. Not a close item; logged so it stops being an unwritten worry.*


### Opened by the pixel plan_id diagnosis (September 5, 2026)

- [ ] **`audit-runtime-paths.sh` cannot see database objects, and that is how this one hid.** *`plan_views_clean` lived only on the box from July until September 5, outside the repo, with five defects in it — including a claim in NEXT #2 that the runtime contradicted. §18's audit exists precisely to catch "the runtime is not the repo copy", and it could never have caught this: it walks **files** — cron lines, compose contexts, dispatchers, symlinks — and a Postgres view is not a file.* 🚨 ***Every §18 instance so far has been code on disk, so the control was built to the shape of the last five failures and is blind to this one.*** **A check scoped to what broke last time only catches what broke last time — this repo's own words, arriving inside the check written to prevent it.** *The fix is small: dump `pg_get_viewdef` for every view in `analytics-postgres` and diff against the repo's `schema_*.sql`, failing on anything defined only on the box. Same for the `content` and `members` databases, which have never been audited either.* ⚠️ **Scope it before building — `schema_analytics.sql` declares 4 tables and 5 views and nobody has checked that the box matches it.**

### Opened by the first observed AI-assistant lead (September 3, 2026)

**The event.** A Bogotá lead, Jenyfer, opened WhatsApp at 08:38 with *"te busqué por IA y me apareció tu nombre… Eres el número 1."* The screenshot she sent is a ChatGPT local-search answer that ranks Triaperformance **#1 of several Bogotá options**, and the two sources it visibly used are the **Google Business Profile card** (5.0, "Programa de acondicionamiento físico", Indicaciones / Sitio web / Llamar) and the **TrainingPeaks coach profile**, cited by name for the substance — Level 2, the sports covered, 1:1 + fuerza + nutrición.

🚨 ***The finding is not the ranking. It is that `triaperformance.com` contributed nothing visible to that answer, and the citation the athlete was shown points at TrainingPeaks*** *— the channel that costs 20% instead of 3.5%. An AI-sourced lead who follows the citation instead of writing to WhatsApp lands on CoachMatch and costs $29.80/month forever. Same failure the referral brief §4 documents for the forwardable blurb, arriving through a different door.*


- [ ] **Independent evidence the AI channel reads TrainingPeaks, not the site — attach here, do not open a second item.** *(Found September 5, 2026, in the pixel data, while diagnosing something else.)* **GPTBot has swept 200–300 of the TrainingPeaks plan listings roughly monthly since June 2025** *— versions 1.2 → 1.4, from 74.7.x and 20.171.x, one hit per plan.* 🚨 ***This is the mechanism behind Jenyfer's screenshot citing the TrainingPeaks coach profile, and it confirms this section's core worry from the other direction:*** *OpenAI has fifteen months of the 20% channel indexed and comparatively little of `triaperformance.com`.* *(It was already excluded from `plan_views_clean` — by accident until September 5, because its UA carries `openai.com/gptbot` in lowercase and the old filter was case-sensitive. Now on purpose.)*

- [ ] **Ask every inbound WhatsApp lead how they found you — one line, before the five questions.** *Jenyfer volunteered it; nobody else will.* **Direct is 46.6% of external sessions and WhatsApp inbound carries no source at all, so the honest answer to "how many AI leads am I getting" is currently "unknown", not "one".** *One line appended to `sales-playbook.md` A2 is the whole build.*

- [ ] **Decide whether the AI channel changes the race-pages ordering (NEXT #1).** *The geographic pin is the ceiling here: one GBP is one city, and he sells a remote service to all of LatAm — an athlete in Lima or Buenos Aires asking the identical question gets their own local coaches. **Race pages are the asset that answers the other query shape** ("plan para el maratón de Valencia"), which is not geographically pinned to Bogotá. The referral branch holds the WIP slot but its own note says the slot is released — it is waiting on the calendar, not on work.* **Cheaper first move, and it is one file:** *`areaServed` is absent from `site/_includes/partials/person-schema.njk`, which was built in August for exactly this purpose. The schema says what he knows and never says where he will coach.*


---

**UNPLACED — ordering is Iván's call, and it may belong above everything below.** *(Opened Aug 12, 2026 from `tenure-analysis.md`.)*


- [ ] **Diagnose the retention drop before spending the next quarter on acquisition.** **The book has shrunk in 5 of the last 6 months — 53 active at end of February, 37 today; 26 churns since April 1 against 13 signups.** And it is not only slower acquisition: retention at twelve months by signup cohort is **2024-H2 73% → 2025-H1 30% → 2025-H2 17%**, measured at fixed horizons so young cohorts aren't penalised. Full analysis, caveats and method: `tenure-analysis.md` §3.
  **Why this is placed above the growth items rather than beside them:** #1 (race pages), #3 (re-engagement blast) and #10 (referrals) all fill a bucket. **This says the bucket is leaking faster than any of them fills it** — at the current rate the book loses ~4 athletes a month, which is more than the referral program could plausibly add. *An acquisition push into a retention problem buys you the same revenue twice at twice the cost.*
  ***Cause: answered same day, and the diagnosis half of this item is closed.*** *Iván took a COO role at a SaaS startup in January 2026, which cut deliverability; the wave starts in April, one training block later — the lag you'd expect, since an athlete quits when the block they already paid for ends, not the week service thins. **Not the price migration.** He is now full-time on the business. Detail: `tenure-analysis.md` §3.*
  ***And the headline overstates it, which he was right to push on.*** *Counting revenue instead of logos: **−30% heads but −21% MRR**, because the athletes leaving are on $75–99 and the ones arriving are on $149. Average rate $93 → $105, and **revenue per coaching hour is up 13%** — which for a business constrained by his calendar is the number that matters most. **Where it stops working: break-even is 3.4 signups/month at $149 against a recent run rate of 2–3.** The gap is one signup a month, and the mix shift is self-limiting — it works only while $99 athletes are being replaced, and ends when the book is all $149.*
  **What stays open is the measurement, not the diagnosis:** `monthly_rate` (§3's revenue view is a model, not a measurement), **one row per billing period** (the name-derived `athlete_id` merges them automatically), and **a churn reason** — *Iván's point that some churn is an athlete completing their goal and resting is right, and nothing in the data can currently see it.* **Folded into the monthly-close item above rather than run as a separate export.**
  **What remains a real decision: whether the next quarter goes to retention or acquisition.** Trigger: two monthly closes, i.e. end of September.

- [ ] **#1 · Race-specific landing pages** — the next big branch *after* the members-area i18n branch in NOW. *(Aug 10, 2026: it was "the natural next big branch" this morning and was passed over deliberately. Reasoning in NOW — race pages grow the top of the funnel, the members area is the thing already sold and not delivered. Nothing about this item changed; it is still fully researched and ungated.)* Planning done July 30, 2026: race longlist matched against `training_plans_inventory.csv` (only (distance × language) pairs with ≥3 matching plans qualify — kills EN full-distance and PT 70.3/full entirely, leaves PT as a marathon-only play), per-race data schema proposed (`race-page-data-schema.md` — plans are joined live at render time by distance/language/weeks-countdown, never stored as a static plan_id per race, so pages stay evergreen), 12 Tier 1 races fully researched and gathered into `data/races.csv`, and a page content outline written (`race-page-content-outline.md`). Full longlist + tiering + search-window calendar in `race-landing-pages-longlist.md`. ~~Trigger: plan template deployed live~~ — **satisfied August 6, 2026.** Nothing gates this build. *Iván's review corrected the inventory read: EN full-distance Ironman actually qualifies (3 plans, not 1 — **confirmed Aug 10, 2026: both flags were stale, now `TRUE`, so the EN full-distance race pages are unblocked with a complete difficulty ladder**), and the plan-matching join now also facets on intensity type (pace/HR/power) and a strength/gym add-on. California International Marathon (Dec 6, 2026) added to `data/races.csv` as a 3-language test case, to trial the full template pattern on one real race before the rest of the batch.*
- [ ] **#2 · TP listing rewrites experiment — LIVE, all 21 listings pasted July 29–30, 2026. Waiting on data.** EN 9 (`en-listing-rewrites-2026-07.md`) + ES 8 / PT 4 (`es-pt-listing-rewrites-2026-07.md`); control = the 3 untouched EN sellers (413817, 414883, 602981, 2.2% conv). **Checkpoints: peek ~Aug 30 (directional), real read ~Oct 27 (90 days)** — queries and decision rule in the Measurement section of `es-pt-listing-rewrites-2026-07.md`. Also watch GA4 for source=trainingpeaks sessions. ~~Fixed along the way: `plan_views_clean` on the VPS now excludes all 3 personal IPs, matching BigQuery.~~ 🚨 ***False, and struck September 5, 2026.*** *The live view excluded **one** — `181.62.20.182`. `190.193.3.211` and `181.86.140.211` were never added, so two of Iván's own IPs counted as marketplace demand for six weeks.* **Same shape as the August 12 auth-service entry in §18 — the record said "fixed" while the runtime disagreed — and the same cause: the view lived only on the box, so nothing could diff the claim against reality.** *Now fixed for real, in `automation/analytics/schema_plan_views.sql`, which is also the fix for the cause. Data impact was small (214 rows in sixteen months, 3 in August); the false record was not.* ⚠️ **This experiment's own measurement queries read the corrected view now, so the Oct 27 read is against different numbers than the Aug 30 peek.**
- [ ] **#3 · Legacy contact import (2,250 contacts) + re-engagement blast.** Scope decided July 29, 2026: import all, not active-only. ~~Trigger: storefront ready to land the blast on.~~ **Satisfied August 6, 2026 — nothing gates this.** *(Renamed Aug 8, 2026, on Iván's correction. This was called "the HubSpot migration", which framed it as blocked on a system that is switched off and kept re-raising "is HubSpot really sunset?" across the docs. **It is not a HubSpot task.** The contacts are already exported and sit on Iván's machine and in Google Drive — not in this repo, and never will be, since it's PII. The only open question is a business one: **import the legacy contacts into Twenty, yes or no**, and if yes what the message says. A cold three-year-old list is worth an honest look before it's worth an email.)*
  ***Export read and suppression preserved, September 8, 2026, ahead of deleting the HubSpot portal.*** *The full-property export is* **2,250 contacts / 2,230 unique addresses**, *and the earlier count is simply superseded — the list kept growing, and Iván's correction is that* **part of the newer contacts are already in Twenty**, *so the export overlaps the live CRM and is not a clean incremental audience.* 🚨 ***The thing that had to be rescued before deletion is the opt-out and bounce state, which nothing else holds:*** **131 addresses** *— 68 unsubscribed-from-all, 36 hard bounces, 22 marketing opt-outs, 2 one-to-one, 2 customer-service, 1 invalid — seeded into* `email_suppression` *with* `source = 'hubspot-legacy-2026-09-08'` *and a reason per signal.* **`email_suppression` had never been seeded from anything: the only writer was the `/api/unsubscribe` endpoint, live since September 6, so before this the suppression list knew about two days of history and nothing before it.** *A blast run without it would have emailed 131 people who had already said no, with no record that they ever did.* ⚠️ **Mailable after suppression: 2,099** *— before de-duplicating against Twenty, which is the next thing this item needs and is not done.* *The seed file carries addresses and stays out of this repo, per the standing PII rule.*
- [ ] **#4 · Workspace DKIM — verify it is actually signing, then decide DMARC.** *(Opened September 8, 2026, as the HubSpot DNS cleanup closed. Not a HubSpot item; it is the hole that cleanup left.)* The two `hs*-_domainkey` CNAMEs were the zone's **only** DKIM records, and Workspace DKIM had never been configured — so between deleting them and adding `google._domainkey`, the domain signed nothing. **The key is now in the zone (2048-bit, verified in the Sept 8 10:35 export).** ⚠️ ***The record existing is not the same as Google signing with it*** — that needs *Start authentication* clicked in Workspace Admin, and a TXT record with the button unpressed looks identical in a zone export. **Definition of done: one mail sent through the real n8n + Gmail SMTP path (not the Gmail web UI, which is a different sender) landing in a Gmail inbox with `dkim=pass header.d=triaperformance.com` in Show original.** *Gates the #3 blast — unsigned cold mail to ~2,000 three-year-old contacts is a spam-folder outcome whatever the copy says.* **`_dmarc` stays `p=none` until DKIM has signed for a couple of weeks**; moving it to `quarantine` is a separate decision and not this item. Detail: `deploy-runbook.md` §1.

  ***Status, September 8, 2026.*** *First* Start authentication *press returned Google's red "wait up to 48 hours" error; the retry was accepted the same day.* ⚠️ **That message is ambiguous and usually is NOT about waiting** — *its common real causes are a malformed or UI-mangled TXT value, or GoDaddy appending the domain to a host that already carries it (`google._domainkey.triaperformance.com.triaperformance.com`).* ✅ ***Both were ruled out from the zone export before waiting on it*** *— the record is `google._domainkey`, `v=DKIM1; k=rsa;`, 2048-bit, correctly split into two quoted strings — so the wait was genuine propagation, which is the benign reading of that error and the only one that resolves on its own.* **Confirmed live the same day: `google._domainkey.triaperformance.com` resolves publicly (Google DoH, `Status: 0`, TTL 3600).** *The trap to avoid while waiting is regenerating the key, which restarts the clock.*

  🚦 ***What is still NOT proven, and it is the only part that matters for #3:*** *a propagated key and an accepted* Start authentication *both stop short of showing that outbound mail is actually signed.* **Definition of done is unchanged: one message through the real n8n + Gmail SMTP path, opened in Gmail, Show original, showing `dkim=pass header.d=triaperformance.com`.** *The Gmail web UI is a different sender and does not test the path the blast will use.* *Same shape as this repo's recurring defect — every intermediate check reported success and none of them looked at the artifact.*
- [ ] **#5 · Plan-completion nurture — re-target buyers when their plan ends.** *(Iván, Aug 6, 2026.)* Today a plan buyer is effectively a one-time customer: **6.4% ever buy again**, and those who do come back after a **median of 72 days** — roughly the length of the block they just finished. Nobody is asked what comes next. The mechanism: on purchase we know the plan's length, so `purchase_date + weeks - 1 week` is the moment to make contact. One rule covers most of the catalogue — 8, 12 and 16 weeks account for 168 of 237 purchases. Message should be the honest coach's question (how did it go, what's the next goal) leading to the logical next plan or All-Access, not a discount blast. **Sizing, from `training-plans-analysis.md` §10:** only 11 buyers are currently mid-plan or about to finish, but **74 finished within the last year and were never asked** — a one-off back-catalogue send, then an evergreen trigger on every future buyer. Trigger: after the HubSpot migration, which supplies the audience.
  ***A second, cheaper audience mechanism — Iván, August 31, 2026. PARKED, logged so it stops being re-asked.*** *`planPurchasedId` on Person exists to hold the **TrainingPeaks marketplace plan id** (the same ids in `data/training_plans_inventory.csv`). Nothing writes it today.* **The idea: create a Person on every plan sale** *— name, last name, email* ***when the buyer leaves one***, *plus the plan id and the purchase date —* **which makes the nurture audience accrue automatically instead of waiting on a migration.** *Then "you bought an 8-week swimming plan" becomes a query rather than a list-building exercise, and the plan id resolves the plan's name and length from the inventory, so no name needs storing.*
  ⚠️ **The reason it is parked and not queued: the email is optional.** *TrainingPeaks does not always pass a buyer address, so this audience is a subset of unknown size —* ***measure what share of recent sales carry an email before designing anything on top of it.*** *Also note the standing rule this bumps against: customer names and emails never enter the repo. Twenty is not the repo, so a Person record is fine; an export of it is not.*
- [ ] **#6 · Weekly GBP-review → IG testimonial drip — already running manually at ~1/week.** Supply is fine (~~45 reviews, 36 repurposed, 9 unused ≈ 9 weeks~~ **46 reviews, 38 repurposed, 8 unused ≈ 8 weeks** — *corrected Aug 14, 2026; owner `social-proof-and-reviews.md` §Live metrics*, plus a recyclable back catalog and ~14 asks due next cycle). The open item is **automating the drip** so it stops costing weekly manual time. Trigger: any time; first recurring content job.
- [ ] **#7 · EN/PT review generation** (playbook in `social-proof-and-reviews.md`) — every EN/PT athlete gets asked until EN ≥10, PT ≥5. Trigger: ongoing, milestone-triggered asks.
- [ ] **#8 · B-lite premium bundle — now scoped as "+$50 testing-and-zones consultation" on the top 20 sellers.** ~~Trigger: storefront live.~~ **Satisfied August 6, 2026 — nothing gates this.** *(Sharpened Aug 11, 2026, Iván. The call was "20-min onboarding call", which named a duration rather than a deliverable; it is now *"I help you run your test and set your zones correctly"* — a defined outcome aimed at the commonest failure of a self-served plan, an athlete running a zone-based block off guessed zones. Scope fixed at the **top 20** sellers as a test, resolving a top-10/top-sellers contradiction that had been sitting inside `growth-roadmap.md` itself.)* **Its second argument is delegation:** the call is protocol work off `methodology.md`, not Iván's judgement, which makes it the natural first paid task for a junior coach and a bounded trial run for the **coach hire** item in LATER — a look at someone's athlete-facing quality before handing them a 1:1 athlete. Judge it on that as much as on the $50. Design detail and full reasoning: `growth-roadmap.md` §Storefront, standing decision 3.
- [ ] **#9 · Affiliate program applications** (Amazon first). Trigger: first gear article scheduled — the 180-day/3-sale clock starts at *approval*, so don't apply before traffic exists.
- [ ] **#15 · Canonical exercise library for the members area — every decision made, nothing built.** *(Opened and fully specified September 8, 2026; **parked the same day by Iván (D5) so race pages keep the WIP slot**.)* **Home doc: `exercise-library-decisions.md`**, which owns D1–D5 and the reasoning behind each. Drafted cues: `exercise-library-canonical-cues.md`. Inventory: `data/members_exercises.csv` + `data/members_exercises_unique.csv`, generated by `automation/extract-members-exercises.js`.
  **The problem, measured rather than asserted:** the nine exercise artifacts hold **253 exercises in Spanish and the same in each of EN and PT** (parity verified, nothing missing) — **759 name+cue copies for 194 real exercises** *(derived September 8, 2026 by `automation/apply-exercise-merge-map.js`; re-run it rather than quoting this)*. *Editing one cue correctly today means editing up to 18 places across 15 files, which is why `core/` ended up calling cat-cow "Gato-vaca" while five other artifacts called it "Gato-camello".*
  **Design, settled:** `site/_data/exercises.json` — **same pattern and same precedent as `site/_data/library.json`**, whose own header records the identical failure on the marketing side. The library owns identity, words and media (`name`, `cue`, `tag`, `video`, three languages); **the artifact owns only the dose** (`sets`, `reps`, `rest`, `secs`) plus an optional one-line `note`. ⚠️ **A page may never override `cue`** — if a placement needs different words for the movement itself, it is a different exercise with its own id. *That single rule is what makes the library hold.*
  **Translations are carried, never regenerated** — the EN/PT text moves across from whichever artifact's cue wins. *They were confirmed correct on Aug 21; re-translating would quietly discard reviewed work.*
  **Verification is the gate, not a nicety:** build `_site` before and after, diff the rendered exercise text page by page, and **every difference must be a rename on the signed-off list.** Pattern exists in `tests/mobility-matrix.js`.
  ***What is already usable while this is parked:*** **the dedup decisions do not expire.** *The filming list is* **193 clips** *— 194 canonical exercises, of which bird dog's two entries share one. 33 of them appear in more than one artifact and cover 86 placements; Gato-camello alone covers six.* **One clip serves every placement.** ⚠️ *But the strength engine still has no `video` field at all (`activation-tool.js` has one and renders it; `strength-tool.js` does not), so **55 of the 194 canonical exercises have nowhere to put a video until that branch runs** — aquiles, rodillas, hombro, core-ciclista, core-corredor.*
  🔑 ***Scope addition, Iván's question September 8, 2026, and it is the half that makes the rest durable: the library must be consulted when the NEXT artifact is authored, or artifact #10 recreates the drift this session just spent a day deciding away.*** **`artifact-publish-runbook.md` says nothing about checking whether an exercise already exists** — it covers the engine, the template, the port and the verify, and a new tool's exercises are written from scratch every time. *That is exactly how `core/` came to call cat-cow "Gato-vaca" while five other artifacts called it "Gato-camello".* **What is needed is a lookup step that matches on the MOVEMENT, not the string** — an author writing "Gato-vaca", "Cat-cow" or "Gato camello" must be shown the existing `catcow` entry and reuse its id. *The near-duplicate pass built this session (`automation/cluster-exercise-duplicates.js`) already does that matching; the missing piece is running it against a candidate rather than against the corpus, and a runbook step that requires it.* ⚠️ **Build this in the same branch, not after** — a library nothing is obliged to consult is a suggestion.
  ✅ ***The name half already SHIPPED, September 8, 2026, ahead of this branch — 69 renames in three rounds across all three languages, all 33 clusters now reading one name everywhere*** *(`exercise-name-harmonisation.md`; regression check `automation/exercise-name-audit.js`).* **This branch inherits those names and must not re-decide them.** *It could ship early because names are display strings that already existed in all three languages — no new copy, no translation. The cues cannot: merged wording has to be written in Spanish and translated twice, which is precisely what makes it a branch.* ⚠️ *One cue edit rode along and is deliberate: renaming two pages to "vientre del gemelo" left them promising a restriction only aquiles explained, so D3a's guard sentence was lifted verbatim from aquiles into rodillas and movilidad in all three languages.*
  🚨 ***The SPANISH CUES also shipped, September 8, 2026 — Iván wrote all 33 harmonised cues and they are live across 86 placements in the nine Spanish pages*** *(`data/cue_harmonisation_signed.json`; `exercise-library-decisions.md` D6a–D6c).* ✅ ***EN and PT SHIPPED the same day — 172 cues and 89 names across both languages, Iván's text*** *(`data/cue_harmonisation_en_pt.json`, D7a–D7c).* **All three languages are harmonised; the debt opened in the morning was closed by evening.** ✅ ***And the remaining 166 cues followed the same day — the ENTIRE library, 253 placements × 3 languages, now carries Iván's harmonised names and cues*** *(`data/cue_remaining_signed.csv`, D8a–D8d).* **188 canonical exercises, 187 clips to film.** ⚠️ *Three content losses rode in with that pass — the breathing-tool link, the frog's recovery-day rule, and half the IT-band correction — **all caught by `tests/mobility-matrix.js` and `tests/recovery-matrix.js`, not by reading**, and restored from git. The test suite paid for itself in one session.* 🚩 ***One thing it exposed and did not fix: Portuguese has no register check.*** *Three PT cues arrived in* tu *where the other thirty use* você, *and they were caught by reading. Spanish has `automation/register-sweep.py` for exactly this — **a PT arm is worth building before the next Portuguese content pass.*** ~~⚠️ **EN and PT still carry the OLD cues. That is the single largest open piece of this item and it is a real inconsistency in the repo, not a cosmetic one** — *the same exercise now reads one way in Spanish and another in English and Portuguese, and every day it stays that way the translation job gets compared against a moving Spanish original.* **Do the EN/PT cue pass first when this branch opens, before `exercises.json`.** *Note `automation/mobility-i18n.py` and `recovery-i18n.py` map pre-translated strings by position — they do not translate, so they will not close this on their own.*~~
  **Trigger: the race-pages branch (#1) closes.**
  📌 ***Numbered #15, not #12, and the gap is deliberate — do not "fix" it.*** *`#12`, `#13` and `#14` were all allocated on September 4 in `_to_delete/.next_items_tmp.md` and recovered by the September 8 hygiene pass. **#12 (the LinkedIn rewrite) is DONE and sits in `open-loops-archive.md` under that number**; #13 and #14 were already closed. **Re-using any of the three would put two different items under one label, one of them in the archive** — which is exactly the ambiguity the numbering exists to prevent. A gap in the sequence is cheap; a collided number is not.*
- [ ] **#11 · The TSS / CTL / ATL / TSB explainer — one proper long-form video plus written definitions.** *(Iván, Aug 12, 2026: "something I am always repeating and I have to do it properly.")* **This is the clearest "costs his time forever" item on the list**, which is the test this project applies to everything: it is explained by hand, repeatedly, to nearly every athlete and to leads mid-objection.
   **It already exists as a one-off and is doing real work.** A Loom recorded for a different athlete (`https://www.loom.com/share/22d14e5f153e4aa38178ab0ebe1f0765`) was sent to Roberto Fernández in the middle of the hardest question set any lead in the corpus asked, and it was part of closing him. **Do not re-record from nothing — that Loom is the outline.**
   **Two deliverables, deliberately, because they do different jobs.** The **video** carries the explanation an athlete watches once in week 1 and a lead watches while deciding. The **written definitions** are what someone re-reads in month four when they've forgotten what TSB means, and they are the half that is skimmable, linkable from a coaching message, and indexable — a glossary page is exactly the kind of page that ranks.
   **What it has to cover, from what actually gets asked:** what TSS is and why a number can be impossible (Nadine's run logged **1487 TSS** off a GPS elevation error — Iván caught and corrected it by hand); CTL/ATL/TSB in plain language; how to read the PMC chart; what "form" should look like going into a race; and **why the numbers can sit inside normal range and still be the wrong week to push** — that last one is the whole point of the weekly feedback loop and is the hardest thing to explain in a chat message.
   **Home:** the `/recursos/` hub in NOW, which currently has no page and no content — this is its first real content. Also reusable in the members area, in pre-sale (`sales-playbook.md` B4/B8), and in onboarding week 1.
   **The language trade-off, stated because it will otherwise be discovered late: a video is the one asset that does not translate cheaply.** That argues for the written definitions carrying the ES/EN/PT load and the video being Spanish-only to start, rather than delaying the whole thing until it can ship in three languages.
   Trigger: any time — it is writing and recording, not build work, so it does not queue behind the branch.


### Opened September 6, 2026 — members-token granting

- [ ] **`quick_grant.py` exists only on the VPS at `~/.hermes/`, with no repo copy.** *Found while rewriting `automation/members-area/OPERATIONS.md` §1: Iván's own token playbook ends with `python3 quick_grant.py <email> --name "..."`, and there is no file by that name anywhere in this repo.* **This is precisely the condition the July 31, 2026 standing practice was written to end** — three scripts and the live Caddy config existing only on the box with no version history (`ai-infrastructure-documentation.md` §18). *Either move it into `automation/members-area/` and point the invocation at the repo copy, or delete it: §1 now documents three granting paths, and a fourth undocumented one on the box is worse than none.* ⚠️ **Read it before deciding** — if it prints a token to stdout it is also a leak surface, and §1c already covers the on-the-spot case it was presumably written for.

- [ ] **#16 · n8n attribution is ON in all 17 email nodes, across 10 workflows.** *(Found September 9, 2026, from Iván's screenshot of the `Send an Email` options list — the node's `options` is `{}` everywhere, so the default applies.)* **Every outbound email this business sends currently carries n8n's "sent automatically with n8n" footer**, including the two that go to people who already paid: `Send Welcome Email` and `Send Coaching Welcome Email` in `subscription-lifecycle-automation.json`. ⚠️ ***The CoachMatch rewrite shipped the same day is the one that makes this urgent*** — its opening line is *"te escribo yo para no dejarte solo con un link"*, directly above a machine attribution. **Fix per node: Options → Add option → Append n8n Attribution → OFF** (`"options": { "appendAttribution": false }`). *The live source of truth is n8n, not these files — change it in the UI, then mirror into the `automation/` reference copy.* **The full list:**

  | Workflow | Node(s) |
  |---|---|
  | CoachMatch – Email Nurture 2-3 | `Send an Email`, `Send an Email1`, `Send an Email PT`, `Send an Email1 PT` |
  | CoachMatch Lead Automation | `Send an Email`, `Send an Email PT` |
  | Subscription Lifecycle (New + Churn) | `Send Welcome Email`, `Send Resend Email`, `Send Coaching Welcome Email` |
  | Athlete Intake Form | `Email Briefing` |
  | Website Contact Form → Twenty | `Send confirmation email` |
  | Plan Catalog Email Capture | `Send reply email` |
  | Zone Calculator Lead Magnet | `Send reply email` |
  | Send — CoachMatch PT Backlog | `Send` |
  | Daily Error Digest | `Send Digest Email` |
  | `stage11-cancellation-paste.json` | `Send Goodbye Email` |

  📌 *`zone-workouts-workflow.json` and `zone-workouts-workflow.ascii.json` are the same workflow — one node, two reference copies. **Fix it once in n8n and mirror to both files**, or the next audit reads it as two.*

- [ ] **#17 · Mirror the rewritten CoachMatch ES sequence into `automation/`.** *(Opened September 9, 2026. Trigger: Iván imports the new copy into n8n.)* **The three Spanish emails were rewritten this session** — email 1 no longer carries the price, all three ask a question, and the day-5 break-up offers only the zones guide. *The diagnosis behind it: the n8n email always beat Iván's WhatsApp, so a lead read $149 with no discovery and no framing, then went silent — and the CRM recorded that as "no response". **Price losses were being absorbed by the no-response bucket**, which is why close #1's "price = 6.6% of losses" is a floor, not an estimate.* **Register verified**: `register-sweep.py --wide` returns 0 lines would change; the only flags are `escribí` and `bajé`, both first-person preterite. ⚠️ **Decisions taken with it, so they are not re-opened:** *(a)* **price stays at $149** — Iván has already told his athletes referrals and new customers are at that number; *(b)* **the unsubscribe footer was removed from the ES emails** rather than wired, because `unsubscribe_url` is produced nowhere in either workflow and was rendering an empty `href` (see the runbook correction below); *(c)* **PT stays as-is** — old copy, price still in PT email 1, no footer — *deliberate, not drift: the PT sequence sells All-Access as the language answer and is a different offer*; *(d)* **no reply alert** — replies to the new "respóndeme aquí" CTA land in the IMAP inbox with no notification, and Iván has accepted that.
  🔑 ***What this buys, and it is the reason to do it:*** *with the price out of the emails, **"no response" and "not interested — price" stop overlapping.** Labelled properly from here, six weeks of leads give the first honest read on whether $149 is the problem — which is the read the next pricing decision needs.*

---

## LATER (parked deliberately)

- [ ] **Content engine — corpus awareness and a wider topic vocabulary.** *(Opened and parked Aug 30, 2026. Trigger was the GSC/GA4 pipeline.)* **These are one job.** The first stops the agent repeating what exists; the second gives it somewhere new to go. Doing only the first would leave it deduplicating its way into an ever-smaller space.
  ✅ ***CORPUS HALF DONE, COMMITTED AND VERIFIED IN THE RUN PATH — September 2–3, 2026.*** *Confirmed by reading the code rather than the note: `published_corpus()`, `corpus_idf()` and `coverage_block()` are called at the top of the run, and `screen_ideas()` is called on the generated ideas — it drops near-duplicates with a printed reason and annotates the survivors. Not a prompt rule; computed.* **What remains under this bullet is only the second half — a wider topic vocabulary. `writer_agent.py` still enforces a closed nine-slug list** *(`running, cycling, swimming, triathlon, nutrition, recovery, physiology, strength, weight-loss`)*, ***which is the constraint the original note warned about: deduplicating inside a fixed vocabulary narrows the space it can write in.*** *Not urgent — the blog has never been clicked, so "somewhere new to go" is a distribution question before it is a vocabulary one.*
  ~~***HALF DONE — the corpus half was built September 2, 2026, unparked early by Iván after the Sep 2 batch made it seven duplicate pairs rather than three.***~~ *The trigger was wrong: it assumed GSC was needed, and it was not. Waiting for it cost two more batches.*
  **What shipped, in `research_agent.py`:** `published_corpus()` reads every live article from the `.njk` files (the files, not `content_pieces` — they are what the site serves, they include the six hand-written articles with no database row, and they exclude rows published then removed). `corpus_idf()` + `dup_score()` score a proposed title against the archive; `screen_ideas()` **drops** clones at ≥0.45, **annotates** anything ≥0.28 with its two nearest published articles written into `rationale`, which is the field the review page already renders, and also compares ideas **against each other within one batch**. `coverage_block()` hands the agent article counts per topic and language.
  ***The root cause was not "it cannot see what is published" — it could.*** *The prompt has always carried a list headed "ALREADY PROPOSED OR PUBLISHED (do not repeat)". It was the wrong list — `SELECT working_title FROM content_ideas`, so idea working titles rather than published headlines, rejected ideas mixed in with live ones, and no language, topic or slug to compute anything from — and, more importantly, **nothing checked it.** The agent proposed a second "El método noruego simplificado" with the first one's title in its own context window.* **This is the convergence lesson again**, which went from 2/12 to 8/12 usable ideas the day it moved out of the prompt and into `theme_clusters()`: *a model asked to check something against a long list will say it did; code that computes it does.*
  **Verified by replaying the failures**: all seven duplicate pairs are caught, three of them dropped outright, the rest flagged with the true twin named; and four genuinely new ideas (open-water goggles, GPS altimetry, carbon plates, a marathon world record) pass clean. Both suites in the session log.
  ***Still open: the vocabulary half below.*** *And one honest limitation — title similarity is a proxy. It caught every duplicate we know about, but it cannot see two articles that answer the same question in unrelated words. Traction data is what would.*

  **Teach the research agent what has already been written — the highest-value change left in the content engine now the plumbing works.** *(Opened Aug 30, 2026, from evidence in that day's batch. **BUILT Sep 2, 2026** — see the note above.)*
  **The evidence, not a worry — three near-duplicates in one batch of 20:**
  1. **`El método noruego simplificado`** (ES, physiology) — Aug 30 shares a *literal headline prefix* with the Aug 4 article. Three weeks apart, same subject.
  2. **Threshold intensity control** (ES, physiology) — *"Control de Intensidad en Series de Umbral"* and *"Regulación de intensidad: cómo ajustar tus ritmos de umbral en tiempo real"*, **both written the same day**. The agent does not even deduplicate within its own batch.
  3. **Marathon durability** (PT, running) — Aug 30 *"Durabilidade Muscular na Maratona"* against Aug 4 *"Durabilidade na Maratona: Como evitar o muro"*.
  *Deliberately NOT on this list: the marathon volume/plan cluster and the weight-loss trio, which the same scan flagged. Those are one subject from genuinely different angles — that is what topical authority looks like, and pruning it would be the wrong lesson.*
  **Two inputs, and the cheap one is not the interesting one.** (a) The published corpus — headline, topic, `trans_key`, date — so it stops re-proposing what exists. Needs no GSC. (b) Traction from GSC — the position 5–20 near-misses — so it proposes *improving* an article that is almost competitive rather than writing a second one next to it. **(b) is what turns deduplication into a strategy:** the right response to "we already covered this" is usually to strengthen the existing article, not to skip the topic.
  **Also fix within-batch duplication** — case 2 above is a single generation call proposing two versions of one idea, which no amount of corpus history would catch.

  **The topic vocabulary is quietly acting as a content strategy, and it is too small to sustain the publishing rate.** *(Iván, Aug 30, 2026 — the sharper version of "no swimming articles", and it reframes the corpus-awareness item above.)*
  **The observation:** nine topics, all evergreen training themes. There are only so many articles to write about threshold training; once five exist the sixth overlaps, which is exactly what the Aug 30 batch produced. **Evergreen topics saturate. Timely ones do not** — new equipment, races, results and records happen continuously. At ~20 articles a batch the finite space gets consumed fast.
  **Why the agent cannot currently propose them, which is not "it did not think of it":**
  1. **The `our_assets` rule filters them out.** Every idea must name concrete Triaperformance assets or be discarded as "one anyone could write." A sound quality filter that doubles as a **topic** filter: new tech, race news and world records have no matching plan, methodology section or members artifact, so they are excluded before anything else is weighed. *This is the change that matters — adding topics without relaxing this changes nothing.*
  2. **The agent has never been told the topic vocabulary exists.** `topic` was added to the writer only, so ideas are not topic-tagged and coverage cannot be reasoned about at all.
  3. `gear` **already exists as an `article_type`** and has produced zero articles, because it was gated on the affiliate program (NEXT #9), which is itself gated on traffic. A designed-in capability that has been switched off by a dependency chain.
  **What to add, with the trade-offs stated rather than assumed:**
  - **`gear` / tech as a topic** — directly serves the affiliate plan, and the article type already exists. Strongest candidate.
  - **`racing`** — races, previews, results. **Check against NEXT #1 first**: the race-landing-pages initiative already owns race content, with 12 races researched in `data/races.csv`. Two initiatives covering races would be the same duplication problem one level up.
  - **News and world records** — *the honest counterpoint*: short traffic life, strong on Instagram, weak for SEO. The EN/PT strategy is explicitly SEO and affiliate, so news serves ES/social and not the other two markets. Worth having, worth not measuring like the rest.
  - **`swimming`** — 36 groups, zero articles, in a business whose core market is triathlon. Ten photos already sit behind an empty pill.
  **Feed the agent coverage counts per topic**, not just the list, so "we have 19 running and 0 swimming" is an input rather than something nobody sees.

- [ ] 🆕 **A $229 TrainingPeaks tier: $149 coaching + 1:1 nutrition from a certified nutritionist.** *(Iván, September 3, 2026.)* ***The interesting half is the delivery model, not the price.*** *Two shapes, and they pay differently:* **(1) A coach who is also a nutritionist takes the whole athlete** — *Iván takes a smaller cut and does no work. Pure referral margin, zero hours.* **(2) Iván coaches and the nutritionist takes only the nutrition** — *Iván keeps the coaching revenue and takes a cut for handing over the nutrition lead.* *He already knows someone who can do (1).* **Why it belongs on this list rather than in the parking lot: shape (1) is the only revenue idea anywhere in this repo that adds margin while subtracting his hours**, which is the test the whole project applies to everything else. *Open: which shape, what split, and whether $229 survives contact with the CoachMatch price ladder (`triaperformance-pricing-and-positioning.md` owns every price — do not set one here). Related but distinct: NEXT #8's +$50 testing-and-zones consultation, which is a different upsell on a different asset.*
- [ ] 🆕 **WhatsApp Business API — an AI first-touch and follow-up, not a broadcast tool.** *(Iván, September 3, 2026.)* **The evidence for this is already measured and it is the strongest case for any acquisition item on the list: of 182 CoachMatch leads lost all-time, 129 — 70.9% — were lost to NO RESPONSE. Price accounted for 6.6%.** *(close #1.)* ***An unvoiced objection cannot be answered; a response rate can be worked,*** *and first-touch speed is exactly what a bot is good at.* *The material exists: `sales-playbook.md` owns the real discovery questions, the mirror-then-price sequence and the objection bank, built from 13 converted-athlete transcripts — so this is a delivery-channel problem, not a copywriting one.* ⚠️ **Scope it as first reach and follow-up only.** *Handing an AI the mirror-then-price conversation is a different and much riskier product than having it get a reply and book the human. Meta approval, template messages and opt-in rules are the real work; the AI is the easy part.*
- [ ] **AI Coach product** — `methodology.md` done; open: pricing, liability scope, cannibalization. ~~Trigger: storefront shipped.~~ **Satisfied August 6, 2026** — still parked deliberately, because the three open questions are the real gate, not the storefront.
- [ ] **Weight-loss nutrition guide, bundled free with the weight-loss plans — and the author may be the asset.** 🆕 *(Extended September 3, 2026, Iván: pair each weight-loss **training** plan with a real weight-loss **nutrition** plan, written under his sister's credentials.)* ***That changes what the bundle is.*** *A coach-written nutrition PDF is a differentiator; a plan authored by a credentialed nutritionist is an **authority** claim, and this site already treats named accreditation as the strongest asset it has — see `credentials.json` and §36, where "partner" lost to "Level 2 Accredited" precisely because a resolvable credential beats a vague one.* **Open, and Iván's alone: is she willing to be named, and does she want a cut or a byline?** *That answer decides whether this is a free bundled asset or the entry point to the paid nutrition tier below.* ⚠️ *Written from a short verbal note — check the framing is what he meant before building.* *(Iván, Aug 11, 2026.)* **Signal, and its caveat:** weight-loss plans are ~50% of this month's sales and were 3 of 9 last month — real direction, but on single-digit counts, so treat it as a reason to spend one-time effort, not as a trend. The sharper data point is a WhatsApp lead from the website who asked outright **whether the plan included nutrition guidance** — a stated objection at the point of purchase, which is the only place it costs money.
  **Free and bundled, not sold.** A standalone PDF at ~$15 is a rounding error that adds a second checkout step; bundled, it removes the objection where it occurs and gives every weight-loss listing a differentiator on TrainingPeaks, where the competition ships a bare plan. **One asset, two uses:** bundled with the plan for buyers, and gated behind email capture for non-buyers — which makes it a natural fourth lead magnet for the public lead-magnet landing page in the small slot, and the first one that isn't Spanish-endurance-generic.
  **Authorship and sign-off:** Iván writes it; his sister, a nutritionist from the Universidad de Buenos Aires, reviews and signs off, with her credentials on the guide. That is both the credibility argument and the scope cover — weight-loss nutrition advice edges toward medical territory and should not go out under a coach's name alone.
  **ES first**, EN/PT through the existing translation pass. Note the standing lesson from the members-area branch: build it once in Spanish and the translation never happens on its own.
  **Do not assume this feeds 1:1 coaching.** A $40 weight-loss plan buyer is a different customer from an endurance-performance athlete — more price-sensitive, shorter horizon. Any funnel from the guide to a paid nutrition or coaching offer is a bonus, not the business case, and the coaching-plus-nutrition bundle discussed alongside this on Aug 11 was **deliberately not logged** — noted here only so it isn't re-proposed as new.
  **Trigger: any time.** It's writing work, not build work, so it doesn't queue behind the members-area branch.
- [ ] **AI plan picker** — plain-language ask → facets → ranked 2–3 plans, doubling as email capture. Needs two data-model fields on the ~100 proven plans first (goal description, target weekly hours). Detail: `growth-roadmap.md` §Storefront, Phase 2.
- [ ] **Full direct checkout** — trigger: site-attributed TP sales > ~$1k/mo sustained.
- [ ] **URL filter parameters on the catalog** (`/planes/?distance=Full`) — would let the Ironman hub's two h2 sections link to their own filtered views, and let blog and GBP posts deep-link a filtered catalog. Facets are client-side only with no URL state today. Trigger: any time; small and self-contained.
- [ ] **Scope the mobile chip JS per catalog container** — panel owner ids restart at `g0` for each `[data-catalog]` and the scrim is looked up document-wide, so a page can only carry one grid. Trigger: the first page that genuinely needs two.
- [ ] **`planCard` shortcode uses the raw plan name, emoji and all** — plan pages strip emoji via `displayName`, blog cards don't, so a card title can render a tofu box. Trigger: confirm on a real machine first; may be a headless-font artifact only.
- [ ] **Consolidate the 4 legacy category stylesheets** (`planes-running/-triatlon/-hyrox/-bajar-de-peso.css`) into `planes-hub.css` — ~95% duplicates, so a hub-layout change is a five-file edit. Trigger: next time one of them needs a real change.
- [ ] **Terra API personal integration** — self-test with own training data. Slack-time project, no trigger needed.
- [ ] **WhatsApp context tool** (chat-export → athlete profile). Trigger: athlete context system work begins.
- [ ] **Coach hire** — gated on athlete context system quality. *(Aug 11, 2026: NEXT #8, the +$50 testing-and-zones consultation, is a cheaper way to get the same information first — a bounded, protocol-driven, athlete-facing task you can watch someone do before trusting them with a 1:1 athlete. It doesn't remove the gate; it front-runs it.)*
- [ ] **Content engine feedback agent** — ~~needs 60–90 days of GSC data before the loop can close.~~ 🚨 ***"TRIGGER SATISFIED" was written here on August 30, 2026 and RETRACTED the same session, on Iván's challenge. Read the retraction, not the claim.*** *The backfill did load Search Console history to **2025-05-06**, and the conclusion drawn from it — that the 60–90-day wait was over — confused two different datasets.* **What 16 months of GSC actually covers is the DOMAIN, not the articles.** *Roughly 14 of those 16 months are the HubSpot site (the VPS went live July 16, 2026, Eleventy cut over July 29). The blog did not exist until the content engine's publish path closed on **August 4, 2026** — so on the day this pipeline shipped, the oldest article was **26 days old** and most were younger. The first five near-miss rows contain **zero blog articles**; all of them are storefront pages.*
  **The honest split, and it is two triggers rather than one:**
  **(a) Query-level demand — available now.** *16 months of what the market searches for, by country. Useful to a research agent as "what do people in Brazil ask for", and it is genuinely new information.*
  **(b) Article-level performance — NOT available, and this is the half the feedback loop is named after.** *"Which of my articles earned impressions" needs the articles to have been live long enough to rank. For the August cohort that is roughly **November 2026**. No amount of backfill moves this, because the pages did not exist.*
  ***The mistake worth keeping: a backfill was treated as satisfying a trigger about article age. The data got older; the articles did not.*** *The Aug 12 note's original sequencing — distribution before the feedback loop, because measurement with no traffic measures nothing — was right, and this claim briefly overturned it on a misreading.* **Read the `word_count` note in §9 before writing the agent** — *one of the first five rows returned was an LLM prompt, not a query.*
  🚨 ***The instrumentation half of this bullet was PROMOTED OUT on August 29, 2026 and is now its own item in NOW.*** *Three initiatives depend on it — this agent, the monthly close and the affiliate program — and while it sat inside a LATER bullet, all three read as parked. Do not scope or build the pipeline from the text below: it is the Aug 12 record, and* ***two of its statements did not survive verification*** *— see the item in NOW and `ai-infrastructure-documentation.md` §9. Kept in full because the reasoning is the record.*
  **What is still genuinely LATER is the agent itself**, and its trigger is unchanged: enough GSC history to rank against. *Note the backfill may satisfy that on day one rather than in three months — so the honest gate on starting the agent is the pipeline being* ***verified***, *not the calendar.*
  ***Instrumentation scoped Aug 12, 2026 — SUPERSEDED, retained as the record. The blocker is not the agent, it is that no performance data reaches the VPS at all.*** *The research agent proposes topics with zero knowledge of which past articles earned impressions, rankings or plan clicks — an open-loop content factory. Three things came out of scoping it:*
  **An MCP connector is the wrong mechanism, and it's worth being precise about why.** The registry has no first-party Google connector (only Supermetrics, a paid aggregator). But even a perfect one would only let *Claude in a conversation* query the data. **The agents run on cron with no Claude in the loop** — for the feedback to reach them, the data has to land in Postgres where a script can read it. Connector ≠ agent access.
  **The build is a copy of something already running.** `automation/analytics/sync_pixel_data.py` (05:00 cron) already authenticates a Google service account, pulls BigQuery, writes Postgres. ~~GSC's Search Analytics API and the GA4 Data API are the same shape with different endpoints.~~ *Corrected Aug 29, 2026 — half right. GSC did resolve to the Search Analytics API. **GA4 did not**: its BigQuery export was already running into the same project the pixel script reads, so the GA4 half needed no new endpoint, no new credential and no new grant at all — and the Data API would have been actively worse, because it silently suppresses rows at low user counts, which is exactly this site's regime.* Iván's part: enable both APIs in the Cloud project, add the service account under Search Console → Users and permissions and GA4 → Property Access Management (Viewer). Possibly the same `pixel-sync-vps` account, keeping it to one credential.
  ~~**Both APIs expose ~16 months of history**~~ *Corrected Aug 29, 2026 — the 16 months is a Search Console fact, not a shared one. **GA4 has no history to backfill**: the property and its BigQuery export were created the same day (July 21, 2026), so the export already covers its entire life. The 16-month claim survives where it matters — GSC — and it is the whole reason the GSC half uses the API rather than the bulk export, which is verifiably forward-only.* So this backfills rather than only accruing forward — the "60–90 days" trigger above may already be satisfied for the pre-existing pages the day it's built.
  **The signal that matters is near-misses, not traffic.** The highest-value input for the research agent is the set of queries where the site already ranks position 5–20 with real impressions: demand Google has confirmed, on topics already almost competitive. *"Write a better article about the thing you're on page two for"* is a far stronger instruction than *"here's what four blogs wrote about last week."*
  *Expectation note, so this isn't judged too early: 13 articles, 10 published in the last 8 days. Zero reads at that age is the calendar, not the content. **The thing that would be a real problem is if the pages aren't indexed** — checkable today in Search Console → Indexing → Pages, no build required. Do that before building any pipeline.*

- [ ] **Content engine V2 — reducing the two review gates.** *(Scoped Aug 12, 2026. Not queued: the honest sequence puts distribution and the feedback loop first, since a review gate should only be loosened once there's evidence it's rubber-stamping.)*
  **The principle: don't remove a gate on a hunch — and the evidence already exists, unread.** `content_pieces` stores `original_body` alongside `body` specifically so the model's output can be diffed against what Iván approved. **Nobody has ever looked at it.** A near-zero edit rate across twenty `plan_guide` pieces is an argument for loosening; anything else isn't. That diff is the prerequisite for everything below.
  **Translation approval should go first, and it's the one that can go honestly.** You can't automate *"is this true?"* — but you can automate *"does this say the same thing as the article already approved?"* **Translation is the only stage with a ground truth to compare against.** A fidelity check could verify mechanically that the sibling names the same entities, carries identical physiological numbers, links plans of the same sport/distance, and matches section structure; flag only semantic drift, auto-approve the rest. That removes a third of the approvals, and the Valencia→CIM swap is exactly what it would have caught — the entity changed while everything around it stayed put.
  **Article approval shouldn't be removed, only made cheaper.** Today it means reading 1,400 words cold. A pre-flight report — every numeric claim extracted with whether it traces to `methodology.md`, plan cards listed with their metric/duration spread, assertions appearing nowhere in the KB — turns reading into checking. A second model as adversarial reviewer (annotating, not approving) would plausibly have flagged the 120–160 g/h carbohydrate figure.
  **The idea queue should shrink, not the gate.** *(Iván's observation, Aug 12.)* The research agent predates the translator and still proposes EN and PT ideas for universal topics the translator now covers free. Approving an EN idea *and* letting an ES article translate produces **two English articles competing on the same topic**. EN/PT ideas are only worth approving when genuinely market-specific — a US race, a Portuguese-catalogue constraint. Restricting generation accordingly would cut the queue meaningfully.

- [ ] **Distribution for the content engine — the unbuilt half.** *(Aug 12, 2026.)* The pipeline ends at "the article is live." Nothing announces it: no Instagram, no GBP post, no LinkedIn. **Publishing without distribution is the exact trap this project's operating philosophy names — under-promoted, not under-built.** It also front-runs the feedback loop above: organic takes months, distribution produces readers this week, and measurement with no traffic measures nothing. *Related but distinct: the two open channel decisions (Instagram account type, LinkedIn track identity) are logged separately below from `content-engine-brief.md` §9; those are decisions, this is the build.* ***Update, Aug 17, 2026 — the GBP API application was approved, which was this item's longest-lead dependency. Do not read that as "GBP is a channel now": what is confirmed is API-family access, not the local-post endpoint. See the verification item in NOW before scoping GBP into this build.***
- [ ] **Reviews → social proof, two ideas from Iván, Aug 30, 2026.** *Both became possible the day reviews landed in Postgres with text, rating and date (`gbp_reviews`).* **(a) Auto-generate an Instagram image per new review** — the manual version is something he already does by hand, so the content is proven and only the labour is new. **(b) Rotate recent reviews on the website** — `social-proof-and-reviews.md` maintains its quote bank by hand, and the site's counts have gone stale twice this month; a build-time pull makes both self-maintaining. *Trigger: after the GBP publishing build, which shares the same credential and the same table.* ***Note the review-reply alert idea was considered and DROPPED*** *— all 46 reviews are replied and Google already emails him on each new one. Building a notifier for a solved problem is how a notifier gets muted.*
- [ ] **White-label AI Coach / deploy blueprint / course** — parking lot, post-current-build.
- [ ] **`methodology.md` §13 open gaps (owner: Iván)** — strength prescription logic, minimum effective week + overtraining thresholds, taper lengths for sprint/Olympic, exit policy. Not urgent as coaching, but this doc is pasted into the AI Coach prompt, so they become product gaps the moment that ships. Trigger: AI Coach work starts.
- [ ] Test Persons (Curl Test, Formulario Prueba…) still sitting in Twenty from debugging — cosmetic cleanup, `contact-form-pipeline-runbook.md`. *(The phone country-code item that used to sit on this line closed Aug 8, 2026 — confirmed working in production by Iván.)*
- [ ] Open decisions from `content-engine-brief.md` §9 still genuinely pending: Instagram account type (Business?), LinkedIn track identity. *(Items 4, 5 and 6 in that list — blog architecture, blog IA, paywall boundary — were settled by the Eleventy build. **Struck in the brief itself Aug 12, 2026**, with the decisions recorded rather than deleted: Eleventy with hreflang from `transKey`; `/blog/` at root for ES with `/en/blog/` and `/pt/blog/`; content public and indexable, `/members/*` gated and `noindex`. Flagged as needing this on Aug 8 and carried four days — a "should be struck" note is not a strike, and while it stood the brief still read as six open decisions.)* Item 1 in that list, affiliate programs, is NEXT #9 — it is tracked there, not here.
- [ ] WhatsApp Business profile website link still un-UTM'd (verified accounts can't edit it) — revisit if Meta ever allows editing again.

---

*Closed items and the full session log: **`open-loops-archive.md`**.*

- [ ] 🆕 **The About page does not tell the story, and the story is the strongest asset the site has.** *(Iván, September 4, 2026.)* **The arc: he started coaching himself, then a friend, then family. For years the goal for the whole year was to help one person. Now he coaches 50+ athletes across LatAm.** *That progression is the claim — not the number on its own. It says the coaching was real before it was a business, which is the one thing a prospect cannot verify from a price page and the one thing most coach bios cannot say.* Three pages: `site/sobre-ivan`, `site/en/about`, `site/pt/sobre-ivan`. Voice: `brand-guidelines.md` §8.
  ⚠️ **Guardrail for whoever writes it: 50+ is CUMULATIVE and must never be phrased as a current count.** *`data/athlete_tenure.csv` holds 98 athletes lifetime; `monthly-close/2026-08.md` puts the current book at **37 paying** at 2026-08-31.* **"I've coached more than 50 athletes" is true and strong. "I coach 50 athletes" is false and is the kind of sentence that ends up quoted back in a sales conversation.** *Same rule for "across LatAm" — check it against `data/monthly_close/2026-08-roster.csv` before it ships rather than assuming it.*

- [ ] 🆕 **There is no explainer for how the coaching actually works — probably a video, recorded by Iván.** *(Iván, September 4, 2026.)* **What 1:1 coaching looks like week to week: the feedback cycle, what he looks at, how the plan gets adjusted and why.** *The content already exists and is written down — `methodology.md` owns the weekly decision loop, the adjustment rules and the communication voice; `sales-playbook.md` owns the service-definition block already used in pre-sale. **This item owns publishing it, not deciding it.** Nothing new has to be figured out; it has to be said out loud on camera.*
  **Why it is worth Iván's face rather than a page:** *the process IS the product — the price ladder sells one consistent service, so what separates it from a $30 plan is the loop, and a loop is easier to believe from a person than from a bullet list.*
  🔗 **It also answers the live item "Nothing in the library is visible to a prospect."** *That one asks for pre-purchase evidence and lists ungating a tool, screenshots or a screen capture. A methodology video is the same job from the other direction — it shows the coaching rather than the software — and unlike the artifacts it carries no gate to open.* **Whoever picks up either item should read both.**

- [ ] **The two admin navs diverged, and both files' comments claimed they matched.** *(Verified September 9, 2026 — Iván's observation, confirmed against both renderers. **Parked by him the same day:** the admin works, and the blocker is not the CSS — nobody has stated how `app.py` reaches `:8092`, so a change there cannot be confirmed deployed rather than merely committed.)*
  **`/admin/enlaces/` and `/admin/secuencias/` render `.admin-nav` from `site/_includes/partials/nav.njk`: a top bar, wordmark, three links. `/admin/ideas/` renders `.adminnav` from `automation/content-engine/admin_service/app.py`: below the `<h1>`, no wordmark, six links — its own four views, a separator, then these two — and it labels the first "Ideas pendientes" where the other says "Ideas".** *The comments in both files have been corrected to describe what is actually there; **this item owns the fix, they own the description.***
  **The shape to build:** *the same top bar on all three, with the four Flask views demoted to a second row on `/admin/ideas/` only.* 🔑 ***Service-internal views and cross-tool destinations are different kinds of link, which is exactly what that separator is currently papering over*** *— and splitting them narrows "keep in sync" to three labels and three URLs, which is the only part of this that two renderers can actually hold in common.*
