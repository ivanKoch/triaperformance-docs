# Open Loops — the single source of truth for what's in flight

**Last updated: August 6, 2026** — KB read-through against the live site. **The Plan Storefront is deployed and live** (this file previously said it wasn't — see the sub-item below), which satisfies the trigger on four queued items. Three open items that lived only in `ai-infrastructure-documentation.md` promoted here. Six stale "open/pending" lines struck across four docs.

This file replaces the open-item lists previously scattered across `ai-infrastructure-documentation.md`, `growth-roadmap.md` (Sequencing), `content-engine-brief.md` (Open decisions), and the runbooks. Those docs keep the *detail*; this file is the *list*. Update it at the end of every working session.

*Session history has moved to the [Session log](#session-log) at the foot of this file — it was crowding out the list this document exists to be.*

## Rules
- **WIP limit: 1 big branch + 1 small slot.** A new branch opens only when one closes.
- Every session ends with its branch either **closed** or **parked with an explicit trigger** written here.
- NOW = active this week. NEXT = queued, with its trigger. LATER = parked deliberately, with its trigger.

---

## NOW

### The one big branch: Plan Storefront Phase 1
The only item on this list that moves revenue toward the $5k/mo target this quarter. Detail: `growth-roadmap.md` (storefront section), `plan-storefront-project-brief.md`.

**Definition of done:** dynamic plan template rendering every published plan · facet filters (sport, distance, difficulty, weeks, language, features) · email capture before the TP redirect · All-Access promo module on every plan page + per-language landing page · all redirects carry UTM + plan_id · Product schema on plan pages · live on triaperformance.com.

Sub-items:
- [x] ~~Finish the weekly-breakdown crawl~~ — **done July 29, 2026.** 301/323 published plans captured (22 confirmed dead, race-year plans past their date), zero rate-limit hits at ≥2s sequential pacing. Loaded into the live Postgres `plan_weekly_breakdown` table and confirmed via psql: **765 rows, 301 plans** (exact match, no discrepancy).
- [x] ~~Dedupe the 4 duplicate `plan_id`s in `plans_raw` (439394, 439396, 439397, 612974)~~ — **done, confirmed July 29, 2026.** Live query showed nothing was actually needed: `plans_raw` is already at 381 rows, matching the corrected source CSV exactly, `612836` present with correct content. No SQL run. (The doc previously said Postgres needed a separate fix — that was stale; someone reloaded the table from the fixed CSV at some undocumented point after July 22.)
- [x] ~~Manually check plan_ids 612974 (Lima ES) and 612836 (Lima PT) on TrainingPeaks — both 404 despite `is_published = TRUE`~~ — **done July 29, 2026.** Iván confirmed both are genuinely unpublished. `is_published` flipped to `FALSE` in `data/training_plans_inventory.csv` (published count now 321, was 323), `data/plan_weekly_breakdown_errors.csv` reasons updated, and `plans_raw` on Postgres updated + confirmed via psql. Fully closed.
- [x] ~~Build the plan template + facets + capture + All-Access module~~ — **built July 30, 2026; confirmed LIVE August 6, 2026.** *Corrected August 6, 2026 — this line previously ended "not yet deployed" and was still saying so two days after the fact. It was wrong: the July 30 commits went out on the normal cron pull and the storefront has been live since. Verified by fetching the live site: `/planes/` returns 179 plans, `/en/plans/` 108, `/pt/planos/` 34 — total 321, an exact match to `is_published=TRUE` in `data/training_plans_inventory.csv`. Individual plan pages render facets, the weekly-breakdown table, the All-Access module, the inline email-capture form, and UTM + `plan_id` on every TrainingPeaks redirect. Product/Offer/Brand schema present in the built output, no `aggregateRating` (as designed). The lesson is the §10 crawl lesson again in a different costume: a state written down as pending stays pending in the docs long after reality moved, unless something checks. Deploy state is cheap to verify against the live URL — check it, don't carry it forward.* Individual plan-page template (321 plans, 3 languages, paginated), catalog pages with client-side facet filters (sport/distance/difficulty/weeks/features) for all 3 languages, the 4 existing hand-written category pages converted to the same catalog component (same URLs, same intro copy, presets confirmed against the real data model — HYROX is a distance not a sport, weight-loss is a feature flag not a category), All-Access promo module on every plan page, inline skippable email capture next to the buy CTA, every redirect carries UTM + plan_id. Detail: `ai-infrastructure-documentation.md` §17. **Still not done:** the email-capture backend (see below).
- [ ] **Product schema → validate with Rich Results Test.** ~~Blocked on deploy~~ — **unblocked August 6, 2026**, live URLs exist. Run it against any plan page, e.g. `https://triaperformance.com/planes/p/plan-8-semanas-5-km-base-principiantes-390919/`. ~10 minutes.
- [x] ~~Plan pages look empty~~ — **done August 6, 2026.** Four generated content sections on all 321 plan pages (who it's for / how the training works / what you need / how TrainingPeaks works), All-Access moved to the end, sibling cross-links on 296 of them. Generated from data rather than copied from the TP listings, which cover only 20 plans and would have created duplicate content against TrainingPeaks. Detail: `ai-infrastructure-documentation.md` §17.
- [x] ~~Mobile catalog filters~~ — **done August 6, 2026.** Facet filters fixed (the `[hidden]` attribute was being overridden by author `display` rules, so nothing ever hid), rebuilt as a sticky chip bar with body-portal dropdown panels for mobile, card link styling fixed. **The session's real lesson is infrastructure, not CSS:** `/assets/*` had no cache-busting and Caddy sends no `Cache-Control`, so three rounds of fixes were tested against stale JavaScript. Now hashed (`?v=<md5>`) via a `v` filter in `.eleventy.js`. Full write-up in `build-log.md`.
- [ ] **Confirm the plan-catalog email-capture backend is actually wired.** Partially done and not previously logged as such: the Caddy route (`/api/plan-lead` → `/webhook/plan-lead`) is in `automation/Caddyfile` and therefore deploying via `deploy-website.sh`, and `automation/plan-lead-workflow.json` is written and ready to import. What is *not* confirmed: whether the workflow is imported and Active in n8n, and whether `PLAN_CATALOG` was added to Twenty's `leadSource` enum. **Test:** submit the form on a live plan page, then check Twenty for the Person record. If nothing lands, it's one of those two. Full spec in `plan-lead-pipeline-runbook.md`. Note the cost of the delay: the form has been live and collecting since July 30 and every email typed into it has gone nowhere. The buy button is unaffected either way.

### Small slot (pick one at a time)
- [ ] **New 1:1 athlete onboarding flow** — this week's signup was fully manual. Define the checklist (Twenty record with `customerType`/`churnDate` conventions, member access if applicable, welcome message), then n8n the deterministic parts. (Iván, July 29)
- [x] ~~Deploy the research agent per `automation/content-engine/SETUP.md` — code written, nothing deployed. Gate A only; no writer.~~ — **closed August 4, 2026, and went well past Gate A.** The whole content engine is live: research agent, writer agent, admin review at `/admin/ideas/` + `/admin/drafts/`, an n8n workflow committing approved drafts to GitHub, and auto-translation. Both agents are on cron (research Mon 06:30, writer daily 07:00, translate daily 07:30) via `automation/content-engine/run-agent.sh`. Five articles live across ES/EN/PT from a standing start. Iván's remaining involvement is two approval steps, both genuine judgment. Detail: `ai-infrastructure-documentation.md` §20.
- [x] ~~Notify when content work is waiting~~ — **done August 4, 2026.** `automation/content-engine/notify.py`, daily at 08:00. Pings the Hermes bot when ideas or drafts are pending, when an agent's status file says FAIL, or when one hasn't run at all in longer than its schedule allows. Deliberately quiet otherwise: it sends on new work, on a backlog older than 3 days, or on a problem — a notifier that pings daily regardless gets muted, and a muted notifier looks like coverage while providing none.
- [ ] **Zone calculator at `/members/zonas/`** — three published articles promise a calculator that adjusts the seven zones to your test result; the page is currently a guide. Either build it (it's the gated-teaser artifact the content strategy assumes exists, and would slot into the members-area artifact pipeline from §19) or soften three sentences. (Aug 4, 2026)
- [ ] **Verify the 120–160 g/h carbohydrate figure** in the published durability article — reads high for a 3–4 hour amateur, and the article's whole nutrition argument rests on it. Needs Iván's coaching call, not a search. (Aug 4, 2026)
- [ ] **Public lead-magnet landing page with email capture** — the 3 Spanish lead magnets each have an 8-email nurture sequence attached, but since the migration there is no public page to capture an email on: bare `/guias/` 404s, `/members/guias/` is gated + `noindex`, and the only public path is a direct PDF link that captures nothing. Any external campaign (GBP posts, IG bio, TP listings) currently has nowhere to send cold traffic that starts a sequence. (Found Aug 2, 2026 while building the GBP post set; Iván taking this next.)
- [ ] **Fix `automation/content-engine/SETUP.md`'s `tp-admin` invocation** — it still documents running with `CONTENT_DB_DSN` (a URI). It is now run with discrete `PG_*` variables, because the password contains `:` and `@` and the URI form is a live tripwire. A five-minute doc fix that stops a future session from breaking a working system by following its own runbook. *(Promoted here Aug 6, 2026 — was logged only in `ai-infrastructure-documentation.md` §20 and never made this list.)*
- [ ] **`APPROVED`-but-unpublished drafts are invisible in the admin UI** — `/admin/drafts/` lists only `DRAFTED`, so approving a piece removes it from the only page that can act on it. Less urgent since the publish query selects on status (the next run collects it), but there's still no way to see or retry a stuck one. *(Promoted here Aug 6, 2026 — same reason.)*
- [ ] **`auto_translate` hands education articles the whole catalogue** — when a source article links no plans, the topic filter has nothing to narrow on and falls back to every plan in the language (108 EN, 34 PT). Suspected right answer: pass no plan list at all in that case, as the honest signal that the piece isn't a selling article — but confirm against real drafts before changing it. *(Promoted here Aug 6, 2026 — same reason.)*
- [x] ~~Ronald Yesid duplicate active tokens — dedupe (query in `OPERATIONS.md`)~~ — **done July 29, 2026.** Wasn't a true duplicate-active case (only 1 of 2 rows was ever active); deactivated the other for cleanliness. Confirmed via psql.
- [x] ~~Duplicate-lead Telegram notification pulls raw HubSpot email HTML — fix~~ — **closed July 29, 2026.** Root cause was pre-July-24 (mailer-daemon/calendar-notification emails passing through the old zero-filter IMAP trigger); already fixed by the existing Filter node. No code change needed. While testing this, found and fixed two real bugs instead: a `Location`-field parsing bug that broke `preferredLanguage`/phone detection/`addressCountry` for leads without a full 3-part location, and added a new BR/AR + no-phone suppression filter for the WhatsApp-outreach Telegram ping (both live-tested, both mirrored into `automation/coachmatch-lead-automation.json`). Detail: `ai-infrastructure-documentation.md` §12 addendum.
- [x] ~~GSC: pull the flagged URLs from Indexing > Pages, confirm the "Duplicate without user-selected canonical" entries are pre-migration stale~~ — **done July 29, 2026.** Real bug, not stale: Caddy served `triaperformance.com` and `www.triaperformance.com` as one identical, non-redirecting site block, so Google picked the www version as canonical despite the correct declared tag. Fixed with a Caddy split (www now 301s to apex), verified live via curl. Detail: `ai-infrastructure-documentation.md` §15 addendum.
- [x] ~~Eleventy Phase 5 cleanup — delete `website/`, move `images/`/`hubfs/`/`guias/` into `site/assets/`~~ — **done July 29, 2026, scope revised.** `hubfs` turned out to be permanent, not migration debt — TrainingPeaks' own marketplace hotlinks that exact route across ~300 live plans, confirmed with Iván, so it stays in `website/hubfs` forever. Only `images` and `guias` moved into `site/assets/`. Along the way: built a real gated landing page at `site/members/guias/index.njk` (Spanish only) since the old bare `/guias/` link had no index and always 404'd (true pre-migration too, just never noticed); added 3 explicit Caddy redirects (old `/guias/<file>.pdf` → `/assets/guias/<file>.pdf`, not wildcarded, so they don't clash with the new real `/members/guias/` page) for the PDF links already sitting in sent emails; deleted the superseded `website/planes`/`pt`/`all-access`/`members`/`en` folders (`website/` itself stays, now holding only `hubfs`, `googleTag.html`, `.DS_Store` — left alone, not tracked as a to-do). Fully verified live: hero images, guías PDF redirect, `/members/guias/` (light theme, matching other subpages after catching a dark-theme CSS mismatch), all confirmed working post-deploy. Detail: `website-build-cutover-runbook.md` steps 17–19, `ai-infrastructure-documentation.md` §16.
- [x] ~~The 3 downloadable guides only exist in Spanish, but the EN and PT blog articles link to the Spanish PDF anyway~~ — **closed July 29, 2026, stopgap.** Removed the guide-reference paragraph from both the EN (`how-to-choose-a-marathon-plan.njk`) and PT (`como-escolher-seu-plano-de-maratona.njk`) articles rather than link a mismatched-language PDF. Real EN/PT translations of the guide, if ever wanted, would be new content work, not logged as a pending item. **Resolved July 29, 2026:** the filenames were just non-self-describing — confirmed by Iván: `pre-entreno.pdf` = nutrition lead magnet, `intervalos.pdf` = mental-training lead magnet, `zonas-de-entrenamiento.pdf` = zones lead magnet. The KB's "nutrition / mental prep / zones" description was correct all along. Additionally, the two FULL guides (`guia-de-herramientas-nutricionales.pdf`, `guia-de-herramientas-mentales.pdf`) are now in `site/assets/guias/` and the members `/members/guias/` page shows all 5 with accurate titles. The 3 lead magnets remain for use in comms/emails/flows — nothing pending there.
- [x] ~~Export the full 2,073-contact HubSpot CSV~~ — **done July 29, 2026.** Keep the CSV out of the git repo (2,073 contacts' PII) — store it locally/Drive/Bitwarden-adjacent, note where.

---

## NEXT (queued, in order, each with its trigger)

1. **TP listing rewrites experiment — LIVE, all 21 listings pasted July 29-30, 2026. Waiting on data.** EN 9 (`en-listing-rewrites-2026-07.md`) + ES 8 / PT 4 (`es-pt-listing-rewrites-2026-07.md`); control = the 3 untouched EN sellers (413817, 414883, 602981, 2.2% conv). **Checkpoints: peek ~Aug 30 (directional), real read ~Oct 27 (90 days)** — queries and decision rule in the Measurement section of `es-pt-listing-rewrites-2026-07.md`. Also watch GA4 for source=trainingpeaks sessions (new contact-CTA funnel from listings → Twenty). Fixed along the way: `plan_views_clean` on the VPS now excludes all 3 personal IPs, matching BigQuery.
2. **Race-specific landing pages** — planned, gathering done — build triggered by plan template. Planning session held July 30, 2026: race longlist matched against `training_plans_inventory.csv` (only (distance × language) pairs with ≥3 matching plans qualify — kills EN full-distance and PT 70.3/full entirely, leaves PT as a marathon-only play), per-race data schema proposed (`race-page-data-schema.md` — plans are joined live at render time by distance/language/weeks-countdown, never stored as a static plan_id per race, so pages stay evergreen), 12 Tier 1 races fully researched and gathered into `data/races.csv` (dates, course profiles, hooks, weather, qualifying relevance, target queries, sources — flagged where a date is a pre-official-announcement estimate), and a page content outline written (`race-page-content-outline.md` — course guide → how-to-train (methodology.md) → laddered plan recs → All-Access module → email capture → FAQ; slots under the existing `/planes/running/`, `/planes/triatlon/` sport-hub IA). Full candidate longlist + tiering + search-window calendar in `race-landing-pages-longlist.md`. ~~Trigger to actually build: plan template deployed live~~ — **trigger satisfied, confirmed August 6, 2026.** The template is live in all three languages; nothing gates this build any more. This is the natural next big branch the moment the storefront branch closes. *Update, same day — Iván's review corrected the inventory read: EN full-distance Ironman actually qualifies (3 plans, not 1 — 2 were flagged `is_published=FALSE`, looks like a stale-flag bug, verify on TrainingPeaks and flip per usual protocol), and the plan-matching join now also facets on intensity type (pace/HR/power) and a strength/gym add-on, not just difficulty — matches the facet filters already planned for the storefront in `growth-roadmap.md`. California International Marathon (Dec 6, 2026) added to `data/races.csv` as a 3-language (EN/ES/PT) test case, to trial the full template pattern on one real race before the rest of the batch. All three docs + the CSV updated accordingly.*
3. **Weekly GBP-review → IG testimonial drip — already running manually at ~1/week.** *(Corrected Aug 2, 2026: the "33 never-posted ≈ 8 months" figure was stale. Real numbers are 45 reviews, 36 already repurposed, 9 unused ≈ 9 weeks — plus a recyclable back catalog and ~14 asks due next cycle, so supply is fine.)* The open item is no longer content supply, it's **automating the drip** so it stops costing weekly manual time. Trigger: any time; first recurring content job.
4. **Full HubSpot migration (all 2,073 contacts) + re-engagement email blast + HubSpot decommission** — decided July 29: migrate all, not active-only. ~~Trigger: storefront/website content ready to land the blast on.~~ **Trigger satisfied August 6, 2026** — the storefront is live and is exactly the destination this blast was waiting for. **Last step of decommission, deliberately left open until then (flagged Aug 2, 2026):** the DNS zone still carries HubSpot's SPF include (`7203776.spf02.hubspotemail.net`) and two DKIM CNAMEs (`hs1-`/`hs2-7203776._domainkey`), which still authorize that HubSpot portal to send as `@triaperformance.com` even though it sends nothing. Pull them only after the blast is sent and the portal is closed — not before, in case the blast goes out from HubSpot. Detail: `deploy-runbook.md` §1.
5. **EN/PT review generation** (playbook in `social-proof-and-reviews.md`) — every EN/PT athlete gets asked until EN ≥10, PT ≥5. Trigger: ongoing, milestone-triggered asks.
6. **B-lite premium bundle** (+$50 onboarding call on top sellers). ~~Trigger: storefront live.~~ **Trigger satisfied August 6, 2026.**
7. **Affiliate program applications** (Amazon first). Trigger: first gear article scheduled — the 180-day/3-sale clock starts at *approval*, so don't apply before traffic exists.

---

## LATER (parked deliberately)

- **AI Coach product** — methodology.md done; open: pricing, liability scope, cannibalization. ~~Trigger: storefront shipped.~~ **Trigger satisfied August 6, 2026** — still parked deliberately (the three open questions are the real gate, not the storefront), but it is now parked by choice rather than by dependency.
- **Terra API personal integration** — self-test with own training data. Slack-time project, no trigger needed.
- **WhatsApp context tool** (chat-export → athlete profile). Trigger: athlete context system work begins.
- **Coach hire** — gated on athlete context system quality.
- **Content engine writer + publisher + feedback agents** — Gate A must prove idea quality first; feedback loop needs 60–90 days of GSC data.
- **White-label AI Coach / deploy blueprint / course** — parking lot, post-current-build.
- **Deeper analytics** (GA4 User-ID, per-event access log, BigQuery→VPS sync) — planning-only.
- **Direct checkout** — trigger: site-attributed TP sales > ~$1k/mo sustained.
- "Who's in Twenty but has no members access" query — trigger: athlete list too big to eyeball.
- Kettlebell members page nav unification — cosmetic.
- Verify with TP account manager: All-Access rev share + monthly subscriber/churn reporting.
- Open decisions from `content-engine-brief.md` §9 still pending: Instagram account type (Business?), LinkedIn track identity.
- WhatsApp Business profile website link still un-UTM'd (verified accounts can't edit it) — revisit if Meta ever allows editing again, or if WhatsApp support confirms a workaround.

---

## Recently closed (context for new conversations)

- Artifact publishing pipeline (Aug 1) — repeatable prototype → approve → port → verify → deploy process for interactive members-area tools, documented in `artifact-publish-runbook.md`. First artifact: Activación de Running (`/members/activacion/` — 8-exercise circuit, 40s timer, previous/skip/replace controls, unilateral = 40s per side) — **deployed live same day.** Second: Activación de Ciclismo (`/members/activacion-ciclismo/`), built end-to-end autonomously as a process test. Both tools then rebuilt same day on a mobile-first 3-tab template (v2) adapted from Iván's Claude-artifact prototypes — shared engine/partial/CSS, tool pages are now data-only, `video` field ready for future YouTube embeds. Third: Core Sin Excusas (`/members/core/`, hotel-room core routine, first pure data-only page on the template, `fuerza` category) — **all pending Iván's commit/push + phone check.** Dark artifact theme now the documented default (`brand-guidelines.md` §7.1). Detail: `ai-infrastructure-documentation.md` §19.

- External-channel UTM rollout (Jul 31) — convention defined and documented (`ai-infrastructure-documentation.md` §9): `utm_source`=platform, `utm_medium`=placement type, `utm_campaign`=`profile` (evergreen) or a content slug, optional `utm_content` for variants. Applied to Instagram bio, Google Business Profile (profile field + post-generator template saved for reuse), Facebook Page about field (updated even though not actively used), LinkedIn, Strava bio, TrainingPeaks coach bio, email signature, and YouTube channel about section. **Not applied:** WhatsApp Business profile — verified accounts can't have their website field edited; revisit if/when that unlocks (see LATER). Not yet verified in GA4 — check Acquisition → Traffic acquisition in ~1–2 weeks to confirm each source/medium is actually landing distinctly, since this is the first time non-plan-page UTMs exist on the site.
- CoachMatch pipeline reliability session (Jul 31) — three real bugs fixed: WhatsApp watchdog rebuilt (message 2/3 progression, auto-lost after 7 days silent, no-phone leads no longer clutter the digest), email-nurture `emailTouchCount` stuck-at-1 bug (GraphQL `Float!`/`Int` type mismatch failing the query silently), and a multi-lead-per-execution bug in lead creation (n8n Code nodes defaulting to "Run Once for All Items" while written single-item style, so a 2-lead IMAP batch only ever updated the first lead). All three fixed and at least one (lead creation) confirmed live. Also closed the "blind scripts" problem: every VPS Python script (`twenty_followup_check.py`, `update_lead_status.py`, `sync_pixel_data.py`) and the live Caddyfile are now tracked in the repo, with a dispatcher/crontab pattern so future edits only ever need `git commit && git push`. Detail: `ai-infrastructure-documentation.md` §18. **Not yet verified:** WhatsApp watchdog hasn't run against a real batch yet (first live cron run Aug 1); no bulk audit of historically-stuck leads from either bug.
- Monday coaching check-in — validated on a real Monday, fully live (Jul 2026)
- Managed Hermes plan — cancelled (Jul 29)
- Eleventy cutover + sitemap/robots + analytics sanity check (Jul 26–28)
- Members-area auth gate + subscription lifecycle + customer backfill (Jul 24–25)
- Contact form → Twenty pipeline (Jul 22)
- Six member artifacts gated behind login (carga, carrera, nutricion, tests, zonas, kettlebell)
- Blog live with first article ×3 languages; 323/323 plan links verified OK (Jul 27)
- Plan-link "6 dead plans" myth closed — artifact of a partial crawl (Jul 27)


---

## Session log

*Newest first. One entry per working session; the detail lives in the docs each entry names.*

- **August 6, 2026** — KB read-through checked against the live site. One material finding: **the Plan Storefront has been live since the July 30 commits went out on the cron pull**, while this file, `ai-infrastructure-documentation.md` §17 and `business-overview.md` all still described it as built-but-not-deployed. Verified against the live URLs (321 plans across ES/EN/PT, exact match to the inventory CSV; schema, facets, All-Access module and UTM'd redirects all rendering). That deploy was the stated trigger on four queued items — race pages, the HubSpot blast, B-lite, and AI Coach — all of which had been sitting behind a gate that had already opened. Also promoted three open items that lived only in the infra doc, logged the missing Aug 5 session, and struck six stale "open/pending" lines across four docs. No build work; a correction session
- **August 5, 2026** — n8n patched 2.30.5 → 2.33.4 in response to n8n's bi-weekly security advisory (multiple High-severity CVEs — GraphQL node credential-header leak, MCP node path-traversal RCE, JS task-runner sandbox escape, Git node code execution). Container recreated in place, `/root/.n8n` tarballed first, all workflows verified Active afterwards and a real website form submission confirmed end-to-end. Detail: `ai-infrastructure-documentation.md` §8 addendum. *(Logged here Aug 6, 2026 — the session happened and was documented in the infra doc, but never made this list.)* Off-branch maintenance session
- **August 4, 2026** — content engine's second half built and live — n8n publish workflow committing approved drafts to GitHub, both agents plus auto-translation on cron, five articles live across three languages from a standing start. Three bugs worth remembering: n8n's `queryReplacement` splits on commas and silently dropped one piece from every batch approval; the translator was swapping the race per market until Iván caught it, which would have made hreflang assert three different articles were the same page; and `validate()` couldn't see a wrong-language plan link because it builds cleanly. Detail: `ai-infrastructure-documentation.md` §20. Off-branch session — no change to the Plan Storefront big branch
- **August 2, 2026** — KB consistency audit, second pass — methodology's injury rule corrected so §7 no longer contradicts §11's red line (no researched medical context, refer out — this doc is pasted into the AI Coach prompt, so the disagreement was a product defect); `deploy-runbook.md` rewritten around the real Eleventy deploy path with the dead rsync-`website/` process removed; the Twenty "native MCP server" claim corrected in `growth-roadmap.md` to match `twenty-crm-runbook.md`; the 2,073-contact scope decision no longer described as open; closed items and HubSpot leftovers stripped from `ai-infrastructure-documentation.md`'s open list, `brand-guidelines.md` and `contact-form-pipeline-runbook.md`; All-Access "all plans included" confirmed as an approved marketing claim and the old caveat retired across three docs. Left deliberately open: the HubSpot SPF/DKIM DNS records, now logged under NEXT #4 as the last step of decommission
- **August 2, 2026** — KB consistency audit, first pass — all 16 docs read end-to-end, 16 discrepancies found, 11 fixed with dated inline notes: GBP review counts corrected in 3 more places, TP Gold list price corrected to $359, All-Access ES subscriber count corrected to 1 after a churn (live total 2: 1 ES + 1 PT), the `/guias/` PDF path corrected in 3 docs, the "6 dead plans" and "4 duplicate plan_ids" ghosts retired for good, `business-overview.md`'s website and CRM status rewritten, HubSpot restated as sunset everywhere, `brand-guidelines.md` §9 page inventory brought in line with what actually shipped, and the All-Access PT product acknowledged. Project instructions gained a Project Memory scope-and-upkeep section, a Knowledge-base hygiene section, and a complete KB index; project Memory rewritten thin and mirrored at `triaperformance-project-memory.md`. Left open by decision: the plan-catalog count discrepancy and the legacy coaching-book figures. Still open, needs Iván's call: whether All-Access includes the entire plan catalog — see NEXT
- **August 2, 2026** — GBP monthly-post set reworked — switched from generic coaching copy to an asset-led rotation: Medal Monday · plans catalog · All-Access · rotating educational slot, each pointing at its own UTM'd site destination instead of a raw `wa.me` link, and AI-generated imagery dropped per `brand-guidelines.md` §6. Review counts corrected across three docs: 45 total, 36 repurposed, 9 unused. New gap logged below — the 3 lead magnets have 8-email nurture sequences but no public landing page with email capture since the migration. Off-branch content session, no change to the Plan Storefront big branch
- **August 1, 2026** — artifact → members-area publishing pipeline established — new `artifact-publish-runbook.md`, first artifact `/members/activacion/` built + verified in local build, dark artifact theme codified in `brand-guidelines.md` §7.1; pending Iván's commit/push + live check. Off-branch small-slot session, no change to the Plan Storefront big branch
- **July 31, 2026** — external-channel UTM rollout closed — see Recently closed; CoachMatch pipeline reliability session — three real bugs found and fixed in WhatsApp follow-up, email nurture, and multi-lead lead-creation handling; all VPS Python scripts + the live Caddy config migrated into the repo, closing the "blind scripts" problem for good. Detail: `ai-infrastructure-documentation.md` §18. No change to the Plan Storefront big branch — this was an off-branch reliability fix session
