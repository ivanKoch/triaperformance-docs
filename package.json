{
  "name": "triaperformance-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for triaperformance.com. Source lives in site/, build output goes to _site/.",
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site",
    "test": "node tests/zones-calc.test.js && node tests/zones-ui.test.js && node tests/internal-links.test.js",
    "test:ui": "node tests/zones-ui.test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "jsdom": "^30.0.1"
  }
}
