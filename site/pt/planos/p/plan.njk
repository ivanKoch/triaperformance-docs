---
pagination:
  data: plans.byLanguage.pt
  size: 1
  alias: plan
  addAllPagesToCollections: true
permalink: "/pt/planos/p/{{ plan.slug }}/"
layout: layouts/base.njk
lang: pt
pageCss: plan-page.css
eleventyComputed:
  title: "{{ plan.seoTitle | safe }}"
  ogTitle: "{{ plan.displayName | safe }}"
  description: "{% set d %}{{ plan.displayName | safe }}: plano de {{ plan.weeks }} semanas na TrainingPeaks{% if plan.weeklyTotals and plan.weeklyTotals.sessions %}, {{ plan.weeklyTotals.sessions }} sessões por semana{% if plan.weeklyTotals.hoursText %} ({{ plan.weeklyTotals.hoursText }} em média){% endif %}{% endif %}. Zonas, progressão e semanas de recuperação incluídas. US$ {{ plan.price }}.{% endset %}{{ d | clamp(158) }}"
---
{% include "partials/plan-page-content.njk" %}
